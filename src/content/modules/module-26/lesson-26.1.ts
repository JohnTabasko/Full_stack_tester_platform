import type { Lesson } from "../../../renderer/types";
import theory26_1 from './lesson-26.1.md?raw';

export const lesson26_1: Lesson = {
  "id": "26.1",
  "moduleId": 26,
  "title": "Aplikacje mobilne webowe, natywne i hybrydowe",
  "description": "Różnice między responsive web, PWA, aplikacją natywną i hybrydową oraz ich konsekwencje dla strategii testów.",
  "order": 1,
  "difficulty": "advanced",
  "tags": [
    "mobile",
    "responsive",
    "pwa",
    "natywne",
    "hybrydowe",
    "strategy"
  ],
  "content": {
    "objective": "Po ukończeniu lekcji potrafisz odróżnić mobile web, PWA, aplikacje natywne i hybrydowe, dobrać narzędzia testowe do typu produktu oraz zaprojektować strategię testów mobilnych opartą na ryzyku.",
    "theory": theory26_1,
    "codeExamples": [
      "type MobileAppKind = 'mobile-web' | 'pwa' | 'natywne' | 'hybrydowe';\n\ntype MobileRisk = {\n  kind: MobileAppKind;\n  risks: string[];\n  suggestedTools: string[];\n};\n\nconst pwaStrategy: MobileRisk = {\n  kind: 'pwa',\n  risks: ['offline cache', 'service worker update', 'powiadomienia push'],\n  suggestedTools: ['Playwright', 'real device smoke', 'Lighthouse'],\n};\n",
      "# Przykład macierzy minimalnej\n- iPhone, aktualny iOS, Safari\n- Android średniej klasy, aktualny Chrome\n- Android starszy, poprzednia wersja systemu\n- Tablet, orientacja pozioma\n"
    ],
    "exercises": [
      {
        "id": "ex-26-1-1",
        "title": "Macierz urządzeń",
        "description": "Dla tematu „Aplikacje mobilne webowe, natywne i hybrydowe” zaprojektuj minimalną macierz urządzeń, systemów i rozdzielczości opartą na ryzyku.",
        "solution": "## Przykładowa macierz urządzeń dla aplikacji e-commerce (mobile web + PWA)\n\n### Krok 1: Analiza danych użytkowników\nZałóżmy, że analytics pokazuje:\n- Android: 58% użytkowników\n  - Samsung Galaxy A54 (14%), Pixel 7 (12%), Xiaomi Redmi Note 12 (9%), Samsung Galaxy S23 (6%), inne (17%)\n- iOS: 38% użytkowników\n  - iPhone 14 (15%), iPhone 13 (11%), iPhone 12 (7%), iPhone 11 (5%)\n- Tablet: 4% użytkowników\n\n### Krok 2: Identyfikacja ryzyk specyficznych dla produktu\n- Checkout (krytyczna ścieżka) — ryzyko klawiatury ekranowej\n- Lista produktów — ryzyko wydajności na low-end\n- Płatność — ryzyko sprzętowe (NFC na Android vs. Apple Pay)\n- Camera (skanowanie produktów) — wymaga realnego urządzenia\n\n### Krok 3: Macierz trójwarstwowa\n\n```markdown\n## Warstwa 1: PR / Pull Request (szybki feedback)\n| Urządzenie | OS | Typ | Co testujemy |\n|---|---|---|---|\n| Pixel 7 emulator | Android 14 | Emulator | Mobile web smoke, PWA offline smoke |\n| iPhone 14 simulator | iOS 17 | Emulator | Mobile web smoke |\n| Galaxy A54 real | Android 13 | Real | App hybrydowa smoke |\n\n## Warstwa 2: Nightly (regular coverage)\n| Urządzenie | OS | Typ | Co testujemy |\n|---|---|---|---|\n| Pixel 7 real | Android 14 | Real | Checkout, klawiatura, gesty |\n| iPhone 14 real | iOS 17 | Real | Checkout, Apple Pay |\n| Samsung Galaxy A54 real | Android 13 | Real | Mid-range performance, NFC |\n| iPhone 12 real | iOS 16 | Real | Older iOS, mniejsza pamięć |\n| Xiaomi Redmi Note 12 real | Android 13 | Real | Low-end performance |\n\n## Warstwa 3: Release (full coverage)\n| Urządzenie | OS | Typ | Co testujemy |\n|---|---|---|---|\n| Wszystkie z nightly + |\n| Samsung Galaxy S23 Ultra | Android 14 | Real | High-end GPU, large screen |\n| iPhone 11 real | iOS 15 | Real | Oldest supported iOS |\n| iPad Air (tablet) | iPadOS 17 | Real | Tablet layout, landscape |\n| Huawei P30 (bez GMS) | Android 10 | Real | Edge case — brak Google Services |\n```\n\n### Krok 4: Kryteria selekcji urządzenia\n| Kryterium | Próg | Dlaczego |\n|---|---|---|\n| Pokrycie użytkowników | > 5% | Zasada Pareto — top 5 urządzeń = 70%+ użytkowników |\n| Różnorodność OS | Min. 2 wersje wstecz | Android 13 i 14; iOS 15, 16, 17 |\n| Różnorodność sprzętowa | Low-end + mid + high | Performance regression |\n| Historia problemów | > 3 crash reports | Crash addressal |\n| Nowości | Top sprzedawane w ostatnim kwartale | Keep matrix fresh |\n\n### Krok 5: Automatyzacja w Playwright (mobile web/PWA)\n\n```typescript\n// playwright.config.ts — macierz urządzeń mobile web\nimport { defineConfig, devices } from '@playwright/test';\n\nexport default defineConfig({\n  projects: [\n    // PR — emulatory (szybki feedback)\n    { name: 'emulator-android-14', use: { ...devices['Pixel 7'] } },\n    { name: 'simulator-ios-17', use: { ...devices['iPhone 14'] } },\n    \n    // Nightly — real devices (BrowserStack)\n    { name: 'pixel7-real', use: { browserName: 'chromium', viewport: { width: 412, height: 915 } } },\n    { name: 'iphone14-real', use: { browserName: 'webkit', viewport: { width: 390, height: 844 } } },\n    \n    // Release — tablet\n    { name: 'ipad-real', use: { ...devices['iPad (gen 7)'] } },\n  ],\n});\n```\n\n### Podsumowanie decyzji\n\n| Decyzja | Uzasadnienie |\n|---|---|\n| 3 urządzenia na PR | Szybki feedback < 10 min |\n| 5 urządzeń na nightly | Pokrycie krytycznych ścieżek < 30 min |\n| 8 urządzeń na release | Kompletność przed wydaniem |\n| Macierz wynika z analytics | Nie z listy możliwości |\n| Low-end jest w matrix | Performance regression jest krytyczny |"
      },
      {
        "id": "ex-26-1-2",
        "title": "Stan początkowy",
        "description": "Opisz setup testu: instalacja aplikacji, dane lokalne, uprawnienia, sesja i sieć.",
        "solution": "## Setup testu mobilnego — kompletny stan początkowy\n\n### 1. Instalacja aplikacji\n\n**Android (Appium):**\n\n```typescript\n// Czysty start (fullReset: true)\nconst cleanInstallCaps = {\n  'appium:app': './apps/shop-android-debug.apk',\n  'appium:fullReset': true, // Odinstaluj po każdym teście\n  'appium:noReset': false,\n};\n\n// Szybki start (noReset: true, setup danych przez API)\nconst quickStartCaps = {\n  'appium:app': './apps/shop-android-debug.apk',\n  'appium:noReset': true, // Zachowaj dane między testami\n  'appium:fullReset': false,\n};\n\n// Setup APK przed sesją\n// adb install -r ./apps/shop-android-debug.apk\n```\n\n**iOS (Appium):**\n\n```typescript\nconst iosCaps = {\n  'appium:app': './apps/shop-ios.ipa',\n  'appium:fullReset': true,\n  'appium:preventRebuilds': false, // Rebuild jeśli needed\n};\n```\n\n**Playwright (mobile web/PWA):**\n\n```typescript\n// Nowy context = czysta sesja przeglądarki\nconst cleanContext = await browser.newContext({\n  viewport: { width: 390, height: 844 },\n  permissions: [], // Bez uprawnień domyślnie\n  storageState: undefined, // Brak ciasteczek\n});\n```\n\n---\n\n### 2. Dane lokalne (cleanup przed testem)\n\n**Android — czyszczenie danych app:**\n\n```bash\n# Przed testem\nadb shell pm clear com.example.shop\nadb shell am force-stop com.example.shop\n\n# Lub przez Appium\nawait driver.execute('mobile: clearAppData', { pkg: 'com.example.shop' });\n```\n\n**Playwright — czyszczenie localStorage/IndexedDB:**\n\n```typescript\n// Przed testem\nawait page.goto('about:blank');\nawait page.evaluate(() => {\n  localStorage.clear();\n  sessionStorage.clear();\n  indexedDB.databases().then(dbs => {\n    dbs.forEach(db => indexedDB.deleteDatabase(db.name));\n  });\n});\n\n// Service Worker cleanup\nawait context.clearCookies();\nawait context.clearPermissions();\n```\n\n**Appium — reset app między testami:**\n\n```typescript\n// W beforeEach\nawait driver.execute('mobile: clearAppData', { pkg: 'com.example.shop' });\nawait driver.launchApp();\n\n// W afterEach — pełny reset\nawait driver.execute('mobile: removeApp', { pkg: 'com.example.shop' });\nawait driver.installApp('./apps/shop-android-debug.apk');\n```\n\n---\n\n### 3. Uprawnienia (świadome ustawienie)\n\n```typescript\n// Playwright — jawne ustawienie uprawnień\n\n// Scenariusz A: Wszystkie uprawnienia\nconst contextWithAllPerms = await browser.newContext({\n  permissions: ['geolocation', 'notifications'],\n  geolocation: { latitude: 52.2297, longitude: 21.0122 },\n});\n\n// Scenariusz B: Brak uprawnień (testuj odmowę)\nconst contextNoPerms = await browser.newContext({\n  permissions: [],\n});\n\n// Scenariusz C: Częściowe uprawnienia\nconst contextPartialPerms = await browser.newContext({\n  permissions: ['geolocation'], // ale NIE notifications\n});\n```\n\n**Appium — kontrola uprawnień Android:**\n\n```typescript\n// Przed testem: ustaw uprawnienia\n// autoGrantPermissions: false = system pyta, Appium NIE akceptuje automatycznie\n\nawait driver.execute('mobile: grantPermissions', {\n  pkg: 'com.example.shop',\n  permissions: [\n    'android.permission.ACCESS_FINE_LOCATION',\n    'android.permission.CAMERA',\n  ],\n  grant: true,\n});\n\n// Użytkownik odmawia:\nawait driver.execute('mobile: grantPermissions', {\n  pkg: 'com.example.shop',\n  permissions: ['android.permission.CAMERA'],\n  grant: false, // revoke\n});\n```\n\n---\n\n### 4. Sesja (stan użytkownika)\n\n```typescript\n// Playwright — setup zalogowanego użytkownika\n\ntest.beforeEach(async ({ page, request }) => {\n  // Stwórz użytkownika przez API\n  const userResponse = await request.post('/api/users', {\n    data: {\n      email: `test-${Date.now()}@example.test`,\n      password: 'TestPass123!',\n      role: 'customer',\n    },\n  });\n  const user = await userResponse.json();\n  \n  // Pobierz token autoryzacji\n  const loginResponse = await request.post('/api/auth/login', {\n    data: { email: user.email, password: 'TestPass123!' },\n  });\n  const { token } = await loginResponse.json();\n  \n  // Ustaw ciasteczko sesji\n  await page.context().addCookies([{\n    name: 'session_token',\n    value: token,\n    domain: 'localhost',\n    path: '/',\n  }]);\n  \n  // Zapisz user ID w test info dla cleanup\n  test.info().annotations.push({ type: 'user-id', description: user.id });\n});\n\ntest.afterEach(async ({ request }) => {\n  const userId = test.info().annotations.find(a => a.type === 'user-id')?.description;\n  if (userId) {\n    await request.delete(`/api/users/${userId}`);\n  }\n});\n```\n\n**Appium — sesja użytkownika:**\n\n```typescript\n// Stwórz sesję przez API, zapisz credentials do keychain\n\ntest.beforeEach(async () => {\n  // Stwórz użytkownika\n  const user = await createTestUser();\n  \n  // Zaloguj się w aplikacji\n  await driver.launchApp();\n  await driver.$('~login-email').setValue(user.email);\n  await driver.$('~login-password').setValue(user.password);\n  await driver.$('~login-submit').click();\n  \n  // Poczekaj na home screen\n  await driver.waitFor('~home-screen', { state: 'visible', timeout: 10_000 });\n  \n  // Zapisz user ID dla cleanup\n  test.info().annotations.push({ type: 'user-id', description: user.id });\n});\n```\n\n---\n\n### 5. Sieć (stan początkowy łączności)\n\n```typescript\n// Playwright — kontrola sieci\n\n// Scenariusz A: Online (domyślnie)\nconst onlineContext = await browser.newContext(); // bez setOffline\n\n// Scenariusz B: Offline przed startem\nconst offlineContext = await browser.newContext();\nawait offlineContext.setOffline(true);\n\n// Scenariusz C: Throttled network (symulacja 3G)\nconst client = await page.context().newCDPSession(page);\nawait client.send('Network.emulateNetworkConditions', {\n  offline: false,\n  downloadThroughput: 100 * 1024 / 8, // 100 KB/s\n  uploadThroughput: 50 * 1024 / 8,\n  latency: 400, // 400ms RTT\n});\n\n// Appium — kontrola sieci Android\nawait driver.execute('mobile: setConnectionType', {\n  type: 'airplane', // 'wifi', 'data', 'airplane', 'none'\n});\n\n// Przywrócenie online\nawait driver.execute('mobile: setConnectionType', {\n  type: 'wifi',\n});\n```\n\n---\n\n### Checklist stanu początkowego\n\n| Element | Pytanie kontrolne | Akcja |\n|---|---|---|\n| **Instalacja** | Czy app jest zainstalowana i w poprawnej wersji? | `adb install` / `driver.installApp()` |\n| **Dane lokalne** | Czy dane z poprzedniego testu zostały usunięte? | `pm clear` / `localStorage.clear()` |\n| **Uprawnienia** | Czy uprawnienia są ustawione świadomie? | `grantPermissions` / `permissions: []` |\n| **Sesja** | Czy użytkownik jest zalogowany/wylogowany zgodnie z planem? | API setup + cookie/token |\n| **Sieć** | Czy sieć jest online/offline zgodnie z planem? | `setOffline()` / `setConnectionType` |\n| **Wersja OS** | Czy wersja OS jest zgodna z matrix? | Sprawdź `adb getprop` / `xcrun simctl list` |\n| **Orientacja** | Czy urządzenie jest w portrait czy landscape? | `setOrientation` / viewport resize |"
      },
      {
        "id": "ex-26-1-3",
        "title": "Scenariusz negatywny",
        "description": "Dodaj wariant odmowy uprawnienia, utraty sieci, wygasłej sesji lub błędnego deep linku.",
        "solution": "## Scenariusze negatywne — testowanie stanów wyjątkowych\n\n### 1. Odmowa uprawnień\n\n```typescript\n// Playwright — testowanie odmowy geolokalizacji\n\ntest('użytkownik odmawia geolokalizacji — app graceful degrades', async ({ page, context }) => {\n  // Krok 1: Start bez uprawnień geolokalizacji\n  const noGeoContext = await browser.newContext({\n    permissions: [], // Brak geolokalizacji\n  });\n  const noGeoPage = await noGeoContext.newPage();\n  \n  // Krok 2: Przejdź do funkcji wymagającej lokalizacji\n  await noGeoPage.goto('/pickup-points');\n  \n  // Krok 3: Aplikacja może pokazać:\n  // A) Prompt systemowy z prośbą o uprawnienie\n  // B) Od razu pole do ręcznego wprowadzenia adresu\n  // C) Informację o braku uprawnień\n  \n  const manualInput = noGeoPage.getByLabel('Wpisz adres lub miasto');\n  const geoPrompt = noGeoPage.getByRole('dialog').or(noGeoPage.getByText('Włącz lokalizację'));\n  \n  // Jeśli pokazuje prompt — kliknij \"Odmów\" lub \"Wpisz ręcznie\"\n  if (await geoPrompt.isVisible({ timeout: 2_000 }).catch(() => false)) {\n    await noGeoPage.getByRole('button', { name: 'Wpisz adres ręcznie' }).click();\n  }\n  \n  // Krok 4: Asercja — pole do ręcznego wprowadzenia jest widoczne\n  await expect(manualInput).toBeVisible();\n  \n  // Krok 5: Wprowadź adres ręcznie — funkcja działa\n  await manualInput.fill('ul. Marszałkowska 1, Warszawa');\n  await noGeoPage.getByRole('button', { name: 'Szukaj' }).click();\n  \n  // Krok 6: Wyniki są widoczne (symulacja — w realnym teście może być empty)\n  const resultsHeader = noGeoPage.getByRole('heading', { name: /wyniki/i });\n  await expect(resultsHeader.or(noGeoPage.getByText('Brak punktów w tej lokalizacji'))).toBeVisible();\n  \n  await noGeoContext.close();\n});\n```\n\n**Appium — odmowa kamery:**\n\n```typescript\ntest('Android: odmowa uprawnień kamery powoduje właściwy komunikat', async () => {\n  const driver = await remote({ /* config */ });\n  \n  // Krok 1: Upewnij się, że app NIE ma uprawnień kamery\n  await driver.execute('mobile: revokePermissions', {\n    pkg: 'com.example.shop',\n    permissions: ['android.permission.CAMERA'],\n  });\n  \n  // Krok 2: Uruchom app\n  await driver.launchApp();\n  \n  // Krok 3: Przejdź do funkcji wymagającej kamery (skanowanie produktu)\n  await driver.$('~scan-button').click();\n  \n  // Krok 4: Sprawdź, czy app pokazuje właściwy komunikat\n  const permissionMessage = await driver.$('~camera-permission-required');\n  await expect(permissionMessage).toBeVisible();\n  \n  // Krok 5: Komunikat powinien zawierać:\n  // - Informację o brakującym uprawnieniu\n  // - Przycisk do otwarcia ustawień\n  const messageText = await permissionMessage.getText();\n  expect(messageText).toMatch(/kamera|dostęp|uprawnienie/i);\n  \n  const openSettingsButton = await driver.$('~open-app-settings');\n  await expect(openSettingsButton).toBeVisible();\n  \n  // Krok 6: Alternatywna ścieżka powinna być dostępna\n  const manualInputButton = await driver.$('~enter-code-manually');\n  await expect(manualInputButton).toBeVisible();\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 2. Utrata sieci podczas akcji\n\n```typescript\n// Playwright — utrata sieci podczas checkoutu\n\ntest('użytkownik traci sieć podczas składania zamówienia — dane są chronione', async ({ page, context }) => {\n  // Krok 1: Przejdź do checkoutu\n  await page.goto('/checkout');\n  await page.getByLabel('Numer karty').fill('4111111111111111');\n  await page.getByLabel('CVC').fill('123');\n  await page.getByLabel('Data ważności').fill('1226');\n  \n  // Krok 2: Symuluj utratę sieci W TRAKCIE akcji\n  await context.setOffline(true);\n  \n  // Krok 3: Spróbuj wysłać formularz\n  const submitButton = page.getByRole('button', { name: 'Zapłać' });\n  await submitButton.click();\n  \n  // Krok 4: Oczekuj właściwej reakcji:\n  // A) Błąd \"Brak połączenia\" — nie crash\n  // B) formularz jest wypełniony i czeka na powrót sieci\n  // C) Dane są zapisane lokalnie\n  \n  const offlineMessage = page.getByRole('alert').or(page.getByText('Brak połączenia'));\n  const savedLocally = page.getByText('Zapisano lokalnie');\n  \n  const hasOfflineMessage = await offlineMessage.isVisible({ timeout: 3_000 }).catch(() => false);\n  const hasSavedLocally = await savedLocally.isVisible({ timeout: 3_000 }).catch(() => false);\n  \n  expect(hasOfflineMessage || hasSavedLocally).toBe(true);\n  \n  // Krok 5: Formularz NIE powinien być wyczyszczony\n  const cardNumber = await page.getByLabel('Numer karty').inputValue();\n  expect(cardNumber).toBe('4111111111111111'); // Dane są nadal w formularzu\n  \n  // Krok 6: Przywróć sieć\n  await context.setOffline(false);\n  \n  // Krok 7: Spróbuj wysłać ponownie\n  await submitButton.click();\n  \n  // Krok 8: Zamówienie powinno być złożone (lub przynajmniej nie stracone)\n  const successMessage = page.getByText('Zamówienie zostało złożone').or(page.getByRole('heading', { name: /potwierdzenie/i }));\n  await expect(successMessage.or(page.getByRole('alert'))).toBeVisible({ timeout: 15_000 });\n  \n  // Krok 9: Weryfikacja na backendzie\n  const response = await page.request.get('/api/orders');\n  const orders = await response.json();\n  // Zamówienie powinno istnieć (nie być utracone)\n  expect(orders.length).toBeGreaterThan(0);\n});\n```\n\n---\n\n### 3. Wygasła sesja\n\n```typescript\n// Playwright — wygasła sesja podczas transakcji\n\ntest('wygasła sesja powoduje redirect do logowania z zachowaniem kontekstu', async ({ page, context }) => {\n  // Krok 1: Ustaw wygasłą sesję\n  await context.addInitScript(() => {\n    // Symuluj wygasły token (ustaw cookie z przeszłą datą)\n    document.cookie = `session_token=expired_token_123; path=/; max-age=0`;\n  });\n  \n  // Krok 2: Spróbuj uzyskać dostęp do chronionej strony\n  await page.goto('/orders');\n  \n  // Krok 3: Aplikacja powinna:\n  // A) Przekierować do logowania\n  // B) Zachować pierwotny URL jako redirect target\n  \n  await expect(page).toHaveURL(/\\/login/, { timeout: 5_000 });\n  \n  // Krok 4: URL powinien zawierać informację o pierwotnym destination\n  const currentUrl = page.url();\n  const hasRedirectParam = currentUrl.includes('redirect=') || currentUrl.includes('returnUrl=');\n  \n  if (hasRedirectParam) {\n    // Dobrze — aplikacja zachowuje kontekst\n    expect(currentUrl).toMatch(/redirect=.*orders/);\n  }\n  \n  // Krok 5: Strona logowania powinna to wskazywać\n  const loginHeading = page.getByRole('heading', { name: /logowanie/i });\n  await expect(loginHeading).toBeVisible();\n  \n  // Krok 6: Zaloguj się\n  await page.getByLabel('Adres e-mail').fill('jan@example.test');\n  await page.getByLabel('Hasło').fill('SecurePass123!');\n  await page.getByRole('button', { name: 'Zaloguj się' }).click();\n  \n  // Krok 7: Po zalogowaniu powinno być przekierowanie z powrotem\n  await page.waitForURL(/\\/orders/, { timeout: 10_000 });\n  await expect(page.getByRole('heading', { name: /zamówienia/i })).toBeVisible();\n});\n```\n\n---\n\n### 4. Błędny deep link\n\n```typescript\n// Appium — deep link do nieistniejącego zasobu\n\ntest('deep link do nieistniejącego zamówienia — właściwa obsługa błędu', async () => {\n  const driver = await remote({ /* config */ });\n  \n  // Krok 1: Otwórz deep link z nieprawidłowym ID\n  await driver.execute('mobile: deepLink', {\n    url: 'myapp://orders/invalid-id-xyz-999',\n    package: 'com.example.shop',\n  });\n  \n  // Krok 2: Aplikacja powinna obsłużyć błąd gracifully\n  // Opcja A: Ekran 404 z przyciskiem powrotu do listy\n  // Opcja B: Przekierowanie na home/list\n  // Opcja C: Dialog z informacją o błędzie\n  \n  const errorScreen = await driver.$('~error-screen');\n  const notFoundText = await driver.$('~order-not-found');\n  const homeButton = await driver.$('~go-to-home');\n  \n  const hasErrorScreen = await errorScreen.isDisplayed({ timeout: 5_000 }).catch(() => false);\n  const hasNotFound = await notFoundText.isDisplayed({ timeout: 5_000 }).catch(() => false);\n  \n  // Przynajmniej jedna z opcji powinna być widoczna\n  expect(hasErrorScreen || hasNotFound).toBe(true);\n  \n  // Krok 3: Nie powinno być crashu (ANR)\n  const anrDialog = await driver.$('android=new UiSelector().text(\"Aplikacja nie odpowiada\")');\n  const hasANR = await anrDialog.isDisplayed({ timeout: 2_000 }).catch(() => false);\n  expect(hasANR).toBe(false); // Brak crashu\n  \n  // Krok 4: Nawigacja powinna być możliwa\n  const homeButtonVisible = await homeButton.isDisplayed().catch(() => false);\n  if (homeButtonVisible) {\n    await homeButton.click();\n    await driver.waitFor('~home-screen', { state: 'visible', timeout: 5_000 });\n    await expect(driver.$('~home-screen')).toBeDisplayed();\n  }\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 5. Kombinacja scenariuszy\n\n```typescript\n// Scenariusz złożony: wygasła sesja + utrata sieci + odmowa uprawnień\n\ntest('użytkownik w trybie offline z wygasłą sesją próbuje użyć funkcji wymagającej uprawnień', async ({ page, context }) => {\n  // Krok 1: Wygasła sesja\n  await context.addInitScript(() => {\n    document.cookie = `session_token=expired; path=/; max-age=0`;\n  });\n  \n  // Krok 2: Brak uprawnień\n  const noPermsContext = await browser.newContext({ permissions: [] });\n  \n  // Krok 3: Tryb offline\n  await noPermsContext.setOffline(true);\n  \n  const combinedPage = await noPermsContext.newPage();\n  \n  // Krok 4: Próba dostępu do funkcji wymagającej wszystkich trzech\n  await combinedPage.goto('/profile/edit');\n  \n  // Krok 5: Aplikacja powinna pokazać:\n  // A) Informację o wygasłej sesji (gdy online dostępne)\n  // B) Informację o trybie offline (gdy próbuje sync)\n  // C) Alternatywną ścieżkę (edytuj lokalnie)\n  \n  const offlineIndicator = combinedPage.getByText('Tryb offline');\n  const savedLocallyOption = combinedPage.getByText('Zapisz lokalnie');\n  \n  const hasOfflineOrLocal = await offlineIndicator.isVisible({ timeout: 3_000 }).catch(() => false) ||\n                             await savedLocallyOption.isVisible({ timeout: 3_000 }).catch(() => false);\n  \n  expect(hasOfflineOrLocal).toBe(true);\n  \n  // Krok 6: Aplikacja nie powinna crashować\n  const pageContent = await combinedPage.content();\n  expect(pageContent).not.toContain('Internal Server Error');\n  expect(pageContent).not.toContain('Something went wrong');\n  \n  await noPermsContext.close();\n});\n```\n\n---\n\n### Checklist scenariuszy negatywnych\n\n| Scenariusz | Co sprawdzasz | Asercja |\n|---|---|---|\n| **Odmowa uprawnień** | App nie crashuje, pokazuje alternative | `isVisible()` alternatywy |\n| **Utrata sieci** | Formularz nie traci danych, pokazuje offline state | Dane nadal w polach |\n| **Wygasła sesja** | Redirect do logowania z zachowaniem URL | `toHaveURL(/login/)` + redirect param |\n| **Błędny deep link** | Brak crashu, ekran błędu lub redirect | Brak ANR, jest error/404 |\n| **Kombinacja** | Aplikacja prioryetyzuje stany i nie crashuje | Żaden z powyższych nie powoduje ANR |"
      },
      {
        "id": "ex-26-1-4",
        "title": "Diagnostyka mobilna",
        "description": "Wskaż artefakty potrzebne przy awarii: screenshot, wideo, logcat/syslog, device info i network logs.",
        "solution": "## Diagnostyka mobilna — kompletny zestaw artefaktów przy awarii\n\n### 1. Screenshot (automatyczny)\n\n**Playwright:**\n\n```typescript\n// playwright.config.ts — screenshot on failure (domyślnie włączony)\nexport default defineConfig({\n  use: {\n    screenshot: 'only-on-failure', // lub 'on' dla zawsze\n  },\n});\n\n// Lokalizacja: test-results/screenshots/\n// Nazwa: {test-title}-{timestamp}.png\n```\n\n**Appium — screenshot ręczny:**\n\n```typescript\n// W teście\nconst screenshot = await driver.takeScreenshot();\n// Zapisz do pliku\nimport fs from 'fs';\nfs.writeFileSync(`test-results/screenshot-${Date.now()}.png`, screenshot, 'base64');\n\n// Automatycznie przy failure (w afterEach)\ntest.afterEach(async ({ }, testInfo) => {\n  if (testInfo.status === 'failed') {\n    const screenshot = await driver.takeScreenshot();\n    fs.writeFileSync(`test-results/${testInfo.title}-failure.png`, screenshot, 'base64');\n  }\n});\n```\n\n---\n\n### 2. Wideo (nagranie testu)\n\n**Playwright:**\n\n```typescript\n// playwright.config.ts — video on failure\nexport default defineConfig({\n  use: {\n    video: 'retain-on-failure', // lub 'on' dla zawsze\n  },\n});\n\n// W teście — pobierz video URL\ntest.afterEach(async ({ }, testInfo) => {\n  if (testInfo.status === 'failed') {\n    const videoPath = testInfo.attachments\n      .find(a => a.name === 'video')\n      ?.path;\n    console.log('Video:', videoPath);\n  }\n});\n```\n\n**Appium — wideo:**\n\n```typescript\n// W capabilities\nconst videoCaps = {\n  'appium:enableVideoRecording': true,\n  'appium:videoQuality': 'HIGH', // 'LOW', 'MEDIUM', 'HIGH'\n  'appium:videoType': 'mp4',\n};\n\n// Lub w trakcie sesji\nawait driver.startRecordingScreen();\n// ... test execution ...\nconst video = await driver.stopRecordingScreen();\nfs.writeFileSync(`test-results/video-${Date.now()}.mp4`, video, 'base64');\n```\n\n---\n\n### 3. Logcat (Android) / syslog (iOS)\n\n**Android — logcat:**\n\n```bash\n# Pobierz logcat z ostatnich X lines\nadb logcat -d -t 500 > test-results/logcat-$(date +%Y%m%d-%H%M%S).log\n\n# Filtruj tylko error/fatal\nadb logcat -d *:E > test-results/logcat-errors-only.log\n\n# Filtruj tylko app logs\nadb logcat -d -s com.example.shop:V > test-results/logcat-app-only.log\n\n# Logcat z konkretnego momentu (od crasha)\nadb logcat -d | grep -A 50 \"FATAL EXCEPTION\"\n```\n\n```typescript\n// Appium — pobierz logcat w teście\ntest.afterEach(async () => {\n  if (testInfo.status === 'failed') {\n    const logcat = await driver.execute('mobile: logCat', {\n      filters: ['FATAL', 'Exception', 'ANR'],\n    });\n    fs.writeFileSync(`test-results/logcat-${Date.now()}.txt`, logcat);\n  }\n});\n\n// Alternatywnie: adb exec-out\nconst { execSync } = require('child_process');\nexecSync('adb logcat -d -t 1000 > test-results/logcat.txt');\n```\n\n**iOS — system log:**\n\n```bash\n# Pobierz syslog\nlog show --predicate 'process == \"com.example.shop\"' --last 10m > test-results/ios-syslog.txt\n\n# Lub przez Xcode\n# Instruments > System Log > Export\n```\n\n```typescript\n// Appium iOS\nconst systemLog = await driver.execute('mobile: queryNotificationEvents', {});\nfs.writeFileSync('test-results/ios-syslog.txt', systemLog);\n```\n\n---\n\n### 4. Device info (informacje o urządzeniu)\n\n**Android — pełny dump:**\n\n```bash\n# Model i wersja\nadb shell getprop ro.product.model\nadb shell getprop ro.build.version.release\nadb shell getprop ro.build.version.security_patch\n\n# Pamięć\nadb shell dumpsys meminfo com.example.shop\n\n# Bateria\nadb shell dumpsys battery\n\n# Sieć\nadb shell ifconfig\nadb shell ping -c 3 google.com\n\n# Gyrozakres/accelerometer\nadb shell sensors\n\n# Zainstalowane pakiety\nadb shell pm list packages\n```\n\n```typescript\n// Appium — device info\nconst deviceInfo = await driver.execute('mobile: deviceInfo');\nconsole.log({\n  model: deviceInfo.model,\n  brand: deviceInfo.brand,\n  osVersion: deviceInfo.osVersion,\n  screenWidth: deviceInfo.screenWidth,\n  screenHeight: deviceInfo.screenHeight,\n  freeMemory: deviceInfo.availMem,\n  totalMemory: deviceInfo.totalMem,\n});\n```\n\n**iOS — pełny dump:**\n\n```bash\n# Model\nxcrun simctl list devices | grep -E \"iPhone|iPad\"\n\n# Wersja iOS\nxcrun simctl list devices available | grep \"iOS\"\n\n# Crash reports\nls ~/Library/Logs/DiagnosticReports/\n\n# System log\nlog show --predicate 'process == \"com.example.shop\"' --last 5m\n```\n\n---\n\n### 5. Network logs (HAR file)\n\n**Playwright — HAR capture:**\n\n```typescript\n// Intercept all requests\nconst { chromium } = require('playwright');\n\ntest('capture network logs on failure', async ({ page }) => {\n  const harEntries = [];\n  \n  page.on('request', request => {\n    harEntries.push({\n      url: request.url(),\n      method: request.method(),\n      headers: request.headers(),\n      postData: request.postData(),\n      timestamp: Date.now(),\n    });\n  });\n  \n  page.on('response', response => {\n    const existing = harEntries.find(e => e.url === response.url());\n    if (existing) {\n      existing.status = response.status();\n      existing.statusText = response.statusText();\n    }\n  });\n  \n  // ... test execution ...\n  \n  test.afterEach(async ({ }, testInfo) => {\n    if (testInfo.status === 'failed') {\n      const har = {\n        log: {\n          version: '1.2',\n          creator: { name: 'Playwright', version: '1.x' },\n          entries: harEntries,\n        },\n      };\n      fs.writeFileSync(\n        `test-results/network-${testInfo.title}-${Date.now()}.har`,\n        JSON.stringify(har, null, 2)\n      );\n    }\n  });\n});\n```\n\n**Appium — network traffic:**\n\n```typescript\n// Android — network logs przez Chrome DevTools\nconst cdpSession = await driver.createCDPSession('webview');\nawait cdpSession.send('Network.enable');\n\ncdpSession.on('Network.requestWillBeSent', params => {\n  // Log request\n});\ncdpSession.on('Network.responseReceived', params => {\n  // Log response\n});\n\n// Lub przez Appium\nawait driver.execute('mobile: startLogsBroadcast');\n```\n\n---\n\n### 6. Accessibility tree dump (Android/iOS)\n\n```bash\n# Android — UIAutomator dump\nadb shell uiautomator dump /sdcard/ui-dump.xml\nadb pull /sdcard/ui-dump.xml test-results/ui-dump-$(date +%s).xml\n```\n\n```typescript\n// Appium — accessibility tree\nconst pageSource = await driver.getPageSource();\nfs.writeFileSync(`test-results/page-source-${Date.now()}.xml`, pageSource);\n\n// Format XML — łatwy do parsowania\n// Zawiera: @resource-id, @text, @content-desc, @enabled, @focused\n```\n\n---\n\n### 7. Crash dump (Android/iOS)\n\n```bash\n# Android — ANR traces\nadb pull /data/anr/traces.txt test-results/anr-traces.txt 2>/dev/null || echo \"No ANR traces\"\n\n# Android — tombstone (crash)\nadb pull /data/tombstones/ test-results/tombstones/ 2>/dev/null || echo \"No tombstones\"\n\n# iOS — crash logs\nls ~/Library/Logs/DiagnosticReports/ | grep com.example.shop\ncp ~/Library/Logs/DiagnosticReports/com.example.shop*.crash test-results/\n```\n\n---\n\n### 8. Automatyczny collection w CI/CD\n\n```typescript\n// utils/collect-artefakty.ts — funkcja pomocnicza\n\nimport fs from 'fs';\nimport path from 'path';\nimport { execSync } from 'child_process';\n\nexport async function collectArtifacts(testInfo: TestInfo, driver: any) {\n  const artefaktyDir = path.join('test-results', 'artefakty', testInfo.title);\n  fs.mkdirSync(artefaktyDir, { recursive: true });\n  \n  const timestamp = new Date().toISOString().replace(/:/g, '-');\n  \n  // 1. Screenshot\n  try {\n    const screenshot = await driver.takeScreenshot();\n    fs.writeFileSync(path.join(artefaktyDir, `screenshot-${timestamp}.png`), screenshot, 'base64');\n  } catch (e) { console.error('Screenshot failed:', e); }\n  \n  // 2. Video (jeśli nagrywanie było włączone)\n  try {\n    const video = await driver.stopRecordingScreen();\n    fs.writeFileSync(path.join(artefaktyDir, `video-${timestamp}.mp4`), video, 'base64');\n  } catch (e) { console.error('Video failed:', e); }\n  \n  // 3. Logcat (Android)\n  try {\n    execSync(`adb logcat -d -t 2000 > \"${path.join(artefaktyDir, `logcat-${timestamp}.txt`)}\"`);\n  } catch (e) { console.error('Logcat failed:', e); }\n  \n  // 4. Page source (accessibility tree)\n  try {\n    const source = await driver.getPageSource();\n    fs.writeFileSync(path.join(artefaktyDir, `page-source-${timestamp}.xml`), source);\n  } catch (e) { console.error('Page source failed:', e); }\n  \n  // 5. Device info\n  try {\n    const deviceInfo = await driver.execute('mobile: deviceInfo');\n    fs.writeFileSync(\n      path.join(artefaktyDir, `device-info-${timestamp}.json`),\n      JSON.stringify(deviceInfo, null, 2)\n    );\n  } catch (e) { console.error('Device info failed:', e); }\n  \n  // 6. Network HAR\n  // (jeśli intercept był włączony — zapisz wcześniej zebrane dane)\n  \n  console.log(`Artifacts collected: ${artefaktyDir}`);\n}\n```\n\n---\n\n### Przykładowy raport awarii\n\n```markdown\n# Bug Report: ANR na Samsung Galaxy A54 przy ładowaniu listy produktów\n\n## Artefakty\n| Typ | Lokalizacja | Opis |\n|---|---|---|\n| Screenshot | `test-results/artefakty/.../screenshot-2024-06-23T10-15-30.png` | ANR dialog |\n| Wideo | `test-results/artefakty/.../video-2024-06-23T10-15-30.mp4` | 12s przed crashem |\n| Logcat | `test-results/artefakty/.../logcat-2024-06-23T10-15-30.txt` | Pełny log z ANR trace |\n| Device info | `test-results/artefakty/.../device-info-2024-06-23T10-15-30.json` | Model, OS, pamięć |\n| Page source | `test-results/artefakty/.../page-source-2024-06-23T10-15-30.xml` | Accessibility tree |\n| HAR | `test-results/artefakty/.../network-2024-06-23T10-15-30.har` | 12 requestów, ostatni timeout |\n| ANR trace | `test-results/artefakty/.../anr-traces.txt` | Thread dump z deadlocka |\n\n## Device Info (z JSON)\n```json\n{\n  \"model\": \"Samsung Galaxy A54\",\n  \"osVersion\": \"Android 13\",\n  \"securityPatch\": \"2024-05-01\",\n  \"availMem\": \"420 MB\",\n  \"totalMem\": \"6 GB\",\n  \"screenResolution\": \"1080x2340\"\n}\n```\n\n## Analiza logcat\n```\n05-23 10:15:25.123  1234  5678 E ANR : Application is not responding...\n05-23 10:15:25.456  1234  5678 E ANR : Reason: main thread blocked for 5003ms\n05-23 10:15:25.789  1234  5678 E ANR : Load: 8.2 / 8.5 (memory pressure)\n...\n```\n```\n\n---\n\n### Checklist artefaktów\n\n| Artefakt | Kiedy | Narzędzie |\n|---|---|---|\n| **Screenshot** | Zawsze przy failure | Playwright auto / Appium `takeScreenshot()` |\n| **Wideo** | Zawsze przy failure (flaky tests) | Playwright auto / Appium recording |\n| **Logcat/syslog** | Tylko Android/iOS native | `adb logcat` / `log show` |\n| **Device info** | Zawsze przy failure | Appium `deviceInfo` / `adb getprop` |\n| **Network HAR** | Przy network-related failures | Playwright intercept / Appium CDP |\n| **Page source** | Przy UI-related failures | Appium `getPageSource()` |\n| **Crash dump** | Tylko przy crashu/ANR | `adb pull /data/anr/` |\n| **Memory dump** | Przy OOM issues | `adb shell dumpsys meminfo` |"
      },
      {
        "id": "ex-26-1-5",
        "title": "CI mobile",
        "description": "Zaprojektuj, które testy mobilne uruchamiać w PR, nocą i przed wydaniem.",
        "solution": "## CI/CD pipeline dla testów mobilnych — trójwarstwowa strategia\n\n### Architektura pipeline\n\n```yaml\n# .github/workflows/mobile-tests.yml\nname: Mobile Tests Pipeline\n\non:\n  push:\n    branches: [main, develop]\n  pull_request:\n  schedule:\n    - cron: '0 2 * * *'  # Nightly: 2:00 AM\n  workflow_dispatch:       # Manual trigger\n\nenv:\n  APPIUM_HOST: localhost\n\njobs:\n  # ==========================================\n  # WARSTWA 1: PR — szybki feedback < 10 min\n  # ==========================================\n  mobile-web-smoke:\n    runs-on: ubuntu-latest\n    timeout-minutes: 10\n    steps:\n      - uses: actions/checkout@v4\n      \n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: '22'\n          cache: 'npm'\n      \n      - name: Install dependencies\n        run: npm ci\n      \n      - name: Install Playwright browsers\n        run: npx playwright install --with-deps chromium\n      \n      - name: Run mobile web smoke (emulators)\n        run: npx playwright test tests/mobile/smoke.spec.ts\n        env:\n          BASE_URL: ${{ vars.STAGING_URL }}\n          PROJECT_ID: ${{ vars.ANALYTICS_PROPERTY }}\n      \n      - name: Upload results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: playwright-report-pr\n          path: playwright-report/\n      \n      - name: Publish report\n        uses: actions/upload-artifact@v4\n        with:\n          name: test-results-pr\n          path: test-results/\n\n  app-emulator-smoke:\n    runs-on: ubuntu-latest\n    timeout-minutes: 15\n    services:\n      appium:\n        image: appium/appium:latest\n        ports:\n          - 4723:4723\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      - name: Setup Android emulator\n        run: |\n          echo \"y\" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install \"platform-tools\"\n          echo \"y\" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install \"platforms;android-34\"\n          $ANDROID_HOME/emulator/emulator -avd Pixel_7_API_34 -no-window -no-audio &\n          adb wait-for-device\n      - name: Run Appium smoke on emulator\n        run: npm run test:app:smoke -- --device=pixel7-emulator\n        env:\n          APPIUM_HOST: appium\n      - name: Upload artifacts\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: appium-smoke-results\n          path: test-results/\n\n  # ==========================================\n  # WARSTWA 2: NIGHTLY — regular coverage ~30 min\n  # ==========================================\n  nightly-android-browserstack:\n    runs-on: ubuntu-latest\n    timeout-minutes: 45\n    if: github.event_name == 'schedule'\n    strategy:\n      matrix:\n        device: ['pixel7-android14', 'galaxy-a54-android13', 'redmi-note12-android13']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      - name: Run on BrowserStack\n        run: |\n          npx wdio wdio.bs.conf.ts --device=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n      - name: Upload results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: bs-nightly-${{ matrix.device }}\n          path: test-results/\n      - name: Upload report\n        uses: actions/upload-artifact@v4\n        with:\n          name: bs-report-${{ matrix.device }}\n          path: playwright-report/\n\n  nightly-ios-browserstack:\n    runs-on: ubuntu-latest\n    timeout-minutes: 40\n    if: github.event_name == 'schedule'\n    strategy:\n      matrix:\n        device: ['iphone14-ios17', 'iphone12-ios16']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      - name: Run iOS on BrowserStack\n        run: |\n          npx wdio wdio.bs.conf.ts --device=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n      - name: Upload results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: bs-nightly-ios-${{ matrix.device }}\n          path: test-results/\n\n  nightly-summary:\n    needs: [nightly-android-browserstack, nightly-ios-browserstack]\n    runs-on: ubuntu-latest\n    if: always()\n    steps:\n      - name: Download all results\n        uses: actions/download-artifact@v4\n        with:\n          pattern: 'bs-nightly-*'\n          path: test-results/\n      - name: Generate summary\n        run: |\n          echo \"## Nightly Test Summary\" >> $GITHUB_STEP_SUMMARY\n          echo \"- Android: ${{ needs.nightly-android-browserstack.result }}\" >> $GITHUB_STEP_SUMMARY\n          echo \"- iOS: ${{ needs.nightly-ios-browserstack.result }}\" >> $GITHUB_STEP_SUMMARY\n\n  # ==========================================\n  # WARSTWA 3: RELEASE — full coverage ~90 min\n  # ==========================================\n  release-device-matrix:\n    needs: [nightly-summary]\n    runs-on: ubuntu-latest\n    timeout-minutes: 120\n    if: |\n      github.event_name == 'push' && \n      github.ref == 'refs/heads/main' &&\n      startsWith(github.event.head_commit.message, 'release') || \n      github.event_name == 'workflow_dispatch'\n    strategy:\n      matrix:\n        include:\n          - device: 'pixel7-android14'\n            platform: 'android'\n          - device: 'galaxy-s23-android14'\n            platform: 'android'\n          - device: 'xiaomi-redmi12-android12'\n            platform: 'android'\n          - device: 'iphone14-ios17'\n            platform: 'ios'\n          - device: 'iphone11-ios15'\n            platform: 'ios'\n          - device: 'ipad-air-ipados17'\n            platform: 'ios'\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      - name: Run full test suite on device\n        run: |\n          npx wdio wdio.bs.conf.ts --device=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n          IS_RELEASE: 'true'\n      - name: Upload all artifacts\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: release-${{ matrix.platform }}-${{ matrix.device }}\n          path: |\n            test-results/\n            playwright-report/\n            screenshots/\n\n  release-gate:\n    needs: [release-device-matrix]\n    runs-on: ubuntu-latest\n    if: always()\n    steps:\n      - name: Check all device results\n        run: |\n          echo \"## Release Gate Results\"\n          # Parse all test results and fail if any critical tests failed\n          FAILED=$(find test-results -name '*.xml' -exec grep -l 'failures=\"[^0]' {} \\; | wc -l)\n          if [ $FAILED -gt 0 ]; then\n            echo \"::error::Critical tests failed on $FAILED devices\"\n            exit 1\n          fi\n          echo \"✓ All devices passed release criteria\"\n```\n\n---\n\n### Konfiguracja playwright.config.ts dla CI\n\n```typescript\n// playwright.config.ts — CI-optimized configuration\nimport { defineConfig, devices } from '@playwright/test';\n\nconst isCI = process.env['CI'] === 'true';\nconst isPR = process.env['GITHUB_EVENT_NAME'] === 'pull_request';\nconst isNightly = process.env['GITHUB_EVENT_NAME'] === 'schedule';\nconst isRelease = process.env['IS_RELEASE'] === 'true';\n\nexport default defineConfig({\n  testDir: './tests/mobile',\n  timeout: isCI ? 60_000 : 30_000, // Dłuższe na CI (wolniejsze maszyny)\n  expect: { timeout: isCI ? 15_000 : 5_000 },\n  retries: isPR ? 2 : isNightly ? 1 : 0, // Retry na PR, mniej na release\n  workers: isCI ? '50%' : undefined, // Ogranicz na CI (resource limits)\n  \n  reporter: [\n    ['html', { outputFolder: 'playwright-report' }],\n    ['junit', { outputFile: 'test-results/results.xml' }],\n    isCI ? ['github'] : [], // GitHub annotations\n  ],\n  \n  use: {\n    screenshot: 'only-on-failure',\n    video: 'retain-on-failure',\n    trace: isPR ? 'on-first-retry' : 'on-all-retries',\n    baseURL: process.env['BASE_URL'],\n  },\n  \n  projects: [\n    // === PR: Szybki feedback (emulatory)\n    ...(isPR || isCI ? [\n      { name: 'emulator-android', use: { ...devices['Pixel 7'] } },\n      { name: 'simulator-ios', use: { ...devices['iPhone 14'] } },\n    ] : []),\n    \n    // === NIGHTLY: Regular coverage (BrowserStack real devices)\n    ...(isNightly ? [\n      { name: 'pixel7-real', use: { browserName: 'chromium' } },\n      { name: 'galaxy-a54-real', use: { browserName: 'chromium' } },\n      { name: 'iphone14-real', use: { browserName: 'webkit' } },\n    ] : []),\n    \n    // === RELEASE: Full matrix\n    ...(isRelease ? [\n      { name: 'pixel7-real', use: { browserName: 'chromium' } },\n      { name: 'galaxy-s23-real', use: { browserName: 'chromium' } },\n      { name: 'redmi12-real', use: { browserName: 'chromium' } },\n      { name: 'iphone14-real', use: { browserName: 'webkit' } },\n      { name: 'iphone11-real', use: { browserName: 'webkit' } },\n      { name: 'ipad-air-real', use: { ...devices['iPad (gen 7)'] } },\n    ] : []),\n  ],\n});\n```\n\n---\n\n### Zasady organizacji testów w CI\n\n| Zasada | Implementacja | Dlaczego |\n|---|---|---|\n| **Fail-fast na PR** | Krytyczny test pada → pipeline fail | Szybki feedback dla developera |\n| **Retry flaky** | Retry 1-2x na PR | Redukcja false negatives |\n| **Sharding** | 4 shard workers na noc | Skrócenie czasu nocnego |\n| **Artifacts only on failure** | Screenshot/video przy failure | Oszczędność storage |\n| **Parallel execution** | Matrix strategy parallel | Pełna macierz w jednym run |\n| **Selective runs** | Git diff detection | Uruchom tylko affected tests |\n| **Resource limits** | Workers = 50% CPU | Nie przeciążaj maszyny CI |\n\n---\n\n### Przykładowy trigger na commit (fast feedback)\n\n```yaml\n# Uruchom tylko przy zmianach w katalogu mobile/\non:\n  push:\n    paths:\n      - 'src/mobile/**'\n      - 'tests/mobile/**'\n      - 'playwright.config.ts'\n      - 'appium.config.ts'\n```"
      },
      {
        "id": "ex-26-1-6",
        "title": "Ryzyko UX",
        "description": "Opisz, jak przetestujesz gesty, klawiaturę ekranową, orientację i przerwanie aplikacji.",
        "solution": "## Testowanie UX mobilnego — gesty, klawiatura, orientacja, przerwania\n\n### 1. Gesty dotykowe\n\n**Playwright — touch emulation:**\n\n```typescript\n// Test swipe na liście produktów\ntest('użytkownik może przewijać listę produktów gestem swipe', async ({ page }) => {\n  await page.setViewportSize({ width: 390, height: 844 });\n  await page.goto('/products');\n  \n  // Pobierz wymiary listy\n  const productList = page.locator('.product-list');\n  const listBox = await productList.boundingBox();\n  \n  // Swipe up (przewiń w górę = nowe produkty)\n  const startX = listBox!.x + listBox!.width / 2;\n  const startY = listBox!.y + listBox!.height - 50;\n  const endY = listBox!.y + 50;\n  \n  await page.touchscreen.swipe(startX, startY, startX, endY, 300);\n  \n  // Poczekaj na nowe elementy\n  const newProducts = page.locator('.product-item');\n  await expect(newProducts.first()).toBeVisible({ timeout: 5_000 });\n  \n  // Sprawdź, że lista się przewinęła (pierwszy element nie jest już ten sam)\n  const firstProductName = await page.locator('.product-item').first().textContent();\n  // Może być nowy produkt (lub ten sam, jeśli lista jest cached)\n});\n\n// Test tap i hold (long press)\ntest('long press na produkcie pokazuje menu kontekstowe', async ({ page }) => {\n  const productCard = page.locator('.product-card').first();\n  const box = await productCard.boundingBox();\n  \n  // Tap i hold przez 1 sekundę\n  await page.touchscreen.tap(box!.x + box!.width / 2, box!.y + box!.height / 2);\n  await page.waitForTimeout(1000); // Hold\n  \n  // Sprawdź menu kontekstowe\n  const contextMenu = page.getByRole('menu');\n  await expect(contextMenu).toBeVisible();\n  await expect(page.getByRole('menuitem', { name: 'Dodaj do ulubionych' })).toBeVisible();\n});\n\n// Test pinch-to-zoom\ntest('pinch-to-zoom działa na obrazie produktu', async ({ page }) => {\n  const productImage = page.locator('.product-image');\n  const box = await productImage.boundingBox();\n  const centerX = box!.x + box!.width / 2;\n  const centerY = box!.y + box!.height / 2;\n  \n  // Pinch gesture: dwa palce zbliżają się (zoom in)\n  await page.touchscreen.tap(centerX - 50, centerY);\n  await page.touchscreen.tap(centerX + 50, centerY);\n  // Playwright ma ograniczone wsparcie dla multi-touch\n  // W prawdziwym teście użyj Appium\n});\n```\n\n**Appium — pełne gesty:**\n\n```typescript\n// Android/iOS — Multi-touch gestures\n\ntest('użytkownik może pinch-to-zoom na zdjęciu produktu', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const imageElement = await driver.$('~product-image');\n  const box = await imageElement.getLocation();\n  const centerX = box.x + box.width / 2;\n  const centerY = box.y + box.height / 2;\n  \n  // Pinch gesture (zoom in): dwa palce oddalają się\n  await driver.touchPerform([\n    { action: 'press', options: { x: centerX - 50, y: centerY } },\n    { action: 'press', options: { x: centerX + 50, y: centerY } },\n  ]);\n  \n  await driver.pause(500);\n  \n  await driver.touchPerform([\n    { action: 'moveTo', options: { x: centerX - 150, y: centerY } },\n    { action: 'moveTo', options: { x: centerX + 150, y: centerY } },\n  ]);\n  \n  await driver.touchPerform([\n    { action: 'release', options: {} },\n    { action: 'release', options: {} },\n  ]);\n  \n  // Sprawdź zoom indicator\n  const zoomLevel = await driver.$('~zoom-level');\n  await expect(zoomLevel).toContainText('200%');\n  \n  await driver.deleteSession();\n});\n\n// Swipe to delete\ntest('swipe left na elemencie listy pokazuje przycisk usuń', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const listItem = await driver.$('~wishlist-item-0');\n  const box = await listItem.getLocation();\n  \n  // Swipe left\n  await driver.touchPerform([\n    { action: 'press', options: { x: box.x + 50, y: box.y + box.height / 2 } },\n    { action: 'moveTo', options: { x: box.x - 200, y: box.y + box.height / 2 } },\n    { action: 'release', options: {} },\n  ]);\n  \n  // Przycisk Delete powinien się pojawić\n  const deleteButton = await driver.$('~delete-item-0');\n  await expect(deleteButton).toBeVisible();\n  \n  // Tap na Delete\n  await deleteButton.click();\n  \n  // Potwierdź usunięcie (dialog lub undo)\n  const undoButton = await driver.$('~undo-delete');\n  await expect(undoButton).toBeVisible();\n  \n  await driver.deleteSession();\n});\n\n// Drag and drop\ntest('drag and drop — przeniesienie produktu do koszyka', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const product = await driver.$('~product-card-1');\n  const cartIcon = await driver.$('~cart-icon');\n  \n  const productBox = await product.getLocation();\n  const cartBox = await cartIcon.getLocation();\n  \n  // Drag from product to cart\n  await driver.touchPerform([\n    { action: 'press', options: { x: productBox.x + 50, y: productBox.y + 50 } },\n  ]);\n  \n  // Move in steps toward cart\n  const steps = 10;\n  for (let i = 1; i <= steps; i++) {\n    const x = productBox.x + (cartBox.x - productBox.x) * (i / steps);\n    const y = productBox.y + (cartBox.y - productBox.y) * (i / steps);\n    await driver.touchPerform([\n      { action: 'moveTo', options: { x, y } },\n    ]);\n    await driver.pause(50);\n  }\n  \n  await driver.touchPerform([{ action: 'release', options: {} }]);\n  \n  // Sprawdź koszyk\n  const cartBadge = await driver.$('~cart-badge');\n  const badgeText = await cartBadge.getText();\n  expect(badgeText).toBe('1');\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 2. Klawiatura ekranowa\n\n```typescript\n// Playwright — symulacja keyboard overlay\n\ntest('checkout field CVC nie jest zasłonięty klawiaturą', async ({ page, context }) => {\n  await page.setViewportSize({ width: 375, height: 667 }); // iPhone SE\n  await page.goto('/checkout');\n  \n  // Wypełnij kartę\n  await page.getByLabel('Numer karty').fill('4111111111111111');\n  \n  // Skup się na polu CVC — klawiatura się pojawi\n  const cvcField = page.getByLabel('CVC');\n  await cvcField.focus();\n  \n  // Klawiatura numeryczna się pojawia\n  // Sprawdź, czy pole CVC jest widoczne NAD klawiaturą\n  const keyboardHeight = 291; // iOS keyboard height w px\n  const viewportHeight = 667;\n  \n  const cvcBox = await cvcField.boundingBox();\n  \n  // Pole powinno być widoczne (przynajmniej część nad klawiaturą)\n  const isVisibleAboveKeyboard = cvcBox!.y + cvcBox!.height < viewportHeight - keyboardHeight;\n  \n  if (!isVisibleAboveKeyboard) {\n    // FAIL: Pole jest całkowicie zasłonięte\n    // Zrób screenshot\n    await page.screenshot({ path: 'test-results/keyboard-overlay-bug.png' });\n  }\n  \n  expect(isVisibleAboveKeyboard).toBe(true);\n  \n  // Klawiatura numeryczna powinna być widoczna\n  const keyboard = page.locator('.keyboard-numeric');\n  await expect(keyboard).toBeVisible();\n  \n  // Możliwość wprowadzenia CVC\n  await cvcField.fill('123');\n  await expect(cvcField).toHaveValue('123');\n});\n\n// Test keyboard dismiss\ntest('tap poza polem zamyka klawiaturę', async ({ page }) => {\n  await page.getByLabel('Imię').fill('Jan');\n  \n  // Tap na obszar poza inputem\n  await page.click('body', { position: { x: 10, y: 10 } });\n  \n  // Klawiatura powinna się zamknąć\n  // (weryfikacja przez brak keyboard element)\n  const keyboardVisible = await page.locator('.keyboard').isVisible().catch(() => false);\n  expect(keyboardVisible).toBe(false);\n});\n```\n\n**Appium — keyboard handling:**\n\n```typescript\n// Android — keyboard overlay\ntest('Android: pole hasła nie jest zasłonięte klawiaturą', async () => {\n  const driver = await remote({ /* config */ });\n  \n  await driver.launchApp();\n  await driver.$('~login-email').setValue('jan@example.test');\n  \n  const passwordField = await driver.$('~login-password');\n  await passwordField.click();\n  \n  // Poczekaj na klawiaturę\n  await driver.waitFor('~keyboard', { state: 'visible', timeout: 2_000 }).catch(() => {});\n  \n  // Sprawdź, czy pole jest widoczne\n  const fieldVisible = await passwordField.isDisplayed();\n  expect(fieldVisible).toBe(true);\n  \n  // Jeśli pole jest zasłonięte — przewiń formularz\n  if (!await passwordField.isDisplayed()) {\n    // ScrollIntoView\n    await driver.execute('mobile: scroll', {\n      element: await passwordField,\n      strategy: 'android.widget.EditText',\n    });\n  }\n  \n  await passwordField.setValue('SecurePass123!');\n  \n  await driver.deleteSession();\n});\n\n// iOS — dismiss keyboard\ntest('iOS: tap poza polem zamyka klawiaturę', async () => {\n  const driver = await remote({ /* config */ });\n  \n  await driver.$('~name-input').setValue('Jan');\n  \n  // Dismiss keyboard — tap na \"Done\" lub poza field\n  await driver.execute('mobile: dismissKeyboard');\n  \n  // Lub: tap na przycisk \"Done\" na klawiaturze\n  const doneButton = await driver.$('~Done');\n  if (await doneButton.isDisplayed().catch(() => false)) {\n    await doneButton.click();\n  }\n  \n  // Klawiatura powinna być schowana\n  const keyboardPresent = await driver.$('~keyboard').isDisplayed().catch(() => false);\n  expect(keyboardPresent).toBe(false);\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 3. Orientacja (portrait/landscape)\n\n```typescript\n// Playwright — zmiana orientacji\ntest('formularz jest użyteczny w trybie landscape', async ({ page }) => {\n  // Start portrait\n  await page.setViewportSize({ width: 390, height: 844 });\n  await page.goto('/profile/edit');\n  \n  // Zmień na landscape\n  await page.setViewportSize({ width: 844, height: 390 });\n  \n  // Sprawdź layout\n  // W trybie landscape formularz może mieć 2 kolumny\n  const formColumns = page.locator('.form-row');\n  const columnCount = await formColumns.count();\n  \n  // Przycisk \"Zapisz\" musi być widoczny\n  const saveButton = page.getByRole('button', { name: 'Zapisz zmiany' });\n  await expect(saveButton).toBeVisible();\n  \n  // Wróć do portrait\n  await page.setViewportSize({ width: 390, height: 844 });\n  \n  // Layout zmieniony — jedna kolumna\n  const newColumnCount = await formColumns.count();\n  // Kolumny mogą się różnić (ale nie powinno być crashu)\n});\n\n// Test auto-rotate setting\ntest('app obsługuje lock orientacji (nie rotuje gdy włączony)', async ({ page }) => {\n  await page.setViewportSize({ width: 390, height: 844 });\n  await page.goto('/product-detail');\n  \n  // Symuluj rotate event\n  await page.evaluate(() => {\n    // Odpowiednik CSS orientation change\n    window.dispatchEvent(new Event('orientationchange'));\n  });\n  \n  // App może ignorować rotate (jeśli użytkownik zablokował)\n  const currentWidth = page.viewportSize()?.width;\n  // Sprawdź, czy app zachowuje poprzednią orientację\n  // (w realnym teście użyj Appium z setOrientation API)\n});\n```\n\n**Appium — orientation:**\n\n```typescript\n// Ustaw orientację\nawait driver.execute('mobile: rotate', { degrees: 90, perspective: 'landscape' });\n\n// Lub\nawait driver.setOrientation('LANDSCAPE');\n\n// Sprawdź orientację\nconst orientation = await driver.getOrientation(); // 'LANDRAPE' | 'PORTRAIT'\n\n// Test landscape\ntest('landscape: checkout side-by-side layout', async () => {\n  await driver.setOrientation('LANDSCAPE');\n  await driver.launchApp();\n  await driver.$('~checkout-button').click();\n  \n  // W landscape - side by side layout\n  const leftPanel = await driver.$('~checkout-left-panel');\n  const rightPanel = await driver.$('~checkout-right-panel');\n  \n  const leftBox = await leftPanel.getLocation();\n  const rightBox = await rightPanel.getLocation();\n  \n  // Oba panele powinny być widoczne obok siebie\n  expect(leftBox.x).toBeLessThan(rightBox.x);\n  \n  await driver.setOrientation('PORTRAIT');\n});\n```\n\n---\n\n### 4. Przerwanie aplikacji (background/foreground)\n\n```typescript\n// Appium — testowanie lifecycle (app w tle / na wierzchu)\n\ntest('app w tle — dane nie są stracone po powrocie', async () => {\n  const driver = await remote({ /* config */ });\n  \n  // Wypełnij formularz\n  await driver.launchApp();\n  await driver.$('~name-input').setValue('Jan Kowalski');\n  await driver.$('~email-input').setValue('jan@example.test');\n  \n  // Wróć w tło (symulacja: użytkownik kliknął inną app)\n  await driver.execute('mobile: activateApp', { bundleId: 'com.apple.mobilesafari' });\n  \n  // Poczekaj chwilę\n  await driver.pause(3000);\n  \n  // Wróć na wierzch\n  await driver.execute('mobile: activateApp', { pkg: 'com.example.shop' });\n  \n  // Dane powinny być zachowane\n  const nameInput = await driver.$('~name-input');\n  await expect(nameInput).toHaveValue('Jan Kowalski');\n  \n  const emailInput = await driver.$('~email-input');\n  await expect(emailInput).toHaveValue('jan@example.test');\n  \n  // Formularz powinien być w tym samym stanie\n  const keyboardPresent = await driver.$('~keyboard').isDisplayed().catch(() => false);\n  // Klawiatura może być schowana (normalne) — sprawdź tylko dane\n});\n\n// Test: notification podczas używania app\ntest('app obsługuje push notification podczas foreground', async () => {\n  const driver = await remote({ /* config */ });\n  \n  await driver.launchApp();\n  await driver.$('~orders-tab').click();\n  \n  // Symuluj otrzymanie push notification\n  // (w realnym teście: Firebase Console lub backend push)\n  \n  // Sprawdź, czy banner/alert jest widoczny\n  const notificationBanner = await driver.$('~push-notification-banner');\n  const hasBanner = await notificationBanner.isDisplayed({ timeout: 2_000 }).catch(() => false);\n  \n  if (hasBanner) {\n    // Banner jest widoczny — app nie crashuje\n    await expect(notificationBanner).toBeVisible();\n    \n    // Tap na notification — przejdź do wskazanego ekranu\n    await notificationBanner.click();\n    \n    // Sprawdź deep link action\n    const targetScreen = await driver.$('~order-detail-screen');\n    await expect(targetScreen).toBeVisible();\n  }\n  \n  await driver.deleteSession();\n});\n\n// Test: crash app — recovery\ntest('app po crashu uruchamia się poprawnie', async () => {\n  const driver = await remote({ /* config */ });\n  \n  // Symuluj crash (force close)\n  await driver.execute('mobile: killApp', { pkg: 'com.example.shop' });\n  \n  // Uruchom ponownie\n  await driver.launchApp();\n  \n  // App powinna się uruchomić bez crashu\n  const homeScreen = await driver.$('~home-screen');\n  await expect(homeScreen).toBeVisible({ timeout: 15_000 });\n  \n  // Sprawdź, że crash reporter nie pokazuje się\n  const crashDialog = await driver.$('android=new UiSelector().text(\"App nie odpowiada\")');\n  const hasCrash = await crashDialog.isDisplayed().catch(() => false);\n  expect(hasCrash).toBe(false);\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### Checklist UX testów\n\n| Kategoria | Co testujesz | Asercja |\n|---|---|---|\n| **Gesty** | Swipe listy, long press, drag-drop, pinch-zoom | Elementy reagują poprawnie |\n| **Klawiatura** | Overlay, dismiss, keyboard type | Pola widoczne i edytowalne |\n| **Orientacja** | Portrait ↔ landscape, locked rotation | Layout dostosowuje się |\n| **Przerwania** | App → background → foreground | Stan zachowany |\n| **Notifications** | Push podczas foreground | App nie crashuje |\n| **Lifecycle** | Crash → restart | Clean recovery |\n\n---\n\n### Narzędzia pomocnicze\n\n```typescript\n// Screenshot porównanie layoutów\nasync function captureOrientations(page: Page, url: string) {\n  await page.goto(url);\n  \n  await page.setViewportSize({ width: 390, height: 844 });\n  await page.screenshot({ path: `portrait.png` });\n  \n  await page.setViewportSize({ width: 844, height: 390 });\n  await page.screenshot({ path: `landscape.png` });\n}\n\n// Video nagranie gestu\n// Użyj: ffmpeg -f x11grab -framerate 30 -video_size 390x844 -i :0.0 gesture-test.mp4\n```"
      }
    ],
    "quiz": [
      {
        "id": "q26-1-1",
        "question": "Czym różni się mobile web od aplikacji natywne?",
        "options": [
          "Mobile web działa w przeglądarce, natywne korzysta z API systemu i jest instalowana jako aplikacja",
          "Niczym",
          "Native zawsze jest stroną HTML",
          "Mobile web nie wymaga sieci"
        ],
        "correctAnswer": 0,
        "explanation": "Typ aplikacji decyduje o narzędziach, ryzykach i sposobie automatyzacji."
      },
      {
        "id": "q26-1-2",
        "question": "Dlaczego realne urządzenia są ważne?",
        "options": [
          "Ujawniają problemy sprzętowe, systemowe, sieciowe i wydajnościowe niewidoczne w emulatorze",
          "Bo emulatory nigdy nie działają",
          "Bo zastępują wszystkie testy",
          "Tylko do screenshotów"
        ],
        "correctAnswer": 0,
        "explanation": "Emulator nie oddaje w pełni zachowania prawdziwego urządzenia."
      },
      {
        "id": "q26-1-3",
        "question": "Co jest kluczowe w testach uprawnień?",
        "options": [
          "Sprawdzenie przyznania, odmowy i zmiany uprawnień",
          "Tylko happy path",
          "Ukrycie promptów systemowych",
          "Brak resetu aplikacji"
        ],
        "correctAnswer": 0,
        "explanation": "Uprawnienia są częścią przepływu użytkownika i źródłem wielu błędów."
      },
      {
        "id": "q26-1-4",
        "question": "Po co testować deep links?",
        "options": [
          "Aby sprawdzić wejście do aplikacji z zewnętrznego kontekstu",
          "Aby zmienić CSS",
          "Aby ominąć routing",
          "Wyłącznie dla desktopu"
        ],
        "correctAnswer": 0,
        "explanation": "Deep linki są częstym sposobem wejścia z e-maila, push notification lub kampanii."
      },
      {
        "id": "q26-1-5",
        "question": "Co powinien zawierać raport awarii mobilnej?",
        "options": [
          "Model urządzenia, OS, wersję aplikacji, logi, screenshot/wideo i kroki reprodukcji",
          "Tylko nazwę testu",
          "Sam kolor ekranu",
          "Bez danych środowiska"
        ],
        "correctAnswer": 0,
        "explanation": "Bez informacji o urządzeniu i systemie awaria mobilna bywa niemożliwa do odtworzenia."
      },
      {
        "id": "q26-1-6",
        "question": "Kiedy device farm jest szczególnie przydatna?",
        "options": [
          "Gdy potrzebujemy pokryć wiele realnych urządzeń bez ich lokalnego utrzymywania",
          "Do testowania wyłącznie unit",
          "Do zastąpienia strategii",
          "Nigdy"
        ],
        "correctAnswer": 0,
        "explanation": "Device farm daje dostęp do macierzy urządzeń i systemów."
      },
      {
        "id": "q26-1-7",
        "question": "Co odróżnia dobry test tryb offline?",
        "options": [
          "Jawnie przełącza sieć, sprawdza komunikat i synchronizację po powrocie online",
          "Dodaje sleep",
          "Ignoruje dane lokalne",
          "Działa tylko na desktopie"
        ],
        "correctAnswer": 0,
        "explanation": "Offline mode wymaga kontroli sieci i stanu lokalnego."
      },
      {
        "id": "q26-1-8",
        "question": "Najważniejsza zasada lekcji „Aplikacje mobilne webowe, natywne i hybrydowe” to:",
        "options": [
          "Testy mobilne muszą kontrolować urządzenie, system i stan aplikacji",
          "Wystarczy jeden emulator",
          "Uprawnień nie testujemy",
          "Sieć zawsze jest stabilna"
        ],
        "correctAnswer": 0,
        "explanation": "Mobilność oznacza zmienne urządzenia, warunki i integracje z systemem."
      }
    ],
    "references": [
      {
        "title": "Appium Documentation",
        "url": "https://appium.io/docs/en/latest/",
        "description": "Dokumentacja Appium, standardu automatyzacji aplikacji mobilnych."
      },
      {
        "title": "Android Developers - Test apps on Android",
        "url": "https://developer.android.com/training/testing",
        "description": "Oficjalne materiały Google o testowaniu aplikacji Android."
      },
      {
        "title": "Apple Developer - Testing",
        "url": "https://developer.apple.com/testing/",
        "description": "Materiały Apple o testowaniu aplikacji iOS i ekosystemu Apple."
      },
      {
        "title": "BrowserStack App Automate",
        "url": "https://www.browserstack.com/docs/app-automate",
        "description": "Dokumentacja testowania aplikacji mobilnych na device farm."
      }
    ],
    "tipsAndTricks": [
      "Macierz urządzeń powinna wynikać z danych użytkowników i ryzyka, nie z listy wszystkich możliwych modeli.",
      "Emulator jest świetny do szybkiej informacji zwrotnej, ale realne urządzenie ujawnia problemy sprzętowe, sieciowe i systemowe.",
      "W testach mobilnych szczególnie dbaj o stan aplikacji: uprawnienia, pamięć, sieć, sesję i dane lokalne.",
      "Każdy test deep linku, tryb offline lub push notification powinien mieć jawny setup i cleanup stanu urządzenia."
    ],
    "commonMistakes": [
      {
        "mistake": "Testowanie wyłącznie na jednym emulatorze",
        "solution": "Zbuduj małą, ryzykową macierz urządzeń obejmującą realne modele, OS i rozdzielczości."
      },
      {
        "mistake": "Ignorowanie uprawnień systemowych",
        "solution": "Testuj odmowę, przyznanie i zmianę uprawnień dla kamery, lokalizacji, powiadomień i plików."
      },
      {
        "mistake": "Brak kontroli stanu aplikacji",
        "solution": "Czyść dane aplikacji, resetuj sesję i jawnie ustawiaj warunki początkowe."
      },
      {
        "mistake": "Mylenie mobile web z natywne",
        "solution": "Dobierz narzędzia i strategię do typu aplikacji: RWD/PWA, hybrydowe albo natywne."
      }
    ]
  }
};