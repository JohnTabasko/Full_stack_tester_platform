import type { Lesson } from "../../../renderer/types";
import theory26_3 from './lesson-26.3.md?raw';

export const lesson26_3: Lesson = {
  "id": "26.3",
  "moduleId": 26,
  "title": "Emulatory, prawdziwe urządzenia i farmy urządzeń",
  "description": "Emulatory, real devices i farmy: device matrix, BrowserStack, artefakty, logcat, Appium logs, czysty stan urządzenia i dane testowe.",
  "order": 3,
  "difficulty": "advanced",
  "tags": [
    "device-farm",
    "browserstack",
    "saucelabs",
    "emulator",
    "real-device",
    "matrix"
  ],
  "content": {
    "objective": "Po ukończeniu lekcji potrafisz dobrać macierz urządzeń do ryzyka, rozumiesz różnice między emulatorami i prawdziwe urządzenia oraz wiesz, jak używać device farm bez nadmiernych kosztów i niestabilności.",
    "theory": theory26_3,
    "codeExamples": [
      "type DeviceTier = 'pr' | 'nightly' | 'release';\n\ntype DeviceProfile = {\n  name: string;\n  os: string;\n  tier: DeviceTier;\n  reason: string;\n};\n\nconst devices: DeviceProfile[] = [\n  { name: 'Pixel 7 emulator', os: 'Android 14', tier: 'pr', reason: 'szybki smoke' },\n  { name: 'iPhone 14 real device', os: 'iOS 17', tier: 'release', reason: 'krytyczny segment użytkowników' },\n];\n",
      "# Raport awarii mobilnej powinien zawierać:\n- model urządzenia,\n- wersję OS,\n- wersję aplikacji,\n- typ sieci,\n- orientację,\n- logi systemowe,\n- wideo lub screenshot.\n"
    ],
    "exercises": [
      {
        "id": "ex-26-3-1",
        "title": "Konfiguracja emulatora",
        "description": "Skonfiguruj Android emulator (AVD) i iOS Simulator dla CI. Automatyzuj start emulatora, instalację APK i cleanup.",
        "solution": "## Konfiguracja emulatorów dla CI/CD — Android AVD i iOS Simulator\n\n### 1. Android AVD — Setup w CI (GitHub Actions)\n\n```yaml\n# .github/workflows/android-emulator-tests.yml\n\nname: Android Emulator Tests\n\non:\n  push:\n    branches: [main, develop]\n  pull_request:\n\njobs:\n  android-emulator:\n    runs-on: ubuntu-latest\n    timeout-minutes: 20\n    \n    steps:\n      - uses: actions/checkout@v4\n      \n      - name: Setup Java\n        uses: actions/setup-java@v4\n        with:\n          java-version: '17'\n          distribution: 'temurin'\n      \n      - name: Setup Android SDK\n        uses: android-actions/setup-android@v2\n      \n      - name: Create AVD\n        run: |\n          echo \"y\" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install \\\n            \"platform-tools\" \\\n            \"platforms;android-34\" \\\n            \"build-tools;34.0.0\" \\\n            \"system-images;android-34;google_apis;x86_64\"\n          \n          $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager create avd \\\n            --name \"Pixel7_API34\" \\\n            --package \"system-images;android-34;google_apis;x86_64\" \\\n            --device \"pixel_7\"\n      \n      - name: Start emulator in background\n        run: |\n          $ANDROID_HOME/emulator/emulator -avd Pixel7_API34 \\\n            -no-window \\\n            -no-audio \\\n            -gpu swiftshader_indirect \\\n            -no-snapshot \\\n            -no-boot-anim &\n          \n          echo \"Waiting for emulator to boot...\"\n          $ANDROID_HOME/platform-tools/adb wait-for-device\n          $ANDROID_HOME/platform-tools/adb shell \"while [[ -z $(getprop sys.boot_completed) ]]; do sleep 1; done\"\n          echo \"Emulator booted\"\n      \n      - name: Install app\n        run: |\n          $ANDROID_HOME/platform-tools/adb install -r ./apps/shop-android-debug.apk\n      \n      - name: Run Appium tests\n        run: |\n          docker run -d -p 4723:4723 --name appium appium/appium:latest\n          sleep 5\n          \n          npx wdio wdio.emulator.conf.ts\n        env:\n          APPIUM_HOST: localhost\n          DEVICE: 'pixel7-android34-emulator'\n      \n      - name: Upload results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: emulator-test-results\n          path: |\n            test-results/\n            playwright-report/\n            screenshots/\n      \n      - name: Cleanup\n        if: always()\n        run: |\n          docker stop appium || true\n          docker rm appium || true\n          $ANDROID_HOME/platform-tools/adb emu kill || true\n```\n\n---\n\n### 2. Android AVD — wdocker-compose (lokalny dev)\n\n```yaml\n# docker-compose.yml (opcjonalnie)\n\nversion: '3.8'\nservices:\n  appium:\n    image: appium/appium:latest\n    ports:\n      - \"4723:4723\"\n    depends_on:\n      - emulator\n    environment:\n      - ANDROID_HOME=/opt/android-sdk\n      - JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64\n    volumes:\n      - ./apps:/apps\n\n  emulator:\n    image: android-sdk-emulator:latest\n    # Custom image z emulatorem\n    # Lub użyj android-actions/setup-android w CI\n```\n\n---\n\n### 3. Android AVD — Configuration file (wdio.emulator.conf.ts)\n\n```typescript\n// wdio.emulator.conf.ts\n\nimport type { Options } from '@wdio/types';\n\nexport const config: Options.Testrunner = {\n  runner: 'local',\n  \n  specs: ['./tests/mobile/android-emulator.spec.ts'],\n  \n  capabilities: [{\n    platformName: 'Android',\n    'appium:automationName': 'UiAutomator2',\n    \n    // Device: emulator (nie UDID real device)\n    'appium:deviceName': 'Pixel7_API34',\n    'appium:platformVersion': '14',\n    \n    // App install\n    'appium:app': '/apps/shop-android-debug.apk',\n    \n    // Emulator-specific\n    'appium:avd': 'Pixel7_API34',\n    'appium:avdArgs': [\n      '-no-window',\n      '-no-audio',\n      '-gpu', 'swiftshader_indirect',\n    ],\n    \n    // Performance\n    'appium:noReset': false,\n    'appium:autoGrantPermissions': false,\n    \n    // Timeout\n    'appium:newCommandTimeout': 30_000,\n  }],\n  \n  logLevel: 'info',\n  \n  services: [], // Brak usług Appium — uruchamiamy jako docker\n  \n  framework: 'mocha',\n  \n  reporter: [\n    ['html', { outputDir: 'playwright-report' }],\n    ['junit', { outputFile: 'test-results/results.xml' }],\n  ],\n  \n  mochaOpts: {\n    timeout: 60_000,\n  },\n  \n  before: async () => {\n    // Upewnij się że emulator działa przed testami\n    const { execSync } = require('child_process');\n    try {\n      execSync('adb devices', { stdio: 'pipe' });\n      console.log('✓ Emulator accessible via ADB');\n    } catch (e) {\n      throw new Error('Emulator not accessible. Run setup-android.sh first.');\n    }\n  },\n  \n  after: async () => {\n    // Cleanup: screenshoty i logcat\n    const { execSync } = require('child_process');\n    try {\n      execSync('adb logcat -d > test-results/logcat-final.log', { stdio: 'pipe' });\n    } catch (e) {\n      // Ignore errors during cleanup\n    }\n  },\n};\n```\n\n---\n\n### 4. iOS Simulator — Setup w CI\n\n```yaml\n# .github/workflows/ios-simulator-tests.yml\n\nname: iOS Simulator Tests\n\non:\n  push:\n    branches: [main]\n  pull_request:\n\njobs:\n  ios-simulator:\n    runs-on: macos-13  # Mac runner required for iOS\n    timeout-minutes: 25\n    \n    steps:\n      - uses: actions/checkout@v4\n      \n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: '22'\n          cache: 'npm'\n      \n      - name: Install dependencies\n        run: npm ci\n      \n      - name: List available simulators\n        run: |\n          xcrun simctl list devices available | grep \"iPhone\\|iPad\"\n      \n      - name: Boot iOS Simulator\n        run: |\n          xcrun simctl boot \"iPhone 14\"\n          open -a Simulator\n          \n          # Poczekaj aż simulator się uruchomi\n          xcrun simctl booting \"iPhone 14\" && echo \"Simulator booted\"\n      \n      - name: Build app for simulator\n        run: |\n          # Symulacja build (prawdziwy build wymaga Xcode project)\n          xcrun simctl install booted ./apps/shop-ios.app || echo \"App installed\"\n      \n      - name: Start Appium\n        run: |\n          docker run -d -p 4723:4723 --name appium appium/appium:latest\n          sleep 3\n      \n      - name: Run tests\n        run: |\n          npx wdio wdio.ios-simulator.conf.ts\n        env:\n          APPIUM_HOST: localhost\n          DEVICE: 'iphone14-ios17-simulator'\n      \n      - name: Upload artifacts\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: ios-simulator-results\n          path: |\n            test-results/\n            playwright-report/\n      \n      - name: Shutdown simulator\n        if: always()\n        run: |\n          xcrun simctl shutdown all || true\n          docker stop appium || true\n```\n\n---\n\n### 5. iOS Simulator — Configuration (wdio.ios-simulator.conf.ts)\n\n```typescript\n// wdio.ios-simulator.conf.ts\n\nimport type { Options } from '@wdio/types';\n\nexport const config: Options.Testrunner = {\n  runner: 'local',\n  \n  specs: ['./tests/mobile/ios-simulator.spec.ts'],\n  \n  capabilities: [{\n    platformName: 'iOS',\n    'appium:automationName': 'XCUITest',\n    \n    // Simulator — UDID 'auto' lub konkretny\n    'appium:deviceName': 'iPhone 14',\n    'appium:platformVersion': '17',\n    'appium:udid': 'auto', // Auto-find available simulator\n    \n    // App\n    'appium:app': './apps/shop-ios.app',\n    \n    // Simulator-specific\n    'appium:simulatorStartupTimeout': 60_000,\n    'appium:autoAcceptAlerts': true, // Auto-accept permission dialogs\n    \n    // Performance\n    'appium:noReset': false,\n    \n    // Timeout\n    'appium:newCommandTimeout': 30_000,\n  }],\n  \n  logLevel: 'info',\n  services: [],\n  \n  framework: 'mocha',\n  \n  reporter: [\n    ['html', { outputDir: 'playwright-report' }],\n    ['junit', { outputFile: 'test-results/results.xml' }],\n  ],\n  \n  mochaOpts: {\n    timeout: 60_000,\n  },\n};\n```\n\n---\n\n### 6. Script do automatycznego startu (setup-emulator.sh)\n\n```bash\n#!/bin/bash\n# setup-emulator.sh — uruchom emulator przed testami\n\nset -e\n\necho \"=== Android Emulator Setup ===\"\n\n# Check Android SDK\nif [ -z \"$ANDROID_HOME\" ]; then\n  echo \"❌ ANDROID_HOME not set\"\n  exit 1\nfi\n\necho \"✓ ANDROID_HOME: $ANDROID_HOME\"\n\n# Check ADB\nif ! command -v adb &> /dev/null; then\n  echo \"❌ adb not found\"\n  exit 1\nfi\necho \"✓ adb available\"\n\n# Check if emulator is already running\nEMULATOR_RUNNING=$(adb devices | grep -c \"emulator\")\nif [ \"$EMULATOR_RUNNING\" -gt \"0\" ]; then\n  echo \"✓ Emulator already running\"\nelse\n  echo \"📱 Starting emulator...\"\n  \n  # List available AVDs\n  echo \"Available AVDs:\"\n  $ANDROID_HOME/emulator/emulator -list-avds\n  \n  # Start emulator\n  $ANDROID_HOME/emulator/emulator -avd Pixel7_API34 \\\n    -no-window \\\n    -no-audio \\\n    -gpu swiftshader_indirect \\\n    -no-snapshot \\\n    -wipe-data &\n  \n  EMULATOR_PID=$!\n  echo \"Emulator started with PID: $EMULATOR_PID\"\n  \n  # Wait for boot\n  echo \"⏳ Waiting for boot...\"\n  adb wait-for-device\n  \n  # Wait for system boot completed\n  adb shell \"while [[ -z \\$(getprop sys.boot_completed) ]]; do sleep 1; done\"\n  \n  echo \"✓ Emulator booted\"\nfi\n\n# Install app (if changed)\necho \"📦 Installing app...\"\nadb install -r ./apps/shop-android-debug.apk || echo \"App already installed\"\necho \"✓ App installed\"\n\n# Verify app installed\nadb shell pm list packages | grep com.example.shop && echo \"✓ App verified\"\n\necho \"=== Emulator Setup Complete ===\"\n```\n\n---\n\n### 7. Cleanup script\n\n```bash\n#!/bin/bash\n# cleanup-emulator.sh — cleanup po testach\n\nset -e\n\necho \"=== Emulator Cleanup ===\"\n\n# Kill Appium\ndocker stop appium 2>/dev/null || echo \"Appium not running\"\ndocker rm appium 2>/dev/null || echo \"Appium not in container\"\n\n# Get logs\nif [ -d \"test-results\" ]; then\n  echo \"📄 Saving logcat...\"\n  adb logcat -d -t 1000 > test-results/logcat-$(date +%Y%m%d-%H%M%S).log 2>/dev/null || echo \"Logcat not available\"\nfi\n\n# Uninstall app (cleanup)\necho \"🗑️ Uninstalling app...\"\nadb uninstall com.example.shop 2>/dev/null || echo \"App not installed\"\n\n# Stop emulator (opcjonalne)\n# adb emu kill 2>/dev/null || echo \"No emulator to kill\"\n\necho \"=== Cleanup Complete ===\"\n```\n\n---\n\n### 8. Playwright config dla emulatorów mobile web\n\n```typescript\n// playwright.config.ts — mobile web na emulatorach\n\nimport { defineConfig, devices } from '@playwright/test';\n\nexport default defineConfig({\n  testDir: './tests/mobile-web',\n  \n  timeout: 30_000,\n  expect: { timeout: 10_000 },\n  \n  retries: 2, // Retry na CI (emulator bywa niestabilny)\n  \n  use: {\n    baseURL: 'http://localhost:3000',\n    screenshot: 'only-on-failure',\n    video: 'retain-on-failure',\n    trace: 'on-first-retry',\n  },\n  \n  projects: [\n    // Android Emulator\n    {\n      name: 'android-emulator',\n      use: {\n        ...devices['Pixel 7'],\n        // Dostosuj viewport do emulatora\n        viewport: { width: 412, height: 915 },\n      },\n    },\n    \n    // Android Emulator — low-end (mniejszy RAM CPU)\n    {\n      name: 'android-emulator-low-end',\n      use: {\n        browserName: 'chromium',\n        // Symulacja słabszego urządzenia (throttling)\n        viewport: { width: 360, height: 640 },\n      },\n    },\n    \n    // iOS Simulator\n    {\n      name: 'ios-simulator',\n      use: {\n        ...devices['iPhone 14'],\n        viewport: { width: 390, height: 844 },\n      },\n    },\n    \n    // iPad Simulator\n    {\n      name: 'ipad-simulator',\n      use: {\n        ...devices['iPad (gen 7)'],\n      },\n    },\n  ],\n  \n  reporter: [\n    ['html', { outputDir: 'playwright-report' }],\n    ['junit', { outputFile: 'test-results/results.xml' }],\n    ['github'], // GitHub annotations\n  ],\n});\n```\n\n---\n\n### Checklist konfiguracji emulatorów\n\n| Element | Android AVD | iOS Simulator | Sprawdź |\n|---|---|---|---|\n| **JAVA_HOME** | ✓ (Java 17+) | ✓ (w macOS) | `java -version` |\n| **ANDROID_HOME** | ✓ | N/A | `echo $ANDROID_HOME` |\n| **Xcode** | N/A | ✓ (Command Line Tools) | `xcode-select -p` |\n| **AVD/SDK** | ✓ (platform-tools, system-image) | N/A | `avdmanager list avd` |\n| **ADB path** | ✓ w PATH | N/A | `adb devices` |\n| **GPU acceleration** | ✓ (swiftshader) | ✓ (Metal) | Emulator UI responsiveness |\n| **Port forwarding** | ✓ (adb forward) | ✓ (automatic) | `adb devices` shows device |\n| **Boot wait** | ✓ (getprop sys.boot_completed) | ✓ (xcrun simctl booting) | Time to ready |\n\n---\n\n### Troubleshooting emulator issues\n\n```bash\n# Problem: Emulator nie startuje\n# Rozwiązanie:\n\n# 1. Sprawdź HAXM/KVM\nls /dev/kvm\n# Jeśli brak: enable in BIOS or use software rendering\n\n# 2. Check system images\n$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --list_installed | grep system-image\n\n# 3. Recreate AVD\n$ANDROID_HOME/cmdline-tools/latest/bin/avdmanager delete avd -n Pixel7_API34\n$ANDROID_HOME/cmdline-tools/latest/bin/avdmanager create avd -n Pixel7_API34 -k \"system-images;android-34;google_apis;x86_64\"\n\n# 4. Wipe data\n$ANDROID_HOME/emulator/emulator -avd Pixel7_API34 -wipe-data\n\n# iOS Simulator problems:\n\n# 1. Reset simulator\nxcrun simctl shutdown all\nxcrun simctl erase all\n\n# 2. Reset content and settings\nrm -rf ~/Library/Developer/Xcode/DerivedData/*\n\n# 3. Reinstall simulators\nxcode-select --install\n```"
      },
      {
        "id": "ex-26-3-2",
        "title": "Macierz urządzeń realnych",
        "description": "Zbuduj macierz realnych urządzeń na podstawie danych analytics. Dodaj urządzenia do BrowserStack konfiguracji.",
        "solution": "## Budowanie macierzy realnych urządzeń — od danych do BrowserStack\n\n### Faza 1: Zbieranie danych z analytics\n\n```typescript\n// scripts/build-device-matrix.ts\n\nimport { google } from 'googleapis';\nimport fs from 'fs';\n\ntype DeviceStats = {\n  device: string;\n  osVersion: string;\n  sessions: number;\n  sessionShare: number;\n  conversionRate: number;\n  crashRate: number;\n};\n\nasync function fetchAnalyticsData(propertyId: string, days = 30): Promise<DeviceStats[]> {\n  const analytics = google.analyticsdata('v1beta');\n  const auth = await google.auth.getClient({\n    scopes: ['https://www.googleapis.com/auth/analytics.readonly'],\n  });\n\n  const response = await analytics.properties.runReport({\n    auth,\n    property: `properties/${propertyId}`,\n    requestBody: {\n      dimensions: [\n        { name: 'deviceCategory' },\n        { name: 'deviceModel' },\n        { name: 'operatingSystemVersion' },\n      ],\n      metrics: [\n        { name: 'sessions' },\n        { name: 'screenPageViews' },\n        { name: 'bounceRate' },\n        { name: 'conversions' },\n      ],\n      dateRanges: [{\n        startDate: `${days}daysAgo`,\n        endDate: 'today',\n      }],\n    },\n  });\n\n  const rows = response.data.rows || [];\n  const totalSessions = rows.reduce(\n    (sum, row) => sum + parseInt(row.metricValues?.[0]?.value || '0', 10),\n    0\n  );\n\n  return rows.map(row => {\n    const sessions = parseInt(row.metricValues?.[0]?.value || '0', 10);\n    const conversions = parseInt(row.metricValues?.[3]?.value || '0', 10);\n    \n    return {\n      device: row.dimensionValues?.[1]?.value || 'Unknown',\n      osVersion: row.dimensionValues?.[2]?.value || 'Unknown',\n      sessions,\n      sessionShare: sessions / totalSessions,\n      conversionRate: sessions > 0 ? (conversions / sessions) * 100 : 0,\n      crashRate: 0, // Wypełnij z crash reports\n    };\n  }).sort((a, b) => b.sessions - a.sessions);\n}\n\n// Usage\nconst analyticsData = await fetchAnalyticsData('123456789');\nconsole.log(analyticsData.slice(0, 10));\n\n/*\nOutput:\n[\n  { device: 'Samsung Galaxy A54', osVersion: 'Android 13', sessions: 15420, sessionShare: 0.14, ... },\n  { device: 'iPhone 14', osVersion: 'iOS 17', sessions: 13200, sessionShare: 0.12, ... },\n  { device: 'Pixel 7', osVersion: 'Android 14', sessions: 11800, sessionShare: 0.11, ... },\n  ...\n]\n*/\n```\n\n---\n\n### Faza 2: Integracja z crash reports\n\n```typescript\n// fetch-crash-data.ts — integracja z Firebase Crashlytics\n\nimport { initializeApp, cert } from 'firebase-admin';\nimport { getFirestore } from 'firebase-admin/firestore';\n\nasync function fetchCrashData(): Promise<Map<string, number>> {\n  // Inicjalizacja Firebase Admin\n  initializeApp({\n    credential: cert('./firebase-admin.json'),\n  });\n  \n  const db = getFirestore();\n  \n  // Pobierz crashy z ostatnich 30 dni\n  const crashSnapshot = await db.collection('crashes')\n    .where('timestamp', '>', new Date(Date.now() - 30 * 24 * 60 * 60 * 1000))\n    .get();\n  \n  const deviceCrashCount = new Map<string, number>();\n  \n  crashSnapshot.forEach(doc => {\n    const data = doc.data();\n    const device = data.deviceModel;\n    const count = deviceCrashCount.get(device) || 0;\n    deviceCrashCount.set(device, count + 1);\n  });\n  \n  return deviceCrashCount;\n}\n\n// Dodaj crash rate do analytics\nasync function enrichDeviceMatrix(analyticsData: DeviceStats[]) {\n  const crashData = await fetchCrashData();\n  \n  return analyticsData.map(device => ({\n    ...device,\n    crashRate: crashData.get(device.device) || 0,\n  }));\n}\n```\n\n---\n\n### Faza 3: Generowanie macierzy\n\n```typescript\n// generate-device-matrix.ts\n\ntype DeviceMatrixEntry = {\n  device: string;\n  osVersion: string;\n  tier: 'pr' | 'nightly' | 'release';\n  reason: string;\n  sessionShare: number;\n  crashRate: number;\n};\n\nfunction generateDeviceMatrix(analyticsData: DeviceStats[]): DeviceMatrixEntry[] {\n  const matrix: DeviceMatrixEntry[] = [];\n  \n  // === PR (Pull Request) — top 3, najszybszy feedback ===\n  const prDevices = analyticsData\n    .filter(d => d.sessionShare >= 0.08) // Top devices, >8% users\n    .slice(0, 3);\n  \n  prDevices.forEach(d => {\n    matrix.push({\n      device: d.device,\n      osVersion: d.osVersion,\n      tier: 'pr',\n      reason: `Top device (${(d.sessionShare * 100).toFixed(1)}% users)`,\n      sessionShare: d.sessionShare,\n      crashRate: d.crashRate,\n    });\n  });\n  \n  // === NIGHTLY — krytyczne + high crash ===\n  const nightlyDevices = analyticsData\n    .filter(d => \n      d.sessionShare >= 0.05 || // >5% users\n      d.crashRate > 5 || // High crash rate\n      d.device.includes('Xiaomi') // Low-end representative\n    )\n    .filter(d => !prDevices.some(p => p.device === d.device)) // Exclude PR devices\n    .slice(0, 5);\n  \n  nightlyDevices.forEach(d => {\n    matrix.push({\n      device: d.device,\n      osVersion: d.osVersion,\n      tier: 'nightly',\n      reason: d.crashRate > 5 \n        ? `High crash rate (${d.crashRate} crashes)`\n        : `Representative mid-range (${(d.sessionShare * 100).toFixed(1)}% users)`,\n      sessionShare: d.sessionShare,\n      crashRate: d.crashRate,\n    });\n  });\n  \n  // === RELEASE — full coverage ===\n  const releaseDevices = analyticsData\n    .slice(0, 10); // Top 10 devices\n  \n  releaseDevices.forEach(d => {\n    const exists = matrix.some(m => m.device === d.device);\n    if (!exists) {\n      matrix.push({\n        device: d.device,\n        osVersion: d.osVersion,\n        tier: 'release',\n        reason: `Top 10 device (${(d.sessionShare * 100).toFixed(1)}% users)`,\n        sessionShare: d.sessionShare,\n        crashRate: d.crashRate,\n      });\n    }\n  });\n  \n  // Add edge case devices\n  matrix.push({\n    device: 'Huawei P30', // Bez GMS\n    osVersion: 'Android 10',\n    tier: 'release',\n    reason: 'Edge case: no Google Services',\n    sessionShare: 0.01,\n    crashRate: 0,\n  });\n  \n  return matrix;\n}\n\n// Export to JSON\nconst matrix = generateDeviceMatrix(analyticsData);\nfs.writeFileSync('device-matrix.json', JSON.stringify(matrix, null, 2));\n```\n\n---\n\n### Faza 4: BrowserStack configuration\n\n```typescript\n// browserstack-matrix.config.ts — BrowserStack z macierzą\n\nimport type { Options } from '@wdio/types';\n\nconst DEVICE_MATRIX = [\n  // === PR Tier ===\n  { device: 'Samsung Galaxy A54', os: 'Android', osVersion: '13', deviceVersion: '13', tier: 'pr' },\n  { device: 'iPhone 14', os: 'iOS', osVersion: '17', deviceVersion: '17', tier: 'pr' },\n  { device: 'Pixel 7', os: 'Android', osVersion: '14', deviceVersion: '14', tier: 'pr' },\n  \n  // === Nightly Tier ===\n  { device: 'Samsung Galaxy S23', os: 'Android', osVersion: '14', deviceVersion: '14', tier: 'nightly' },\n  { device: 'iPhone 12', os: 'iOS', osVersion: '16', deviceVersion: '16', tier: 'nightly' },\n  { device: 'Xiaomi Redmi Note 12', os: 'Android', osVersion: '13', deviceVersion: '13', tier: 'nightly' },\n  \n  // === Release Tier ===\n  { device: 'Samsung Galaxy Tab S8', os: 'Android', osVersion: '13', deviceVersion: '13', tier: 'release' },\n  { device: 'iPhone 11', os: 'iOS', osVersion: '15', deviceVersion: '15', tier: 'release' },\n  { device: 'Huawei P30', os: 'Android', osVersion: '10', deviceVersion: '10', tier: 'release' },\n];\n\nexport const config: Options.Testrunner = {\n  runner: 'local',\n  \n  specs: ['./tests/mobile/app-hybrid.spec.ts'],\n  \n  // MultiCapabilities — uruchom testy na wszystkich urządzeniach\n  // Użyj zmiennej środowiskowej do filtrowania tier\n  multiCapabilities: DEVICE_MATRIX\n    .filter(d => {\n      const tier = process.env['DEVICE_TIER'];\n      if (!tier) return true; // Wszystkie jeśli brak filter\n      return d.tier === tier;\n    })\n    .map(device => ({\n      'bstack:options': {\n        deviceName: device.device,\n        osVersion: device.osVersion,\n        deviceVersion: device.deviceVersion,\n        realMobile: true,\n      },\n      'browserstack.user': process.env['BROWSERSTACK_USER'],\n      'browserstack.key': process.env['BROWSERSTACK_KEY'],\n      'appium:platformName': device.os,\n      'appium:automationName': device.os === 'Android' ? 'UiAutomator2' : 'XCUITest',\n      'appium:app': process.env['BROWSERSTACK_APP_ID'],\n      'browserstack.video': true,\n      'browserstack.debug': true,\n      'browserstack.networkLogs': true,\n    })),\n  \n  logLevel: 'info',\n  \n  services: [\n    ['browserstack', {\n      browserstackLocal: true,\n    }],\n  ],\n  \n  framework: 'mocha',\n  \n  reporter: [\n    ['html', { outputDir: 'playwright-report' }],\n    ['junit', { outputFile: 'test-results/results.xml' }],\n  ],\n  \n  mochaOpts: {\n    timeout: 60_000,\n  },\n};\n```\n\n---\n\n### Faza 5: GitHub Actions z macierzą\n\n```yaml\n# .github/workflows/device-matrix-tests.yml\n\nname: Device Matrix Tests\n\non:\n  schedule:\n    # Nightly: wszystkie urządzenia nightly tier\n    - cron: '0 2 * * *'\n  pull_request:\n    # PR: tylko pr tier\n  workflow_dispatch:\n    # Manual: wszystkie tier\n\nenv:\n  BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n  BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n\njobs:\n  # === PR TIER ===\n  pr-devices:\n    runs-on: ubuntu-latest\n    if: github.event_name == 'pull_request'\n    strategy:\n      fail-fast: false\n      matrix:\n        device: ['galaxy-a54-android13', 'iphone14-ios17', 'pixel7-android14']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      \n      - name: Run on BrowserStack (PR tier)\n        run: |\n          DEVICE_TIER=pr npx wdio wdio.bs-matrix.conf.ts --device=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n      \n      - name: Upload results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: pr-${{ matrix.device }}-results\n          path: test-results/\n\n  # === NIGHTLY TIER ===\n  nightly-devices:\n    runs-on: ubuntu-latest\n    if: github.event_name == 'schedule'\n    strategy:\n      fail-fast: false\n      matrix:\n        device: ['galaxy-s23-android14', 'iphone12-ios16', 'redmi-note12-android13']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      \n      - name: Run on BrowserStack (Nightly tier)\n        run: |\n          DEVICE_TIER=nightly npx wdio wdio.bs-matrix.conf.ts --device=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n      \n      - name: Upload results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: nightly-${{ matrix.device }}-results\n          path: test-results/\n\n  # === RELEASE TIER ===\n  release-devices:\n    runs-on: ubuntu-latest\n    needs: nightly-devices\n    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')\n    strategy:\n      fail-fast: false\n      matrix:\n        device: ['galaxy-tab-s8-android13', 'iphone11-ios15', 'huawei-p30-android10']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      \n      - name: Run on BrowserStack (Release tier)\n        run: |\n          DEVICE_TIER=release npx wdio wdio.bs-matrix.conf.ts --device=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n      \n      - name: Upload results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: release-${{ matrix.device }}-results\n          path: test-results/\n```\n\n---\n\n### Faza 6: Wynik — JSON macierzy\n\n```json\n// device-matrix.json — wygenerowany automatycznie\n{\n  \"matrix\": [\n    {\n      \"device\": \"Samsung Galaxy A54\",\n      \"osVersion\": \"Android 13\",\n      \"tier\": \"pr\",\n      \"reason\": \"Top device (14.2% users)\",\n      \"sessionShare\": 0.142,\n      \"crashRate\": 0.3\n    },\n    {\n      \"device\": \"iPhone 14\",\n      \"osVersion\": \"iOS 17\",\n      \"tier\": \"pr\",\n      \"reason\": \"Top device (12.1% users)\",\n      \"sessionShare\": 0.121,\n      \"crashRate\": 0.1\n    },\n    {\n      \"device\": \"Pixel 7\",\n      \"osVersion\": \"Android 14\",\n      \"tier\": \"pr\",\n      \"reason\": \"Top device (10.8% users)\",\n      \"sessionShare\": 0.108,\n      \"crashRate\": 0.2\n    },\n    {\n      \"device\": \"Samsung Galaxy S23\",\n      \"osVersion\": \"Android 14\",\n      \"tier\": \"nightly\",\n      \"reason\": \"High-end representative (6.2% users)\",\n      \"sessionShare\": 0.062,\n      \"crashRate\": 0.1\n    },\n    {\n      \"device\": \"iPhone 12\",\n      \"osVersion\": \"iOS 16\",\n      \"tier\": \"nightly\",\n      \"reason\": \"Older iOS representative (6.8% users)\",\n      \"sessionShare\": 0.068,\n      \"crashRate\": 0.2\n    },\n    {\n      \"device\": \"Xiaomi Redmi Note 12\",\n      \"osVersion\": \"Android 13\",\n      \"tier\": \"nightly\",\n      \"reason\": \"Low-end representative (9.5% users) + high crash rate\",\n      \"sessionShare\": 0.095,\n      \"crashRate\": 8.5\n    }\n  ],\n  \"generated\": \"2024-06-23T10:00:00Z\",\n  \"dataSource\": {\n    \"analytics\": \"GA4 Property 123456789\",\n    \"crashReports\": \"Firebase Crashlytics\",\n    \"period\": \"30 days\"\n  }\n}\n```\n\n---\n\n### Checklist budowania macierzy\n\n| Krok | Zadanie | Output |\n|---|---|---|\n| **1. Analytics** | Pobierz top devices z sesjami | `device-stats.json` |\n| **2. Crash reports** | Pobierz crash rate per device | `crash-stats.json` |\n| **3. Support tickets** | Zbierz problematyczne urządzenia | Lista |\n| **4. Selekcja** | Zastosuj kryteria (5%+ lub high crash) | `device-matrix.json` |\n| **5. Tier assignment** | PR/Nightly/Release | Macierz gotowa |\n| **6. BrowserStack config** | Skonfiguruj multiCapabilities | `wdio.bs.conf.ts` |\n| **7. CI integration** | Pipeline z tier filtering | GitHub Actions |\n| **8. Review (quarterly)** | Aktualizuj macierz | Co 3 miesiące |"
      },
      {
        "id": "ex-26-3-3",
        "title": "Optymalizacja kosztów",
        "description": "Zaprojektuj strategię redukcji kosztów device farm: sharding, scheduling, free tier usage, selective runs.",
        "solution": "## Optymalizacja kosztów device farm — strategie i implementacja\n\n### 1. Sharding testów — równomierny podział\n\n```yaml\n# .github/workflows/sharded-tests.yml\n\nname: Sharded Mobile Tests\n\non:\n  schedule:\n    - cron: '0 3 * * *'  # Nightly 3 AM\n\njobs:\n  # Oblicz liczbę shardów na podstawie liczby testów\n  calculate-shards:\n    runs-on: ubuntu-latest\n    outputs:\n      total-tests: ${{ steps.count.outputs.total }}\n      shard-count: ${{ steps.count.outputs.shards }}\n    steps:\n      - id: count\n        run: |\n          TOTAL=$(npx playwright test --list 2>/dev/null | grep -c \"spec\")\n          SHARDS=$(( (TOTAL + 9) / 10 ))  # 10 testów na shard\n          echo \"total=$TOTAL\" >> $GITHUB_OUTPUT\n          echo \"shards=$SHARDS\" >> $GITHUB_OUTPUT\n  \n  sharded-tests:\n    needs: calculate-shards\n    runs-on: ubuntu-latest\n    strategy:\n      matrix:\n        shard: ${{ range(0, needs.calculate-shards.outputs.shard-count) }}\n        device: ['galaxy-a54-android13', 'iphone14-ios17']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      \n      - name: Run shard ${{ matrix.shard }} on ${{ matrix.device }}\n        run: |\n          npx playwright test \\\n            --shard=${{ matrix.shard }}/${{ needs.calculate-shards.outputs.shard-count }} \\\n            --project=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n      \n      - name: Upload shard results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: shard-${{ matrix.shard }}-${{ matrix.device }}\n          path: test-results/\n  \n  aggregate-results:\n    needs: sharded-tests\n    runs-on: ubuntu-latest\n    if: always()\n    steps:\n      - name: Download all results\n        uses: actions/download-artifact@v4\n        with:\n          pattern: 'shard-*-results'\n          path: test-results/\n      \n      - name: Generate summary\n        run: |\n          echo \"## Test Summary\"\n          echo \"Shards completed: ${{ needs.sharded-tests.result }}\"\n          echo \"Total test time: compute from artifacts\"\n```\n\n---\n\n### 2. Scheduling — różne częstotliwości dla różnych tier\n\n```typescript\n// schedule-strategy.ts — logika scheduling\n\ntype ScheduleStrategy = {\n  tier: 'pr' | 'nightly' | 'release';\n  frequency: 'every-commit' | 'nightly' | 'weekly' | 'manual';\n  deviceCount: number;\n  expectedDuration: number; // minutes\n  costEstimate: number; // USD per run\n};\n\nconst SCHEDULE_STRATEGY: ScheduleStrategy[] = [\n  {\n    tier: 'pr',\n    frequency: 'every-commit',\n    deviceCount: 2, // Tylko top 2\n    expectedDuration: 10, // ~5 min/device\n    costEstimate: 0.50, // BrowserStack: ~$0.25/min\n  },\n  {\n    tier: 'nightly',\n    frequency: 'nightly',\n    deviceCount: 5, // Top 5 + high crash\n    expectedDuration: 25, // ~5 min/device\n    costEstimate: 1.25,\n  },\n  {\n    tier: 'release',\n    frequency: 'manual/weekly',\n    deviceCount: 8, // Pełna macierz\n    expectedDuration: 40, // ~5 min/device\n    costEstimate: 2.00,\n  },\n];\n\n// Oblicz miesięczny koszt\nfunction calculateMonthlyCost() {\n  let monthlyCost = 0;\n  \n  // PR: ~20 commits/dzień × 30 dni = 600 runs\n  monthlyCost += 600 * 0.50; // $300\n  \n  // Nightly: 1 run/dzień × 30 dni = 30 runs\n  monthlyCost += 30 * 1.25; // $37.50\n  \n  // Release: 2 runs/tydzień × 4 = 8 runs\n  monthlyCost += 8 * 2.00; // $16\n  \n  return monthlyCost; // ~$353.50/month\n}\n\nconsole.log(`Estimated monthly cost: $${calculateMonthlyCost().toFixed(2)}`);\n// Bez optymalizacji: PR na 5 urządzeniach = $1250/month\n// Z optymalizacją: $353.50/month (72% reduction)\n```\n\n---\n\n### 3. Selective runs — tylko affected tests\n\n```typescript\n// selective-runs.ts — uruchom tylko zmienione testy\n\nimport { execSync } from 'child_process';\n\nfunction getChangedFiles(compareCommit: string): string[] {\n  const output = execSync(\n    `git diff --name-only ${compareCommit}...HEAD`,\n    { encoding: 'utf-8' }\n  );\n  return output.split('\\n').filter(f => f.trim() !== '');\n}\n\nfunction mapChangedFilesToTests(changedFiles: string[]): string[] {\n  const affectedTests: Set<string> = new Set();\n  \n  for (const file of changedFiles) {\n    if (file.startsWith('src/mobile/')) {\n      // Map source changes to related tests\n      const testFile = file\n        .replace('src/mobile/', 'tests/mobile/')\n        .replace('.ts', '.spec.ts');\n      affectedTests.add(testFile);\n    }\n    \n    if (file.startsWith('tests/mobile/') && file.endsWith('.spec.ts')) {\n      affectedTests.add(file);\n    }\n    \n    if (file === 'playwright.config.ts' || file === 'appium.config.ts') {\n      // Config change = uruchom wszystkie testy\n      return ['all'];\n    }\n  }\n  \n  return Array.from(affectedTests);\n}\n\n// Usage in CI\nconst changedFiles = getChangedFiles('HEAD~1');\nconst affectedTests = mapChangedFilesToTests(changedFiles);\n\nconsole.log(`Changed files: ${changedFiles.join(', ')}`);\nconsole.log(`Affected tests: ${affectedTests.join(', ')}`);\n\nif (affectedTests.includes('all') || affectedTests.length === 0) {\n  console.log('Running all tests');\n} else {\n  console.log(`Running affected tests: ${affectedTests.join(' ')}`);\n  // npx playwright test ... --spec ${affectedTests.join(' ')}\n}\n```\n\n---\n\n### 4. Retry na tym samym urządzeniu, nie na nowym\n\n```yaml\n# Retry strategy — NIE na nowym urządzeniu\n\n# ❌ Źle: Retry na innym urządzeniu = mnożnik kosztów\nstrategy:\n  matrix:\n    device: ['galaxy-a54', 'iphone14', 'pixel7']\n  parallelism: 3\n\n# ✅ Dobrze: Retry na tym samym urządzeniu = stały koszt\nconfig:\n  retries:\n    failedTests: 1  # Retry tylko failed, nie wszystkie\n  \n  # Retry count per device, nie per matrix entry\n  maxRetries: 1\n\n# Retry only na PR (flaky protection)\n# Na nightly/release: 0 retries (chcemy wiedzieć o problemach)\nif: github.event_name == 'pull_request'\n  retries: 2\nelse:\n  retries: 0\n```\n\n---\n\n### 5. Free tier usage — Firebase Test Lab\n\n```bash\n# .github/workflows/firebase-free-tier.yml\n\n# Firebase Test Lab — free 10 device-hours/day dla Android\n\nname: Firebase Free Tier Tests\n\non:\n  schedule:\n    - cron: '0 4 * * *'  # 4 AM — poza peak hours\n\njobs:\n  firebase-android:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      \n      - name: Setup Google Cloud SDK\n        uses: google-github-actions/setup-gcloud@v2\n        with:\n          service_account_key: ${{ secrets.GCP_SA_KEY }}\n      \n      - name: Run instrumented tests on Firebase\n        run: |\n          # Free tier: 10 device-hours/day\n          # Użyj maksymalnie 10h = 2 device × 5min test × 10 runs\n          \n          gcloud firebase test android run \\\n            --app=./apps/shop-android-debug.apk \\\n            --test=./tests/android-instrumentation.apk \\\n            --device model=Pixel7,version=14 \\\n            --device model=GalaxyA54,version=13 \\\n            --timeout 300s \\\n            --results-dir=gs://my-bucket/firebase-results/$(date +%Y%m%d)\n        \n      - name: Download results\n        run: |\n          gsutil -m cp -r gs://my-bucket/firebase-results/latest ./firebase-results\n      \n      - name: Upload artifacts\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: firebase-results\n          path: firebase-results/\n```\n\n---\n\n### 6. Device farm cost optimization matrix\n\n```markdown\n## Strategie redukcji kosztów — podsumowanie\n\n| Strategia | Redukcja kosztów | Trade-off |\n|---|---|---|\n| **Sharding** | 50-70% (run 100 tests na 4 shardach jednocześnie) | Setup complexity |\n| **Selective runs** | 60-80% (tylko affected tests) | Może pominąć regression |\n| **Retry only on PR** | 30% (nightly/release bez retry) | Więcej false negatives |\n| **Free tier (Firebase)** | 100% (dla Android) | Ograniczona macierz |\n| **Scheduling (off-peak)** | 20% (tańsze godziny u providerów) | Opóźniony feedback |\n| **Cache App install** | 15% (nie reinstall na każdy test) | Stare app version risk |\n| **Shard by feature** | 40% (UI tests ≠ API tests) | Nie dla E2E |\n\n## Przykładowy kalkulator\n\n```\nBez optymalizacji:\n- 100 tests × 5 devices × 5 min × $0.25/min = $62.50/run\n- 30 dni × $62.50 = $1,875/month\n\nZ optymalizacją:\n- Sharding: 4 shards × $15.63 = $62.50/run (ale 4x szybciej)\n- Selective: średnio 30% affected = $18.75/run\n- Free tier Firebase: $0 dla Android\n- Retry only PR: $18.75 × 20 PRs + $62.50 × 10 nightly = $437.50 + $625 = $1,062.50\n\nCałkowita redukcja: ~44%\n```\n```\n\n---\n\n### 7. LambdaTest jako tańsza alternatywa\n\n```typescript\n// wdio-lambdatest.conf.ts — tańszy niż BrowserStack\n\nimport type { Options } from '@wdio/types';\n\nexport const config: Options.Testrunner = {\n  // LambdaTest configuration\n  user: process.env['LT_USERNAME'],\n  key: process.env['LT_ACCESS_KEY'],\n  \n  specs: ['./tests/mobile/*.spec.ts'],\n  \n  capabilities: [{\n    platformName: 'Android',\n    'appium:automationName': 'UiAutomator2',\n    'appium:deviceName': 'Samsung Galaxy S23', // LambdaTest device name\n    'appium:platformVersion': '13',\n    'appium:app': process.env['LT_APP_ID'],\n    \n    // LambdaTest specific\n    'LT:Options': {\n      platformName: 'Android',\n      deviceName: 'Samsung Galaxy S23',\n      realMobile: true,\n    },\n  }],\n  \n  services: [\n    ['lambdatest', {\n      tunnel: true, // Local testing\n    }],\n  ],\n  \n  framework: 'mocha',\n  mochaOpts: { timeout: 60_000 },\n  \n  reporter: [\n    ['html', { outputDir: 'playwright-report' }],\n  ],\n};\n\n// LambdaTest pricing: ~$0.08/min (vs $0.25/min BrowserStack)\n// Savings: ~68% per minute\n```\n\n---\n\n### 8. Self-hosted farm — kiedy się opłaca\n\n```bash\n# setup-self-hosted-stf.sh\n\n# OpenSTF / DeviceFarmer to open-source alternative\n# Wymagania:\n# - Serwer z USB ports (lub USB hubs)\n# - Lista urządzeń (10+)\n# - Administrator do utrzymania\n\n# Kalkulator:\n# - 10 urządzeń × $200 = $2,000 (one-time)\n# - Server: $100/month\n# - Admin: $1,000/month (częściowy etat)\n# - Total: $1,100/month + $2,000 one-time\n\n# Break-even vs BrowserStack:\n# BrowserStack: 10 devices × 30 days × $0.25/min × 60min = $4,500/month\n# Self-hosted: $1,100/month + amortyzacja $167/month = $1,267/month\n# Savings: ~72%\n\n# UWAGA: Self-hosted ma hidden costs:\n# - Urządzenia się zużywają\n# - Administrator potrzebny na awarie\n# - No SLA, własne backup/recovery\n```\n\n---\n\n### Checklist optymalizacji\n\n| Element | Sprawdź | Akcja |\n|---|---|---|\n| **Sharding** | Czy test suite trwa > 15 min? | Podziel na 4-8 shardów |\n| **Selective runs** | Czy wszystkie testy uruchamiane na każdym PR? | Filtruj changed files |\n| **Retry budget** | Ile razy retry na urządzenie? | Retry tylko failed, nie wszystkie |\n| **Free tier** | Czy używasz Firebase free tier? | 10h/day free dla Android |\n| **Scheduling** | Czy testy nocne są konieczne? | Tylko critical path nightly |\n| **Device count** | Czy wszystkie 10 urządzeń jest potrzebne na PR? | Top 2-3 na PR |\n| **Provider comparison** | Czy porównałeś ceny? | LambdaTest vs BrowserStack vs Sauce |\n| **Self-hosted vs cloud** | Czy masz >20 urządzeń? | Self-hosted może być tańszy |"
      },
      {
        "id": "ex-26-3-4",
        "title": "Reporting i monitoring",
        "description": "Skonfiguruj dashboard metryk jakości: pass rate, flaky rate, coverage trend. Automatyzuj raportowanie z CI.",
        "solution": "## Dashboard metryk jakości testów mobilnych\n\n### 1. Struktura metryk\n\n```typescript\n// metrics/types.ts — typy metryk\n\ntype TestMetrics = {\n  total: number;\n  passed: number;\n  failed: number;\n  skipped: number;\n  flaky: number;\n  duration: number; // seconds\n  timestamp: string;\n};\n\ntype DeviceMetrics = {\n  device: string;\n  osVersion: string;\n  metrics: TestMetrics;\n  passRate: number; // passed / total * 100\n  flakyRate: number; // flaky / total * 100\n};\n\ntype TrendMetrics = {\n  date: string;\n  passRate: number;\n  flakyRate: number;\n  totalTests: number;\n  failedTests: number;\n  avgDuration: number;\n};\n\ntype CoverageMetrics = {\n  criticalPath: number; // % pokrycia krytycznych ścieżek\n  featureCoverage: number; // % pokrycia funkcji\n  deviceCoverage: number; // % urządzeń z macierzy\n};\n\n// Agregacja metryk\nfunction aggregateMetrics(allResults: DeviceMetrics[]): {\n  overall: TestMetrics;\n  byDevice: DeviceMetrics[];\n  trend: TrendMetrics[];\n  coverage: CoverageMetrics;\n} {\n  const overall = allResults.reduce(\n    (acc, d) => ({\n      total: acc.total + d.metrics.total,\n      passed: acc.passed + d.metrics.passed,\n      failed: acc.failed + d.metrics.failed,\n      skipped: acc.skipped + d.metrics.skipped,\n      flaky: acc.flaky + d.metrics.flaky,\n      duration: acc.duration + d.metrics.duration,\n    }),\n    { total: 0, passed: 0, failed: 0, skipped: 0, flaky: 0, duration: 0 }\n  );\n  \n  return {\n    overall,\n    byDevice: allResults,\n    trend: calculateTrend(allResults),\n    coverage: calculateCoverage(allResults),\n  };\n}\n```\n\n---\n\n### 2. Parser JUnit XML → metryki\n\n```typescript\n// metrics/parser.ts — parsuj wyniki JUnit\n\nimport { parseStringPromise } from 'xml2js';\nimport fs from 'fs';\n\ntype JUnitTestCase = {\n  $: {\n    name: string;\n    classname: string;\n    time: string;\n  };\n  failure?: Array<{ $: { message: string }; _: string }>;\n  skipped?: unknown;\n};\n\ntype JUnitTestSuite = {\n  testcase: JUnitTestCase[];\n  $: {\n    name: string;\n    tests: string;\n    failures: string;\n    skipped: string;\n    time: string;\n  };\n};\n\nasync function parseJUnitResults(xmlPath: string): Promise<TestMetrics> {\n  const xml = fs.readFileSync(xmlPath, 'utf-8');\n  const result = await parseStringPromise(xml);\n  \n  const testsuite: JUnitTestSuite = result.testsuite;\n  const testcases = testsuite.testcase || [];\n  \n  let passed = 0;\n  let failed = 0;\n  let skipped = 0;\n  let flaky = 0;\n  \n  for (const tc of testcases) {\n    if (tc.skipped) {\n      skipped++;\n    } else if (tc.failure) {\n      failed++;\n      // Sprawdź czy to flaky (success przy retry)\n      if (tc.$.time && parseFloat(tc.$.time) > 60) {\n        // Prawdopodobnie flaky (długi czas suggest retry)\n        flaky++;\n      }\n    } else {\n      passed++;\n    }\n  }\n  \n  return {\n    total: testcases.length,\n    passed,\n    failed,\n    skipped,\n    flaky,\n    duration: parseFloat(testsuite.$.time || '0'),\n    timestamp: new Date().toISOString(),\n  };\n}\n\n// Usage\nasync function aggregateAllResults(resultsDir: string): Promise<DeviceMetrics[]> {\n  const files = fs.readdirSync(resultsDir).filter(f => f.endsWith('.xml'));\n  const allMetrics: DeviceMetrics[] = [];\n  \n  for (const file of files) {\n    const deviceName = file.replace('.xml', '');\n    const metrics = await parseJUnitResults(`${resultsDir}/${file}`);\n    \n    allMetrics.push({\n      device: deviceName,\n      osVersion: 'unknown', // Uzupełnij z matrix\n      metrics,\n      passRate: (metrics.passed / metrics.total) * 100,\n      flakyRate: (metrics.flaky / metrics.total) * 100,\n    });\n  }\n  \n  return allMetrics;\n}\n```\n\n---\n\n### 3. GitHub Actions — publikacja metryk\n\n```yaml\n# .github/workflows/metrics.yml\n\nname: Mobile Test Metrics\n\non:\n  workflow_run:\n    workflows: ['Mobile Tests Pipeline']\n    types: [completed]\n\njobs:\n  metrics:\n    runs-on: ubuntu-latest\n    if: github.event.workflow_run.conclusion == 'success'\n    steps:\n      - uses: actions/checkout@v4\n      \n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: '22'\n      \n      - name: Download all test results\n        uses: actions/download-artifact@v4\n        with:\n          pattern: 'test-results-*'\n          path: test-results/\n      \n      - name: Aggregate metrics\n        run: |\n          node scripts/aggregate-metrics.js\n        env:\n          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n      \n      - name: Publish GitHub Summary\n        run: |\n          echo \"## 📊 Mobile Test Metrics\" >> $GITHUB_STEP_SUMMARY\n          echo \"\" >> $GITHUB_STEP_SUMMARY\n          echo \"| Metric | Value |\" >> $GITHUB_STEP_SUMMARY\n          echo \"|--------|-------|\" >> $GITHUB_STEP_SUMMARY\n          echo \"| Total Tests | \\${{ env.TOTAL_TESTS }} |\" >> $GITHUB_STEP_SUMMARY\n          echo \"| Passed | \\${{ env.PASSED_TESTS }} |\" >> $GITHUB_STEP_SUMMARY\n          echo \"| Pass Rate | \\${{ env.PASS_RATE }}% |\" >> $GITHUB_STEP_SUMMARY\n          echo \"| Flaky Rate | \\${{ env.FLAKY_RATE }}% |\" >> $GITHUB_STEP_SUMMARY\n      \n      - name: Push metrics to InfluxDB\n        if: env.INFLUX_URL\n        run: |\n          curl -X POST \"$INFLUX_URL/write?db=testmetrics\" \\\n            --data-binary \"mobile_metrics,run=$GITHUB_RUN_ID pass_rate=$PASS_RATE,flaky_rate=$FLAKY_RATE,total=$TOTAL_TESTS\"\n\n      - name: Upload metrics JSON\n        uses: actions/upload-artifact@v4\n        with:\n          name: metrics-summary\n          path: metrics-summary.json\n```\n\n---\n\n### 4. Grafana dashboard — JSON definition\n\n```json\n// grafana-dashboard.json\n{\n  \"dashboard\": {\n    \"title\": \"Mobile Test Quality Dashboard\",\n    \"uid\": \"mobile-test-quality\",\n    \"timezone\": \"browser\",\n    \"panels\": [\n      {\n        \"title\": \"Pass Rate (%) - Last 30 days\",\n        \"type\": \"stat\",\n        \"gridPos\": { \"x\": 0, \"y\": 0, \"w\": 4, \"h\": 4 },\n        \"targets\": [\n          {\n            \"expr\": \"avg(mobile_metrics{job='nightly'})\",\n            \"legendFormat\": \"Pass Rate\"\n          }\n        ],\n        \"fieldConfig\": {\n          \"defaults\": {\n            \"unit\": \"percent\",\n            \"thresholds\": {\n              \"mode\": \"absolute\",\n              \"steps\": [\n                { \"value\": 0, \"color\": \"red\" },\n                { \"value\": 90, \"color\": \"yellow\" },\n                { \"value\": 98, \"color\": \"green\" }\n              ]\n            }\n          }\n        }\n      },\n      {\n        \"title\": \"Flaky Tests - Trend\",\n        \"type\": \"timeseries\",\n        \"gridPos\": { \"x\": 4, \"y\": 0, \"w\": 8, \"h\": 8 },\n        \"targets\": [\n          {\n            \"expr\": \"sum(mobile_metrics{job='nightly'}) by (device)\",\n            \"legendFormat\": \"{{device}}\"\n          }\n        ]\n      },\n      {\n        \"title\": \"Test Duration (avg) - by device\",\n        \"type\": \"bargauge\",\n        \"gridPos\": { \"x\": 12, \"y\": 0, \"w\": 12, \"h\": 8 },\n        \"targets\": [\n          {\n            \"expr\": \"avg(mobile_metrics_duration) by (device)\",\n            \"legendFormat\": \"{{device}}\"\n          }\n        ]\n      },\n      {\n        \"title\": \"Failed Tests by Device\",\n        \"type\": \"table\",\n        \"gridPos\": { \"x\": 0, \"y\": 8, \"w\": 24, \"h\": 8 },\n        \"targets\": [\n          {\n            \"expr\": \"topk(10, sum(mobile_metrics_failed) by (device, test_name))\",\n            \"format\": \"table\"\n          }\n        ]\n      }\n    ],\n    \"templating\": {\n      \"list\": [\n        {\n          \"name\": \"job\",\n          \"type\": \"query\",\n          \"options\": [\n            { \"value\": \"pr\", \"label\": \"Pull Request\" },\n            { \"value\": \"nightly\", \"label\": \"Nightly\" },\n            { \"value\": \"release\", \"label\": \"Release\" }\n          ]\n        }\n      ]\n    }\n  }\n}\n```\n\n---\n\n### 5. Alerting — powiadomienia o degradacji\n\n```yaml\n# grafana-alerts.yaml\n\napiVersion: 1\n\nalerts:\n  - name: low-pass-rate\n    condition: avg(mobile_pass_rate) < 95\n    for: 5m\n    labels:\n      severity: critical\n      team: qa\n    annotations:\n      summary: \"Pass rate below 95% on {{ $labels.job }}\"\n      description: \"Pass rate: {{ $values.A.value }}% on {{ $labels.job }}\"\n    dashboardLink: \"/d/mobile-test-quality\"\n    \n  - name: high-flaky-rate\n    condition: avg(mobile_flaky_rate) > 5\n    for: 10m\n    labels:\n      severity: warning\n      team: qa\n    annotations:\n      summary: \"Flaky rate above 5% on {{ $labels.job }}\"\n      description: \"Flaky rate: {{ $values.A.value }}%\"\n\n  - name: device-failure-spike\n    condition: sum(mobile_failed{device=\"*\"}) by (device) > 10\n    for: 3m\n    labels:\n      severity: warning\n      team: qa\n    annotations:\n      summary: \"Device {{ $labels.device }} has high failure rate\"\n\n# GitHub Actions notification on alert\n- name: Alert on Slack\n  if: always()\n  run: |\n    if [ \"${{ steps.metrics.outputs.pass_rate }}\" -lt 95 ]; then\n      curl -X POST \"$SLACK_WEBHOOK\" \\\n        -H 'Content-Type: application/json' \\\n        -d '{\"text\": \":rotating_light: Pass rate dropped to ${{ steps.metrics.outputs.pass_rate }}% on ${{ github.ref }}\"}'\n    fi\n```\n\n---\n\n### 6. Automated report generation\n\n```typescript\n// scripts/generate-report.ts\n\nimport fs from 'fs';\n\nfunction generateMarkdownReport(metrics: {\n  overall: TestMetrics;\n  byDevice: DeviceMetrics[];\n  trend: TrendMetrics[];\n}) {\n  const passRate = (metrics.overall.passed / metrics.overall.total * 100).toFixed(1);\n  const flakyRate = (metrics.overall.flaky / metrics.overall.total * 100).toFixed(1);\n  \n  const report = `# Mobile Test Quality Report\n\nGenerated: ${new Date().toISOString()}\n\n## Summary\n\n| Metric | Value |\n|--------|-------|\n| **Total Tests** | ${metrics.overall.total} |\n| **Passed** | ${metrics.overall.passed} (${passRate}%) |\n| **Failed** | ${metrics.overall.failed} |\n| **Skipped** | ${metrics.overall.skipped} |\n| **Flaky** | ${metrics.overall.flaky} (${flakyRate}%) |\n| **Duration** | ${(metrics.overall.duration / 60).toFixed(1)} min |\n\n## By Device\n\n| Device | Pass Rate | Flaky Rate | Failed |\n|--------|-----------|------------|--------|\n${metrics.byDevice.map(d => \n  `| ${d.device} | ${d.passRate.toFixed(1)}% | ${d.flakyRate.toFixed(1)}% | ${d.metrics.failed} |`\n).join('\\n')}\n\n## Trend (Last 7 days)\n\n| Date | Pass Rate | Failed |\n|------|-----------|--------|\n${metrics.trend.map(t => \n  `| ${t.date} | ${t.passRate.toFixed(1)}% | ${t.failedTests} |`\n).join('\\n')}\n\n## Alerts\n\n${metrics.overall.failed > 0 ? '⚠️ There are failing tests. Review detailed logs.' : '✅ All tests passed.'}\n`;\n  \n  fs.writeFileSync('REPORT.md', report);\n  return report;\n}\n```\n\n---\n\n### 7. Integration z Linear/Jira\n\n```typescript\n// scripts/create-tickets.ts — automatyczne tworzenie ticketów na failed tests\n\nimport { LinearClient } from '@linear/sdk';\n\nconst linear = new LinearClient({ apiKey: process.env['LINEAR_API_KEY'] });\n\nasync function createTicketsForFailures(failures: Failure[]) {\n  for (const failure of failures) {\n    // Sprawdź czy ticket już istnieje\n    const existing = await linear.issues({\n      filter: {\n        identifier: { contains: failure.testName },\n      },\n    });\n    \n    if (existing.nodes.length > 0) {\n      console.log(`Ticket already exists for ${failure.testName}`);\n      continue;\n    }\n    \n    // Utwórz nowy ticket\n    const issue = await linear.createIssue({\n      title: `[Mobile] ${failure.testName} failing on ${failure.device}`,\n      description: `\n## Failure Details\n\n**Test:** ${failure.testName}\n**Device:** ${failure.device}\n**OS:** ${failure.osVersion}\n**Run:** ${failure.runUrl}\n\n**Error:**\n\\`\\`\\`\n${failure.error}\n\\`\\`\\`\n\n**Screenshot:** ${failure.screenshotUrl}\n**Trace:** ${failure.traceUrl}\n\n**Reproduction:**\n1. ${failure.steps.join('\\n2. ')}\n\n**Expected:** ${failure.expected}\n**Actual:** ${failure.actual}\n      `,\n      teamId: 'team-id-qa',\n      labelIds: ['label-mobile-testing', 'label-flaky-test'],\n      priority: failure.critical ? 0 : 2, // Urgent vs Normal\n    });\n    \n    console.log(`Created ticket: ${issue.id}`);\n  }\n}\n```\n\n---\n\n### Checklist dashboard\n\n| Komponent | Działanie | Narzędzie |\n|---|---|---|\n| **JUnit parser** | Parsuj XML → metryki | xml2js |\n| **InfluxDB/Grafana** | Store + visualize | InfluxDB + Grafana |\n| **GitHub Summary** | Summary w PR | GitHub Actions |\n| **Slack alerts** | Powiadomienia na degraded | Slack webhook |\n| **Jira/Linear tickets** | Automatyczne tickety | Linear SDK |\n| **SLO/SLA tracking** | Pass rate < 95% = alert | Grafana alerting |\n| **Coverage tracking** | % krytycznych ścieżek | Custom metrics |"
      },
      {
        "id": "ex-26-3-5",
        "title": "CI pipeline mobilny",
        "description": "Zbuduj kompletny pipeline CI: emulator PR → device farm nightly → release gate. Z интеграцией artifact collection.",
        "solution": "## Kompletny CI pipeline mobilny — od PR do Release\n\n### Architektura pipeline\n\n```\n┌─────────────────────────────────────────────────────────────┐\n│                    TRIGGER EVENTS                           │\n├─────────────────────────────────────────────────────────────┤\n│  PR (commit)      │  Schedule (nightly)  │  Release tag    │\n└───────┬───────────┴──────────┬───────────┴───────┬─────────┘\n        │                      │                   │\n        ▼                      ▼                   ▼\n┌───────────────┐      ┌───────────────┐   ┌───────────────┐\n│   PR TIER     │      │  NIGHTLY TIER │   │  RELEASE TIER │\n│  (emulator+2) │      │   (5 device)  │   │   (8 device)  │\n│   ~10 min     │      │    ~30 min    │   │    ~60 min    │\n└───────┬───────┘      └───────┬───────┘   └───────┬───────┘\n        │                      │                   │\n        ▼                      ▼                   ▼\n┌───────────────┐      ┌───────────────┐   ┌───────────────┐\n│  FAIL-FAST    │      │  AGGREGATE    │   │  GATE CHECK   │\n│  (stop on 1st)│      │  (all must    │   │  (all must    │\n│              │      │   pass)       │   │   pass)       │\n└───────────────┘      └───────┬───────┘   └───────┬───────┘\n                               │                   │\n                               ▼                   ▼\n                         ┌───────────┐       ┌───────────┐\n                         │  METRICS  │       │  PUBLISH  │\n                         │  DASHBOARD│       │  RELEASE  │\n                         └───────────┘       └───────────┘\n```\n\n---\n\n### 1. Główny workflow — single file\n\n```yaml\n# .github/workflows/mobile-testing.yml\n\nname: Mobile Testing Pipeline\n\non:\n  push:\n    branches: [main, develop]\n  pull_request:\n    paths:\n      - 'src/mobile/**'\n      - 'tests/mobile/**'\n      - 'playwright.config.ts'\n      - 'appium.config.ts'\n      - 'package.json'\n  schedule:\n    - cron: '0 2 * * *'  # Nightly 2 AM\n  workflow_dispatch:\n    inputs:\n      tier:\n        description: 'Device tier to run'\n        required: true\n        default: 'all'\n        type: choice\n        options: ['pr', 'nightly', 'release', 'all']\n\nenv:\n  APPIUM_HOST: localhost\n  BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n  BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n  STAGING_URL: ${{ vars.STAGING_URL }}\n\njobs:\n  # ============================================================\n  # JOB 1: Setup — przygotowanie środowiska\n  # ============================================================\n  setup:\n    runs-on: ubuntu-latest\n    outputs:\n      matrix-type: ${{ steps.matrix.outputs.type }}\n      should-run: ${{ steps.check.outputs.should_run }}\n    steps:\n      - uses: actions/checkout@v4\n      \n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: '22'\n          cache: 'npm'\n      \n      - run: npm ci\n      \n      - name: Determine trigger type\n        id: matrix\n        run: |\n          if [ \"${{ github.event_name }}\" == \"pull_request\" ]; then\n            echo \"type=pr\" >> $GITHUB_OUTPUT\n          elif [ \"${{ github.event_name }}\" == \"schedule\" ]; then\n            echo \"type=nightly\" >> $GITHUB_OUTPUT\n          elif [ \"${{ github.event_name }}\" == \"workflow_dispatch\" ]; then\n            echo \"type=${{ inputs.tier }}\" >> $GITHUB_OUTPUT\n          else\n            echo \"type=release\" >> $GITHUB_OUTPUT\n          fi\n      \n      - name: Check if tests should run\n        id: check\n        run: |\n          if [ \"${{ github.event_name }}\" == \"push\" ] && [ \"${{ github.ref }}\" != \"refs/heads/main\" ]; then\n            echo \"should_run=false\" >> $GITHUB_OUTPUT\n          else\n            echo \"should_run=true\" >> $GITHUB_OUTPUT\n          fi\n\n  # ============================================================\n  # JOB 2: PR TIER — fast feedback (emulator + 2 real devices)\n  # ============================================================\n  pr-tier:\n    needs: setup\n    if: needs.setup.outputs.should_run == 'true' && needs.setup.outputs.matrix-type == 'pr'\n    runs-on: ubuntu-latest\n    timeout-minutes: 15\n    strategy:\n      fail-fast: true  # Fail fast na PR\n      matrix:\n        include:\n          - device: 'android-emulator'\n            type: 'emulator'\n          - device: 'pixel7-android14'\n            type: 'browserstack'\n          - device: 'iphone14-ios17'\n            type: 'browserstack'\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      \n      # Android Emulator setup\n      - name: Setup Android Emulator\n        if: matrix.type == 'emulator'\n        uses: android-actions/setup-android@v2\n      \n      - name: Start Android Emulator\n        if: matrix.type == 'emulator'\n        run: |\n          $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager create avd --name test --package \"system-images;android-34;google_apis;x86_64\" --device pixel_7\n          $ANDROID_HOME/emulator/emulator -avd test -no-window -no-audio &\n          adb wait-for-device\n      \n      # BrowserStack setup\n      - name: Start Appium\n        if: matrix.type == 'browserstack'\n        run: docker run -d -p 4723:4723 appium/appium\n      \n      - name: Run tests\n        run: |\n          npx wdio wdio.${{ matrix.device }}.conf.ts\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n      \n      - name: Upload artifacts\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: pr-${{ matrix.device }}-results\n          path: |\n            test-results/\n            playwright-report/\n            screenshots/\n\n  # ============================================================\n  # JOB 3: NIGHTLY TIER — regular coverage (5 devices)\n  # ============================================================\n  nightly-tier:\n    needs: setup\n    if: needs.setup.outputs.should_run == 'true' && needs.setup.outputs.matrix-type == 'nightly'\n    runs-on: ubuntu-latest\n    timeout-minutes: 40\n    strategy:\n      fail-fast: false  # Aggregate all results\n      matrix:\n        device: ['pixel7-android14', 'galaxy-a54-android13', 'redmi-note12-android13', 'iphone14-ios17', 'iphone12-ios16']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      \n      - name: Start Appium\n        run: docker run -d -p 4723:4723 appium/appium\n      \n      - name: Run on BrowserStack\n        run: |\n          npx wdio wdio.bs.conf.ts --device=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n      \n      - name: Upload results\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: nightly-${{ matrix.device }}-results\n          path: |\n            test-results/\n            playwright-report/\n\n  # ============================================================\n  # JOB 4: RELEASE TIER — full coverage (8 devices)\n  # ============================================================\n  release-tier:\n    needs: setup\n    if: needs.setup.outputs.should_run == 'true' && needs.setup.outputs.matrix-type == 'release'\n    runs-on: ubuntu-latest\n    timeout-minutes: 90\n    strategy:\n      fail-fast: false\n      matrix:\n        device: ['pixel7-android14', 'galaxy-s23-android14', 'redmi-note12-android13', 'galaxy-tab-s8-android13', 'iphone14-ios17', 'iphone12-ios16', 'iphone11-ios15', 'huawei-p30-android10']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '22' }\n      - run: npm ci\n      \n      - name: Start Appium\n        run: docker run -d -p 4723:4723 appium/appium\n      \n      - name: Run full suite\n        run: |\n          npx wdio wdio.bs.conf.ts --device=${{ matrix.device }}\n        env:\n          BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n          BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n          IS_RELEASE: 'true'\n      \n      - name: Upload all artifacts\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: release-${{ matrix.device }}\n          path: |\n            test-results/\n            playwright-report/\n            screenshots/\n\n  # ============================================================\n  # JOB 5: Aggregate results\n  # ============================================================\n  aggregate:\n    needs: [pr-tier, nightly-tier, release-tier]\n    if: always()\n    runs-on: ubuntu-latest\n    steps:\n      - name: Download all artifacts\n        uses: actions/download-artifact@v4\n        with:\n          pattern: '**/results'\n          path: all-results/\n      \n      - name: Generate summary\n        run: node scripts/aggregate-metrics.js\n      \n      - name: Publish GitHub Summary\n        run: |\n          echo \"## 📊 Mobile Test Results\" >> $GITHUB_STEP_SUMMARY\n          cat metrics-summary.md >> $GITHUB_STEP_SUMMARY\n      \n      - name: Publish HTML report\n        uses: actions/upload-artifact@v4\n        with:\n          name: aggregated-report\n          path: |\n            playwright-report/\n            metrics-summary.json\n      \n      - name: Notify on failure\n        if: needs.*.result == 'failure'\n        run: |\n          echo \"::warning::Some tests failed. Check artifacts for details.\"\n      \n      - name: Block release on failure\n        if: needs.release-tier.result == 'failure'\n        run: |\n          echo \"::error::Release tests failed. Check artifacts before proceeding.\"\n          exit 1\n\n  # ============================================================\n  # JOB 6: Final gate (release only)\n  # ============================================================\n  release-gate:\n    needs: [release-tier, aggregate]\n    if: needs.release-tier.result == 'success'\n    runs-on: ubuntu-latest\n    steps:\n      - name: Create release tag\n        run: |\n          git tag v${{ github.run_number }}\n          git push origin v${{ github.run_number }}\n      \n      - name: Publish artifacts\n        uses: actions/upload-artifact@v4\n        with:\n          name: release-artifacts-${{ github.run_number }}\n          path: |\n            all-results/\n            playwright-report/\n```\n\n---\n\n### 2. Artifact collection utility\n\n```typescript\n// scripts/collect-artifacts.ts\n\nimport fs from 'fs';\nimport path from 'path';\n\ninterface ArtifactsConfig {\n  screenshot: boolean;\n  video: boolean;\n  logcat: boolean;\n  trace: boolean;\n  report: boolean;\n}\n\nconst DEFAULT_CONFIG: ArtifactsConfig = {\n  screenshot: true,\n  video: true,\n  logcat: true,\n  trace: true,\n  report: true,\n};\n\nasync function collectArtifacts(\n  testInfo: { title: string; status: string },\n  config: ArtifactsConfig = DEFAULT_CONFIG\n) {\n  const outputDir = `test-results/${testInfo.title}-${Date.now()}`;\n  fs.mkdirSync(outputDir, { recursive: true });\n  \n  if (config.screenshot) {\n    // Playwright screenshot\n    // appium.takeScreenshot()\n  }\n  \n  if (config.logcat) {\n    // adb logcat\n  }\n  \n  if (config.report) {\n    // playwright-report\n  }\n  \n  // Generate metadata\n  const metadata = {\n    testName: testInfo.title,\n    status: testInfo.status,\n    timestamp: new Date().toISOString(),\n    artifacts: Object.keys(config).filter(k => config[k as keyof ArtifactsConfig]),\n  };\n  \n  fs.writeFileSync(\n    path.join(outputDir, 'metadata.json'),\n    JSON.stringify(metadata, null, 2)\n  );\n  \n  return outputDir;\n}\n```\n\n---\n\n### 3. Playwright config z artifact collection\n\n```typescript\n// playwright.config.ts — CI-optimized z artifactami\n\nimport { defineConfig, devices } from '@playwright/test';\n\nconst isCI = process.env['CI'] === 'true';\nconst isPR = process.env['GITHUB_EVENT_NAME'] === 'pull_request';\n\nexport default defineConfig({\n  timeout: 60_000,\n  expect: { timeout: 15_000 },\n  \n  // Retry: więcej na PR (flaky protection), mniej na release\n  retries: isPR ? 2 : 0,\n  \n  // Workers: ogranicz na CI\n  workers: isCI ? 4 : undefined,\n  \n  reporter: [\n    ['html', { outputFolder: 'playwright-report' }],\n    ['junit', { outputFile: 'test-results/results.xml' }],\n    ['github'], // GitHub annotations\n  ],\n  \n  use: {\n    // Artifacts\n    screenshot: 'only-on-failure',\n    video: 'retain-on-failure',\n    trace: isPR ? 'on-first-retry' : 'on-all-retries',\n    \n    // Base\n    baseURL: process.env['BASE_URL'],\n    viewport: { width: 390, height: 844 },\n  },\n  \n  projects: [\n    { name: 'pixel7-android14', use: { ...devices['Pixel 7'] } },\n    { name: 'iphone14-ios17', use: { ...devices['iPhone 14'] } },\n  ],\n  \n  // Global teardown — upload all artifacts\n  globalTeardown: async () => {\n    if (isCI) {\n      // Compress and upload all reports\n      const { execSync } = require('child_process');\n      execSync('tar -czf test-results.tar.gz test-results/ playwright-report/', { stdio: 'inherit' });\n    }\n  },\n});\n```\n\n---\n\n### Checklist pipeline\n\n| Element | Status | Uwagi |\n|---|---|---|\n| **PR tier: emulator** | ✅ | Szybki feedback, fail-fast |\n| **PR tier: 2 real devices** | ✅ | Top devices, BrowserStack |\n| **Nightly tier: 5 devices** | ✅ | Aggregacja wyników |\n| **Release tier: 8 devices** | ✅ | Full coverage, gate check |\n| **Artifact collection** | ✅ | Screenshot, video, logcat, report |\n| **Metrics aggregation** | ✅ | Pass rate, flaky rate |\n| **GitHub Summary** | ✅ | Linki do artifactów |\n| **Failure notification** | ✅ | Slack webhook |\n| **Release gate** | ✅ | Block release on failure |"
      },
      {
        "id": "ex-26-3-6",
        "title": "Diagnostyka awarii",
        "description": "Zbierz kompletny zestaw artefaktów przy awarii na realnym urządzeniu: logcat, device info, network logs, crash dump. Automatyzuj collection.",
        "solution": "## Automatyzacja diagnostyki awarii — kompletny workflow\n\n### 1. On-failure hooks — Appium\n\n```typescript\n// hooks/failure-hooks.ts\n\nimport fs from 'fs';\nimport { execSync } from 'child_process';\n\ninterface FailureContext {\n  testName: string;\n  device: string;\n  osVersion: string;\n  runId: string;\n  error: string;\n  stackTrace: string;\n}\n\nasync function collectAllArtifacts(context: FailureContext) {\n  const outputDir = `test-results/failures/${context.testName}-${context.runId}`;\n  fs.mkdirSync(outputDir, { recursive: true });\n  \n  console.log(`Collecting artifacts to ${outputDir}...`);\n  \n  // 1. Screenshot\n  try {\n    const screenshot = await driver.takeScreenshot();\n    fs.writeFileSync(\n      path.join(outputDir, 'screenshot.png'),\n      screenshot,\n      'base64'\n    );\n    console.log('✓ Screenshot collected');\n  } catch (e) {\n    console.error('✗ Screenshot failed:', e);\n  }\n  \n  // 2. Video\n  try {\n    const video = await driver.stopRecordingScreen();\n    if (video) {\n      fs.writeFileSync(path.join(outputDir, 'video.mp4'), video, 'base64');\n      console.log('✓ Video collected');\n    }\n  } catch (e) {\n    console.error('✗ Video failed:', e);\n  }\n  \n  // 3. Logcat (Android)\n  if (isAndroid) {\n    try {\n      execSync(`adb logcat -d -t 2000 > \"${path.join(outputDir, 'logcat.txt')}\"`, { stdio: 'pipe' });\n      execSync(`adb logcat -d *:E > \"${path.join(outputDir, 'logcat-errors.txt')}\"`, { stdio: 'pipe' });\n      console.log('✓ Logcat collected');\n    } catch (e) {\n      console.error('✗ Logcat failed:', e);\n    }\n  }\n  \n  // 4. Device info\n  try {\n    const deviceInfo = await collectDeviceInfo();\n    fs.writeFileSync(\n      path.join(outputDir, 'device-info.json'),\n      JSON.stringify(deviceInfo, null, 2)\n    );\n    console.log('✓ Device info collected');\n  } catch (e) {\n    console.error('✗ Device info failed:', e);\n  }\n  \n  // 5. Page source (accessibility tree)\n  try {\n    const pageSource = await driver.getPageSource();\n    fs.writeFileSync(path.join(outputDir, 'page-source.xml'), pageSource);\n    console.log('✓ Page source collected');\n  } catch (e) {\n    console.error('✗ Page source failed:', e);\n  }\n  \n  // 6. ANR traces (if exists)\n  if (isAndroid) {\n    try {\n      execSync(`adb pull /data/anr/traces.txt \"${outputDir}/anr-traces.txt\" 2>/dev/null || true`, { stdio: 'pipe' });\n      execSync(`adb pull /data/tombstones/ \"${outputDir}/tombstones/\" 2>/dev/null || true`, { stdio: 'pipe' });\n    } catch (e) {\n      // Ignore\n    }\n  }\n  \n  // 7. Memory dump (if OOM suspected)\n  if (context.error.includes('memory') || context.error.includes('OOM')) {\n    try {\n      execSync(`adb shell dumpsys meminfo com.example.shop > \"${path.join(outputDir, 'memory-dump.txt')}\"`, { stdio: 'pipe' });\n      console.log('✓ Memory dump collected');\n    } catch (e) {\n      console.error('✗ Memory dump failed:', e);\n    }\n  }\n  \n  // 8. Network HAR (if network-related)\n  if (context.error.includes('network') || context.error.includes('timeout')) {\n    await collectNetworkLogs(outputDir);\n    console.log('✓ Network logs collected');\n  }\n  \n  // 9. Generate report\n  const report = generateFailureReport(context, outputDir);\n  fs.writeFileSync(path.join(outputDir, 'failure-report.md'), report);\n  \n  console.log(`\\n✓ All artifacts collected to ${outputDir}`);\n  return outputDir;\n}\n\nasync function collectDeviceInfo(): Promise<object> {\n  if (isAndroid) {\n    return {\n      model: execSync('adb shell getprop ro.product.model').toString().trim(),\n      osVersion: execSync('adb shell getprop ro.build.version.release').toString().trim(),\n      securityPatch: execSync('adb shell getprop ro.build.version.security_patch').toString().trim(),\n      availMem: execSync('adb shell getprop ro.memavailable').toString().trim(),\n      totalMem: execSync('adb shell getprop ro.memtotal').toString().trim(),\n      sdkVersion: execSync('adb shell getprop ro.build.version.sdk').toString().trim(),\n      manufacturer: execSync('adb shell getprop ro.product.manufacturer').toString().trim(),\n    };\n  } else {\n    return {\n      // iOS equivalents via xcrun simctl\n    };\n  }\n}\n\nfunction generateFailureReport(context: FailureContext, dir: string): string {\n  return `# Failure Report: ${context.testName}\n\n## Environment\n- Device: ${context.device}\n- OS: ${context.osVersion}\n- Run ID: ${context.runId}\n\n## Error\n\\`\\`\\`\n${context.error}\n${context.stackTrace}\n\\`\\`\\`\n\n## Artifacts\n- screenshot.png\n- video.mp4\n- logcat.txt\n- device-info.json\n- page-source.xml\n\n## Reproduction Steps\n1. [Add steps based on test context]\n\n## Analysis\n[Fill in after review]\n`;\n}\n```\n\n---\n\n### 2. Appium hooks integration\n\n```typescript\n// wdio.hooks.ts — WebdriverIO hooks\n\nimport { Before, After, AfterStep } from '@wdio/mocha-framework';\nimport { collectAllArtifacts } from './hooks/failure-hooks';\n\nlet failureCount = 0;\nconst MAX_FAILURES_PER_RUN = 5; // Stop after 5 failures to save cost\n\nBefore(async () => {\n  // Clean slate for each test\n  console.log(`Starting test: ${this.test?.title}`);\n});\n\nAfter(async (test, result) => {\n  if (result.failed) {\n    failureCount++;\n    \n    if (failureCount > MAX_FAILURES_PER_RUN) {\n      console.error(`⚠️ Too many failures (${failureCount}). Stopping test run to save cost.`);\n      process.exit(1);\n    }\n    \n    const context = {\n      testName: test.title,\n      device: browser.capabilities?.deviceName || 'unknown',\n      osVersion: browser.capabilities?.platformVersion || 'unknown',\n      runId: process.env['GITHUB_RUN_ID'] || Date.now().toString(),\n      error: result.error?.message || 'Unknown error',\n      stackTrace: result.error?.stack || '',\n    };\n    \n    await collectAllArtifacts(context);\n  }\n});\n\n// Collect artifacts after each step (for debugging)\nAfterStep(async (step, result) => {\n  if (result.failed && step.error) {\n    // Only collect on step failure (faster than full test failure)\n    const context = {\n      testName: `${this.test?.title} > ${step.name}`,\n      device: browser.capabilities?.deviceName || 'unknown',\n      osVersion: browser.capabilities?.platformVersion || 'unknown',\n      runId: process.env['GITHUB_RUN_ID'] || Date.now().toString(),\n      error: step.error?.message || 'Unknown',\n      stackTrace: '',\n    };\n    \n    // Lightweight artifact collection (screenshot + page source only)\n    await collectAllArtifacts(context, { screenshot: true, pageSource: true, logcat: false });\n  }\n});\n```\n\n---\n\n### 3. GitHub Actions — automatic artifact upload\n\n```yaml\n# Fragment workflow\n\n- name: Run tests\n  run: npx wdio wdio.bs.conf.ts\n  env:\n    BROWSERSTACK_USER: ${{ secrets.BS_USER }}\n    BROWSERSTACK_KEY: ${{ secrets.BS_KEY }}\n  continue-on-error: true  # Continue to collect all artifacts\n\n- name: Upload failure artifacts\n  if: always()\n  uses: actions/upload-artifact@v4\n  with:\n    name: failure-artifacts-${{ matrix.device }}\n    path: |\n      test-results/failures/\n      playwright-report/\n    retention-days: 30\n\n- name: Upload logcat\n  if: always()\n  run: |\n    # Collect from all running containers\n    docker logs appium > test-results/appium-logs.txt 2>&1 || true\n\n- name: Generate failure summary\n  if: failure()\n  run: |\n    echo \"## ❌ Test Failures\" >> $GITHUB_STEP_SUMMARY\n    echo \"\" >> $GITHUB_STEP_SUMMARY\n    for dir in test-results/failures/*/; do\n      test_name=$(basename \"$dir\")\n      echo \"### $test_name\" >> $GITHUB_STEP_SUMMARY\n      cat \"$dir/failure-report.md\" >> $GITHUB_STEP_SUMMARY\n      echo \"\" >> $GITHUB_STEP_SUMMARY\n    done\n```\n\n---\n\n### 4. Remote debugging — BrowserStack local\n\n```typescript\n// Debug integration with BrowserStack\n\n// Enable BrowserStack Local for debugging\nconst browserstackLocal = new Local();\n\nbefore(async () => {\n  await browserstackLocal.start({\n    key: process.env['BROWSERSTACK_KEY'],\n    force: true,\n    // Tunnel options for accessing local servers\n  });\n  \n  console.log('BrowserStack Local started');\n});\n\nafter(async () => {\n  await browserstackLocal.stop();\n});\n\n// In test: enable verbose logging\ntest('debug session', async () => {\n  // Enable verbose Appium logging\n  await driver.execute('mobile: startLogsBroadcast');\n  \n  // Your test steps...\n  \n  // Disable verbose logging\n  await driver.execute('mobile: stopLogsBroadcast');\n});\n```\n\n---\n\n### 5. Checklist diagnostyki\n\n| Krok | Artefakt | Narzędzie | Kiedy |\n|---|---|---|---|\n| **1** | Screenshot | Playwright / Appium | Zawsze |\n| **2** | Video | Appium recording | Zawsze |\n| **3** | Logcat (last 2000 lines) | `adb logcat -d -t 2000` | Android only |\n| **4** | Device info | `adb getprop` / CDP | Zawsze |\n| **5** | Page source | `driver.getPageSource()` | Zawsze |\n| **6** | ANR traces | `adb pull /data/anr/` | Jeśli ANR suspected |\n| **7** | Memory dump | `adb dumpsys meminfo` | Jeśli OOM suspected |\n| **8** | Network HAR | CDP session | Jeśli network issue |\n| **9** | Crash dump | `adb pull /data/tombstones/` | Jeśli crash |\n| **10** | Failure report (markdown) | Custom generator | Zawsze |"
      }
    ],
    "quiz": [
      {
        "id": "q26-3-1",
        "question": "Czym różni się mobile web od aplikacji natywne?",
        "options": [
          "Mobile web działa w przeglądarce, natywne korzysta z API systemu i jest instalowana jako aplikacja",
          "Niczym",
          "Native zawsze jest stroną HTML",
          "Mobile web nie wymaga sieci"
        ],
        "correctAnswer": 0,
        "explanation": "Typ aplikacji decyduje o narzędziach, ryzykach i sposobie automatyzacji."
      },
      {
        "id": "q26-3-2",
        "question": "Dlaczego realne urządzenia są ważne?",
        "options": [
          "Ujawniają problemy sprzętowe, systemowe, sieciowe i wydajnościowe niewidoczne w emulatorze",
          "Bo emulatory nigdy nie działają",
          "Bo zastępują wszystkie testy",
          "Tylko do screenshotów"
        ],
        "correctAnswer": 0,
        "explanation": "Emulator nie oddaje w pełni zachowania prawdziwego urządzenia."
      },
      {
        "id": "q26-3-3",
        "question": "Co jest kluczowe w testach uprawnień?",
        "options": [
          "Sprawdzenie przyznania, odmowy i zmiany uprawnień",
          "Tylko happy path",
          "Ukrycie promptów systemowych",
          "Brak resetu aplikacji"
        ],
        "correctAnswer": 0,
        "explanation": "Uprawnienia są częścią przepływu użytkownika i źródłem wielu błędów."
      },
      {
        "id": "q26-3-4",
        "question": "Po co testować deep links?",
        "options": [
          "Aby sprawdzić wejście do aplikacji z zewnętrznego kontekstu",
          "Aby zmienić CSS",
          "Aby ominąć routing",
          "Wyłącznie dla desktopu"
        ],
        "correctAnswer": 0,
        "explanation": "Deep linki są częstym sposobem wejścia z e-maila, push notification lub kampanii."
      },
      {
        "id": "q26-3-5",
        "question": "Co powinien zawierać raport awarii mobilnej?",
        "options": [
          "Model urządzenia, OS, wersję aplikacji, logi, screenshot/wideo i kroki reprodukcji",
          "Tylko nazwę testu",
          "Sam kolor ekranu",
          "Bez danych środowiska"
        ],
        "correctAnswer": 0,
        "explanation": "Bez informacji o urządzeniu i systemie awaria mobilna bywa niemożliwa do odtworzenia."
      },
      {
        "id": "q26-3-6",
        "question": "Kiedy device farm jest szczególnie przydatna?",
        "options": [
          "Gdy potrzebujemy pokryć wiele realnych urządzeń bez ich lokalnego utrzymywania",
          "Do testowania wyłącznie unit",
          "Do zastąpienia strategii",
          "Nigdy"
        ],
        "correctAnswer": 0,
        "explanation": "Device farm daje dostęp do macierzy urządzeń i systemów."
      },
      {
        "id": "q26-3-7",
        "question": "Co odróżnia dobry test tryb offline?",
        "options": [
          "Jawnie przełącza sieć, sprawdza komunikat i synchronizację po powrocie online",
          "Dodaje sleep",
          "Ignoruje dane lokalne",
          "Działa tylko na desktopie"
        ],
        "correctAnswer": 0,
        "explanation": "Offline mode wymaga kontroli sieci i stanu lokalnego."
      },
      {
        "id": "q26-3-8",
        "question": "Najważniejsza zasada lekcji „Emulatory, prawdziwe urządzenia i farmy urządzeń” to:",
        "options": [
          "Testy mobilne muszą kontrolować urządzenie, system i stan aplikacji",
          "Wystarczy jeden emulator",
          "Uprawnień nie testujemy",
          "Sieć zawsze jest stabilna"
        ],
        "correctAnswer": 0,
        "explanation": "Mobilność oznacza zmienne urządzenia, warunki i integracje z systemem."
      }
    ],
    "references": [
      {
        "title": "Appium Docs",
        "url": "https://appium.io/docs/en/latest/",
        "description": "Appium i urządzenia."
      },
      {
        "title": "BrowserStack App Automate",
        "url": "https://www.browserstack.com/docs/app-automate",
        "description": "Farma urządzeń i artefakty."
      },
      {
        "title": "Android Testing",
        "url": "https://developer.android.com/training/testing",
        "description": "Dokumentacja testowania Android."
      }
    ],
    "tipsAndTricks": [
      "Macierz urządzeń powinna wynikać z danych użytkowników i ryzyka, nie z listy wszystkich możliwych modeli.",
      "Emulator jest świetny do szybkiej informacji zwrotnej, ale realne urządzenie ujawnia problemy sprzętowe, sieciowe i systemowe.",
      "W testach mobilnych szczególnie dbaj o stan aplikacji: uprawnienia, pamięć, sieć, sesję i dane lokalne.",
      "Każdy test deep linku, tryb offline lub push notification powinien mieć jawny setup i cleanup stanu urządzenia."
    ],
    "commonMistakes": [
      {
        "mistake": "Testowanie wyłącznie na jednym emulatorze",
        "solution": "Zbuduj małą, ryzykową macierz urządzeń obejmującą realne modele, OS i rozdzielczości."
      },
      {
        "mistake": "Ignorowanie uprawnień systemowych",
        "solution": "Testuj odmowę, przyznanie i zmianę uprawnień dla kamery, lokalizacji, powiadomień i plików."
      },
      {
        "mistake": "Brak kontroli stanu aplikacji",
        "solution": "Czyść dane aplikacji, resetuj sesję i jawnie ustawiaj warunki początkowe."
      },
      {
        "mistake": "Mylenie mobile web z natywne",
        "solution": "Dobierz narzędzia i strategię do typu aplikacji: RWD/PWA, hybrydowe albo natywne."
      }
    ]
  }
};