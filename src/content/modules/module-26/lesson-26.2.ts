import type { Lesson } from "../../../renderer/types";
import theory26_2 from './lesson-26.2.md?raw';

export const lesson26_2: Lesson = {
  "id": "26.2",
  "moduleId": 26,
  "title": "Podstawy Appium",
  "description": "Architektura Appium, capabilities, lokatory, gesty, oczekiwania, stabilność testów aplikacji natywnych i hybrydowych.",
  "order": 2,
  "difficulty": "advanced",
  "tags": [
    "appium",
    "natywne-mobile",
    "capabilities",
    "lokatory",
    "gestures",
    "oczekiwania"
  ],
  "content": {
    "objective": "Po ukończeniu lekcji rozumiesz architekturę Appium, potrafisz zaprojektować capabilities, stabilne lokatory i podstawowe scenariusze automatyzacji aplikacji mobilnej.",
    "theory": theory26_2,
    "codeExamples": [
      "// Przykład capabilities — koncepcyjnie.\nconst capabilities = {\n  platformName: 'Android',\n  'appium:automationName': 'UiAutomator2',\n  'appium:deviceName': 'Pixel_7_API_34',\n  'appium:app': './apps/app-debug.apk',\n  'appium:autoGrantPermissions': false,\n};\n",
      "// Preferuj accessibility id / test id zamiast kruchego XPath.\nawait driver.$('~login-email').setValue('qa@example.test');\nawait driver.$('~login-submit').click();\n"
    ],
    "exercises": [
      {
        "id": "ex-26-2-1",
        "title": "Konfiguracja capabilities",
        "description": "Skonfiguruj capabilities dla Appium: Android (UiAutomator2) z aplikacją APK, iOS (XCUITest) z aplikacją IPA. Obsłuż oba scenariusze: install from file i use already installed.",
        "solution": "## Konfiguracja capabilities — Android i iOS\n\n### Wariant A: Pełna konfiguracja Android (install APK)\n\n```typescript\n// appium-android.config.ts\n\nconst ANDROID_CAPS_FULL = {\n  // === OBLIGATORY ===\n  platformName: 'Android',\n  'appium:automationName': 'UiAutomator2', // lub 'Espresso'\n  'appium:deviceName': 'Pixel_7_API_34', // nazwa AVD lub UDID real device\n  \n  // === APLIKACJA ===\n  'appium:app': '/Users/user/projects/shop-app/android/app/build/outputs/apk/debug/app-debug.apk',\n  'appium:appPackage': 'com.example.shop',\n  'appium:appActivity': '.MainActivity',\n  'appium:appWaitActivity': 'com.example.shop.*', // lub konkretna aktywność startowa\n  \n  // === INSTALACJA ===\n  'appium:autoGrantPermissions': false, // NIE auto-accept uprawnień\n  'appium:noReset': false, // Czyść dane przed sesją\n  'appium:fullReset': false, // Pełny reset (odinstaluj po)\n  \n  // === TIMEOUT ===\n  'appium:newCommandTimeout': 30_000, // ms bez odpowiedzi\n  'appium:androidScreenshotPath': '/sdcard/screenshots',\n  \n  // === EMULATOR vs REAL DEVICE ===\n  // Jeśli emulator:\n  'appium:avd': 'Pixel_7_API_34', // auto-start emulator\n  'appium:avdArgs': ['-no-window', '-no-audio', '-gpu', 'swiftshader'],\n  // Jeśli real device:\n  // 'appium:udid': 'RF8N123456AB', // `adb devices` serial number\n};\n\n// Session creation\nconst driver = await remote({\n  hostname: process.env['APPIUM_HOST'] || 'localhost',\n  port: parseInt(process.env['APPIUM_PORT'] || '4723', 10),\n  path: '/wd/hub', // Appium hub path\n  capabilities: ANDROID_CAPS_FULL,\n});\n\nconsole.log('Session created:', driver.sessionId);\n```\n\n---\n\n### Wariant B: Android z already installed app (no install)\n\n```typescript\n// Gdy app jest już na urządzeniu — podaj tylko package + activity\n\nconst ANDROID_CAPS_INSTALLED = {\n  platformName: 'Android',\n  'appium:automationName': 'UiAutomator2',\n  'appium:deviceName': 'Pixel_7_API_34',\n  \n  // NIE podawaj 'appium:app' — Appium użyje zainstalowanej app\n  'appium:appPackage': 'com.example.shop',\n  'appium:appActivity': '.MainActivity',\n  \n  // NIE Instaluj — użyj istniejącej\n  'appium:noReset': false, // Czyść dane app (ale nie odinstalowuj)\n  'appium:skipInstall': true, // Pomiń instalację\n};\n```\n\n---\n\n### Wariant C: Android real device (USB)\n\n```typescript\n// Real device z USB debugging\n\nconst ANDROID_REAL_DEVICE_CAPS = {\n  platformName: 'Android',\n  'appium:automationName': 'UiAutomator2',\n  \n  // UDID — znajdziesz przez `adb devices`\n  'appium:udid': 'RF8N123456AB', // Samsung Galaxy A54\n  \n  // Wersja OS na tym urządzeniu\n  'appium:platformVersion': '13',\n  \n  // App\n  'appium:app': '/path/to/app.apk',\n  \n  // Permissions\n  'appium:autoGrantPermissions': false, // Testuj odmowę!\n  'appium:autoAcceptAlerts': false,\n  \n  // Timeouty\n  'appium:newCommandTimeout': 60_000, // Real device bywa wolniejszy\n  \n  // Reset\n  'appium:noReset': false,\n  'appium:fullReset': false,\n};\n\n// Wymagania:\n// 1. USB debugging włączony na urządzeniu\n// 2. `adb devices` pokazuje urządzenie jako 'device' (nie 'unauthorized')\n// 3. Port forwarding: `adb forward tcp:4723 tcp:4723`\n```\n\n---\n\n### Wariant D: iOS Simulator\n\n```typescript\n// iOS Simulator — auto-find available simulator\n\nconst IOS_SIMULATOR_CAPS = {\n  platformName: 'iOS',\n  'appium:automationName': 'XCUITest',\n  \n  // Device name = nazwa z `xcrun simctl list devices available`\n  'appium:deviceName': 'iPhone 14',\n  'appium:platformVersion': '17',\n  \n  // App z build output\n  'appium:app': '/Users/user/Dev/shop-app/build/Build/Products/Debug-iphonesimulator/shop.app',\n  \n  // Simulator specific\n  'appium:simulatorStartupTimeout': 60_000,\n  \n  // Permissions\n  'appium:autoAcceptAlerts': true, // Simulator akceptuje automatycznie\n  \n  // Reset\n  'appium:noReset': false, // Czyść dane między testami\n};\n\n// Uruchom Appium z UDID 'auto'\nconst IOS_AUTO_UDID_CAPS = {\n  ...IOS_SIMULATOR_CAPS,\n  'appium:udid': 'auto', // Appium znajdzie iPhone 14 simulator\n  'appium:showXcodeLog': true, // Pokaż Xcode logs\n};\n```\n\n---\n\n### Wariant E: iOS Real Device\n\n```typescript\n// iOS Real Device — wymaga signed app (IPA) i provisioning profile\n\nconst IOS_REAL_DEVICE_CAPS = {\n  platformName: 'iOS',\n  'appium:automationName': 'XCUITest',\n  \n  // UDID konkretnego urządzenia\n  // Znajdziesz przez: Xcode > Devices > Identifier\n  // Lub: `idevice_id -l`\n  'appium:udid': '00001234-0000123456789012', // iPhone 14 Pro\n  \n  // Bundle ID\n  'appium:bundleId': 'com.example.shop',\n  \n  // App — musi być podpisana z prawidłowym provisioning profile\n  'appium:app': '/Users/user/Dev/shop-app.ipa',\n  \n  // Auto-accept (Safari alerts, permission dialogs)\n  'appium:autoAcceptAlerts': true,\n  \n  // Real device logging\n  'appium:realDeviceLogger': '/usr/local/bin/deviceconsole',\n  \n  // Timeout\n  'appium:newCommandTimeout': 60_000,\n  \n  // Platform version\n  'appium:platformVersion': '17',\n  \n  // Wipe na końcu — nie odinstalowuj\n  'appium:noReset': false,\n};\n\n// Wymagania:\n// 1. App podpisana provisioning profile (nie developer preview)\n// 2. Provisioning profile zawiera UDID urządzenia\n// 3. Xcode tools zainstalowane\n```\n\n---\n\n### Factory function — tworzenie capabilities z konfiguracji\n\n```typescript\n// appium-capabilities.ts — factory pattern\n\ntype Platform = 'android' | 'ios';\ntype DeviceType = 'emulator' | 'simulator' | 'real';\n\ntype DeviceConfig = {\n  deviceName: string;\n  platformVersion: string;\n  appPath: string;\n  deviceType: DeviceType;\n  platform: Platform;\n};\n\ntype CapabilityConfig = Partial<{\n  skipInstall: boolean;\n  autoGrantPermissions: boolean;\n  noReset: boolean;\n  newCommandTimeout: number;\n  appPackage?: string;\n  appActivity?: string;\n  bundleId?: string;\n}>;\n\nexport function createCapabilities(config: DeviceConfig, options: CapabilityConfig = {}) {\n  const base = {\n    platformName: config.platform === 'android' ? 'Android' : 'iOS',\n    'appium:deviceName': config.deviceName,\n    'appium:platformVersion': config.platformVersion,\n    'appium:app': config.appPath,\n    'appium:autoGrantPermissions': options.autoGrantPermissions ?? false,\n    'appium:noReset': options.noReset ?? false,\n    'appium:newCommandTimeout': options.newCommandTimeout ?? 30_000,\n  };\n\n  if (config.platform === 'android') {\n    return {\n      ...base,\n      'appium:automationName': 'UiAutomator2',\n      'appium:appPackage': options.appPackage ?? 'com.example.shop',\n      'appium:appActivity': options.appActivity ?? '.MainActivity',\n      'appium:skipInstall': options.skipInstall ?? false,\n    };\n  }\n\n  if (config.platform === 'ios') {\n    return {\n      ...base,\n      'appium:automationName': 'XCUITest',\n      'appium:bundleId': options.bundleId ?? 'com.example.shop',\n      'appium:autoAcceptAlerts': true,\n    };\n  }\n\n  throw new Error(`Unsupported platform: ${config.platform}`);\n}\n\n// Usage\nconst androidCaps = createCapabilities({\n  deviceName: 'Pixel_7_API_34',\n  platformVersion: '14',\n  appPath: '/path/to/app.apk',\n  deviceType: 'emulator',\n  platform: 'android',\n}, { noReset: true, autoGrantPermissions: false });\n\nconst iosCaps = createCapabilities({\n  deviceName: 'iPhone 14',\n  platformVersion: '17',\n  appPath: '/path/to/app.app',\n  deviceType: 'simulator',\n  platform: 'ios',\n}, { noReset: false });\n```\n\n---\n\n### Walidacja capabilities przed sesją\n\n```typescript\n// validate-caps.ts\n\nfunction validateAndroidCapabilities(caps: Record<string, unknown>): string[] {\n  const errors: string[] = [];\n  \n  // Required\n  if (!caps['platformName']) errors.push('Missing: platformName');\n  if (!caps['appium:automationName']) errors.push('Missing: appium:automationName');\n  if (!caps['appium:deviceName']) errors.push('Missing: appium:deviceName');\n  \n  // App identification — jedno z drugim\n  const hasApp = caps['appium:app'];\n  const hasPackageActivity = caps['appium:appPackage'] && caps['appium:appActivity'];\n  if (!hasApp && !hasPackageActivity) {\n    errors.push('Missing: provide appium:app OR (appium:appPackage + appium:appActivity)');\n  }\n  \n  // Valid automation\n  const validAutomations = ['UiAutomator2', 'Espresso'];\n  if (!validAutomations.includes(caps['appium:automationName'] as string)) {\n    errors.push(`Invalid appium:automationName. Must be one of: ${validAutomations.join(', ')}`);\n  }\n  \n  return errors;\n}\n\n// Test przed uruchomieniem\nconst errors = validateAndroidCapabilities(androidCaps);\nif (errors.length > 0) {\n  console.error('❌ Capability errors:', errors);\n  throw new Error(`Invalid capabilities: ${errors.join(', ')}`);\n}\nconsole.log('✅ Capabilities validated');\n```\n\n---\n\n### Checklist konfiguracji\n\n| Element | Android | iOS | Sprawdź |\n|---|---|---|---|\n| platformName | ✓ | ✓ | 'Android' / 'iOS' |\n| automationName | UiAutomator2 / Espresso | XCUITest | Poprawny driver |\n| deviceName | AVD name / UDID | Simulator name / UDID | `adb devices` / `xcrun simctl list` |\n| app | Ścieżka .apk | Ścieżka .app/.ipa | Plik istnieje |\n| appPackage + activity | Tylko jeśli bez app | — | Dla already installed |\n| bundleId | — | Bundle ID | Dla already installed |\n| autoGrantPermissions | false | autoAcceptAlerts | Testuj odmowę! |\n| noReset | false | false | Czyste dane |\n| newCommandTimeout | 30s | 30s | Dłuższy dla real device |"
      },
      {
        "id": "ex-26-2-2",
        "title": "Lokatory stabilne",
        "description": "Zidentyfikuj najstabilniejsze lokatory dla aplikacji natywnej. Napisz Page Object z lokatorami accessibility ID, resource-id i UiSelector dla Android.",
        "solution": "## Page Object z stabilnymi lokatorami — Android\n\n### Struktura projektu\n\n```\nsrc/\n├── pages/\n│   ├── BasePage.ts          # Klasa bazowa z driver\n│   ├── LoginPage.ts         # Login screen\n│   ├── ProductListPage.ts   # Lista produktów\n│   └── CheckoutPage.ts      # Checkout\n├── utils/\n│   └── locator-helpers.ts   # Reużywalne strategie\n```\n\n---\n\n### BasePage.ts — klasa bazowa\n\n```typescript\n// pages/BasePage.ts\n\nimport { WebdriverIO } from 'webdriverio';\n\nexport abstract class BasePage {\n  protected driver: WebdriverIO.Browser;\n\n  constructor(driver: WebdriverIO.Browser) {\n    this.driver = driver;\n  }\n\n  // === WSPÓŁNE METODY ===\n\n  protected async waitForVisible(selector: string, timeout = 10_000) {\n    await this.driver.waitUntil(\n      async () => {\n        const el = await this.driver.$(selector);\n        return el && await el.isDisplayed();\n      },\n      { timeout, timeoutMsg: `Element ${selector} not visible in ${timeout}ms` }\n    );\n  }\n\n  protected async waitForClickable(selector: string, timeout = 10_000) {\n    await this.driver.waitUntil(\n      async () => {\n        const el = await this.driver.$(selector);\n        if (!el) return false;\n        const displayed = await el.isDisplayed();\n        const enabled = await el.isEnabled();\n        return displayed && enabled;\n      },\n      { timeout, timeoutMsg: `Element ${selector} not clickable in ${timeout}ms` }\n    );\n  }\n\n  protected async waitForNotVisible(selector: string, timeout = 10_000) {\n    await this.driver.waitUntil(\n      async () => {\n        const el = await this.driver.$(selector);\n        return !(el && await el.isDisplayed());\n      },\n      { timeout, timeoutMsg: `Element ${selector} still visible after ${timeout}ms` }\n    );\n  }\n\n  protected async screenshotOnFailure(testName: string) {\n    const screenshot = await this.driver.takeScreenshot();\n    const fs = await import('fs');\n    fs.writeFileSync(`test-results/failure-${testName}-${Date.now()}.png`, screenshot, 'base64');\n  }\n\n  async getPageSource(): Promise<string> {\n    return this.driver.getPageSource();\n  }\n}\n```\n\n---\n\n### LoginPage.ts — pełny przykład\n\n```typescript\n// pages/LoginPage.ts\n\nimport { WebdriverIO } from 'webdriverio';\nimport { BasePage } from './BasePage';\n\nexport class LoginPage extends BasePage {\n  constructor(driver: WebdriverIO.Browser) {\n    super(driver);\n  }\n\n  // === ACCESSIBILITY ID (NAJLEPSZY — priorytet 1) ===\n  // Wymaga: android:accessibilityLabel=\"login-email\" w XML\n  get emailInput() {\n    return this.driver.$('~login-email');\n  }\n\n  get passwordInput() {\n    return this.driver.$('~login-password');\n  }\n\n  get submitButton() {\n    return this.driver.$('~login-submit');\n  }\n\n  get forgotPasswordLink() {\n    return this.driver.$('~forgot-password-link');\n  }\n\n  get errorMessage() {\n    return this.driver.$('~login-error-message');\n  }\n\n  // === RESOURCE ID (DOBRY — priorytet 2) ===\n  // Wymaga: android:id=\"@+id/email_field\" w XML\n  get emailInputById() {\n    return this.driver.$('com.example.shop:id/email_field');\n  }\n\n  get passwordInputById() {\n    return this.driver.$('com.example.shop:id/password_field');\n  }\n\n  get submitButtonById() {\n    return this.driver.$('com.example.shop:id/login_button');\n  }\n\n  // === UI SELECTOR — dla dynamicznych elementów (priorytet 3) ===\n  // Używaj tylko gdy accessibility ID i resource-id nie są dostępne\n  get emailInputByText() {\n    return this.driver.$(\n      'android=new UiSelector().text(\"Adres e-mail\").className(\"android.widget.EditText\")'\n    );\n  }\n\n  get submitButtonByText() {\n    return this.driver.$(\n      'android=new UiSelector().textContains(\"Zaloguj\").className(\"android.widget.Button\")'\n    );\n  }\n\n  get errorMessageByText() {\n    return this.driver.$(\n      'android=new UiSelector().textContains(\"Nieprawidłowy\")'\n    );\n  }\n\n  // === GŁÓWNE LOKATORY (preferowane) ===\n  // Zwraca accessibility ID lub resource-id, z fallback\n  private primaryEmailInput() {\n    return this.driver.$('~login-email'); // Accessibility ID (preferowany)\n  }\n\n  // === METODY INTERAKCJI ===\n\n  async goto(): Promise<void> {\n    // Otwórz ekran logowania\n    await this.driver.execute('mobile: deepLink', {\n      url: 'myapp://login',\n      package: 'com.example.shop',\n    });\n    await this.waitForVisible('~login-email', 15_000);\n  }\n\n  async login(email: string, password: string): Promise<void> {\n    // Użyj accessibility ID (najstabilniejszy)\n    const emailEl = await this.primaryEmailInput();\n    const passwordEl = await this.passwordInput;\n    const submitEl = await this.submitButton;\n\n    await emailEl.waitFor({ state: 'visible', timeout: 10_000 });\n    await emailEl.setValue(email);\n    await passwordEl.setValue(password);\n    await submitEl.click();\n  }\n\n  async expectErrorVisible(expectedMessage?: string): Promise<void> {\n    const errorEl = await this.errorMessage;\n    await errorEl.waitFor({ state: 'visible', timeout: 5_000 });\n    \n    if (expectedMessage) {\n      const text = await errorEl.getText();\n      if (!text.includes(expectedMessage)) {\n        throw new Error(`Expected error \"${expectedMessage}\", got \"${text}\"`);\n      }\n    }\n  }\n\n  async expectNoError(): Promise<void> {\n    const errorEl = await this.errorMessage;\n    const isVisible = await errorEl.isDisplayed().catch(() => false);\n    expect(isVisible).toBe(false);\n  }\n\n  async clickForgotPassword(): Promise<void> {\n    await this.waitForClickable('~forgot-password-link');\n    await this.forgotPasswordLink.click();\n  }\n\n  async isOnLoginScreen(): Promise<boolean> {\n    const emailEl = await this.emailInput;\n    return emailEl.isDisplayed().catch(() => false);\n  }\n}\n```\n\n---\n\n### ProductListPage.ts\n\n```typescript\n// pages/ProductListPage.ts\n\nimport { WebdriverIO } from 'webdriverio';\nimport { BasePage } from './BasePage';\n\nexport class ProductListPage extends BasePage {\n  constructor(driver: WebdriverIO.Browser) {\n    super(driver);\n  }\n\n  // === ACCESSIBILITY IDs ===\n  get productList() {\n    return this.driver.$('~product-list');\n  }\n\n  get searchInput() {\n    return this.driver.$('~search-input');\n  }\n\n  get filterButton() {\n    return this.driver.$('~filter-button');\n  }\n\n  getSortButton() {\n    return (sortOption: string) => this.driver.$(`~sort-${sortOption}`);\n  }\n\n  getProductCard(productId: string) {\n    return this.driver.$(`~product-card-${productId}`);\n  }\n\n  getAddToCartButton(productId: string) {\n    return this.driver.$(`~add-to-cart-${productId}`);\n  }\n\n  getProductPrice(productId: string) {\n    return this.driver.$(`~product-price-${productId}`);\n  }\n\n  // === UI SELECTOR dla dynamicznych list ===\n  get firstVisibleProduct() {\n    return this.driver.$('android=new UiSelector().className(\"android.widget.CardView\").instance(0)');\n  }\n\n  get allProductCards() {\n    // Znajdź wszystkie karty produktów\n    return this.driver.$$('android=new UiSelector().className(\"android.widget.CardView\")');\n  }\n\n  // === METODY ===\n\n  async search(query: string): Promise<void> {\n    await this.searchInput.setValue(query);\n    await this.driver.hideKeyboard(); // Zamknij klawiaturę\n  }\n\n  async addToCart(productId: string): Promise<void> {\n    const addButton = this.getAddToCartButton(productId);\n    await addButton.waitFor({ state: 'visible', timeout: 5_000 });\n    await addButton.click();\n    \n    // Poczekaj na feedback\n    const addedFeedback = this.driver.$('~added-to-cart-feedback');\n    await addedFeedback.waitFor({ state: 'visible', timeout: 3_000 });\n  }\n\n  async getProductCount(): Promise<number> {\n    const cards = await this.allProductCards;\n    return cards.length;\n  }\n\n  async scrollToProduct(productId: string): Promise<void> {\n    const product = this.getProductCard(productId);\n    \n    // ScrollIntoView jeśli dostępne\n    await this.driver.execute('mobile: scroll', {\n      element: product,\n      strategy: 'android.view.View',\n      selector: `@content-desc=\"product-card-${productId}\"`,\n    });\n  }\n\n  async sortByPriceAscending(): Promise<void> {\n    await this.filterButton.click();\n    const sortOption = this.getSortButton('price-asc');\n    await sortOption.click();\n    await this.waitForNotVisible('~sort-dialog');\n  }\n\n  async expectProductVisible(productId: string): Promise<void> {\n    const product = this.getProductCard(productId);\n    await product.waitFor({ state: 'visible', timeout: 5_000 });\n  }\n\n  async expectEmptyState(): Promise<void> {\n    const emptyState = this.driver.$('~empty-products-message');\n    await emptyState.waitFor({ state: 'visible', timeout: 3_000 });\n  }\n}\n```\n\n---\n\n### CheckoutPage.ts\n\n```typescript\n// pages/CheckoutPage.ts\n\nimport { WebdriverIO } from 'webdriverio';\nimport { BasePage } from './BasePage';\n\nexport class CheckoutPage extends BasePage {\n  constructor(driver: WebdriverIO.Browser) {\n    super(driver);\n  }\n\n  // === FORM FIELDS (accessibility IDs) ===\n  get nameInput() {\n    return this.driver.$('~checkout-name');\n  }\n\n  get streetInput() {\n    return this.driver.$('~checkout-street');\n  }\n\n  get cityInput() {\n    return this.driver.$('~checkout-city');\n  }\n\n  get postalCodeInput() {\n    return this.driver.$('~checkout-postal-code');\n  }\n\n  get cardNumberInput() {\n    return this.driver.$('~checkout-card-number');\n  }\n\n  get cvcInput() {\n    return this.driver.$('~checkout-cvc');\n  }\n\n  get expiryInput() {\n    return this.driver.$('~checkout-expiry');\n  }\n\n  get payButton() {\n    return this.driver.$('~checkout-pay-button');\n  }\n\n  get orderSummary() {\n    return this.driver.$('~order-summary');\n  }\n\n  get totalAmount() {\n    return this.driver.$('~checkout-total');\n  }\n\n  get backButton() {\n    return this.driver.$('~checkout-back');\n  }\n\n  // === BŁĘDY WALIDACJI ===\n  getCardError() {\n    return this.driver.$('~card-number-error');\n  }\n\n  getCVVError() {\n    return this.driver.$('~cvc-error');\n  }\n\n  getPostalCodeError() {\n    return this.driver.$('~postal-code-error');\n  }\n\n  // === METODY ===\n\n  async fillDeliveryAddress(data: {\n    name: string;\n    street: string;\n    city: string;\n    postalCode: string;\n  }): Promise<void> {\n    await this.nameInput.setValue(data.name);\n    await this.streetInput.setValue(data.street);\n    await this.cityInput.setValue(data.city);\n    await this.postalCodeInput.setValue(data.postalCode);\n  }\n\n  async fillPaymentData(data: {\n    cardNumber: string;\n    expiry: string;\n    cvc: string;\n  }): Promise<void> {\n    await this.cardNumberInput.setValue(data.cardNumber);\n    await this.expiryInput.setValue(data.expiry);\n    await this.cvcInput.setValue(data.cvc);\n  }\n\n  async submitPayment(): Promise<void> {\n    const payBtn = await this.payButton;\n    await payBtn.waitFor({ state: 'visible', timeout: 5_000 });\n    await payBtn.click();\n  }\n\n  async expectValidationError(field: 'card' | 'cvv' | 'postalCode'): Promise<void> {\n    const errorMap = {\n      card: this.getCardError(),\n      cvv: this.getCVVError(),\n      postalCode: this.getPostalCodeError(),\n    };\n    const errorEl = errorMap[field];\n    await errorEl.waitFor({ state: 'visible', timeout: 3_000 });\n  }\n\n  async getTotalAmount(): Promise<string> {\n    const totalEl = await this.totalAmount;\n    return totalEl.getText();\n  }\n\n  async goBack(): Promise<void> {\n    await this.backButton.click();\n  }\n\n  async isLoading(): Promise<boolean> {\n    const loader = this.driver.$('~checkout-loading');\n    return loader.isDisplayed().catch(() => false);\n  }\n}\n```\n\n---\n\n### Użycie Page Objects w testach\n\n```typescript\n// tests/checkout.spec.ts\n\nimport { remote } from 'webdriverio';\nimport { LoginPage } from '../pages/LoginPage';\nimport { ProductListPage } from '../pages/ProductListPage';\nimport { CheckoutPage } from '../pages/CheckoutPage';\n\nlet driver: WebdriverIO.Browser;\nlet loginPage: LoginPage;\nlet productListPage: ProductListPage;\nlet checkoutPage: CheckoutPage;\n\ntest.beforeEach(async () => {\n  driver = await remote({ /* config */ });\n  await driver.launchApp();\n  \n  loginPage = new LoginPage(driver);\n  productListPage = new ProductListPage(driver);\n  checkoutPage = new CheckoutPage(driver);\n});\n\ntest.afterEach(async () => {\n  await driver.deleteSession();\n});\n\ntest('użytkownik przechodzi pełny checkout', async () => {\n  // Login\n  await loginPage.goto();\n  await loginPage.login('jan@example.test', 'SecurePass123!');\n  \n  // Add to cart\n  await productListPage.search('Laptop');\n  await productListPage.addToCart('prod-001');\n  \n  // Checkout\n  await checkoutPage.fillDeliveryAddress({\n    name: 'Jan Kowalski',\n    street: 'ul. Marszałkowska 1',\n    city: 'Warszawa',\n    postalCode: '00-001',\n  });\n  \n  await checkoutPage.fillPaymentData({\n    cardNumber: '4111111111111111',\n    expiry: '12/26',\n    cvc: '123',\n  });\n  \n  // Sprawdź total\n  const total = await checkoutPage.getTotalAmount();\n  expect(total).toBe('2999.00 PLN');\n  \n  // Submit\n  await checkoutPage.submitPayment();\n  \n  // Oczekuj potwierdzenia\n  const orderConfirmation = driver.$('~order-confirmation');\n  await orderConfirmation.waitFor({ state: 'visible', timeout: 20_000 });\n});\n\ntest('błędna karta — walidacja', async () => {\n  await loginPage.goto();\n  await loginPage.login('jan@example.test', 'SecurePass123!');\n  \n  await checkoutPage.fillDeliveryAddress({\n    name: 'Jan Kowalski',\n    street: 'ul. Marszałkowska 1',\n    city: 'Warszawa',\n    postalCode: '00-001',\n  });\n  \n  // Błędny numer karty\n  await checkoutPage.fillPaymentData({\n    cardNumber: '1234', // za krótka\n    expiry: '12/26',\n    cvc: '123',\n  });\n  \n  await checkoutPage.submitPayment();\n  \n  // Błąd walidacji\n  await checkoutPage.expectValidationError('card');\n});\n```\n\n---\n\n### Zasada wyboru lokatora\n\n| Priorytet | Typ lokatora | Użyj gdy | Przykład |\n|---|---|---|---|\n| **1** | Accessibility ID | Zawsze (jeśli developer dodał) | `'~login-email'` |\n| **2** | Resource ID | Gdy brak accessibility ale masz android:id | `'com.example.shop:id/email_field'` |\n| **3** | UiSelector text | Gdy potrzebujesz dynamicznie znaleźć element | `new UiSelector().text(\"Zaloguj\")` |\n| **4** | XPath | Ostateczność — kruchy i wolny | `'//android.widget.EditText[1]'` |\n\n---\n\n### Jak poprosić developera o accessibility labels\n\n```markdown\n## Wymagania testability dla ekranu logowania\n\n### Accessibility IDs wymagane dla automatyzacji:\n\n| Element | accessibilityLabel | resource-id |\n|---|---|---|\n| Pole email | `login-email` | `com.example.shop:id/email_field` |\n| Pole hasło | `login-password` | `com.example.shop:id/password_field` |\n| Przycisk logowania | `login-submit` | `com.example.shop:id/login_button` |\n| Link \"Zapomniałem hasła\" | `forgot-password-link` | `com.example.shop:id/forgot_password_link` |\n| Komunikat błędu | `login-error-message` | `com.example.shop:id/error_message` |\n\n### Zasada nazewnictwa:\n- `login-{field}` — elementy ekranu logowania\n- `{screen}-{element}` — globalna konwencja\n- Unikaj dynamicznych ID (np. `email_field_1`)\n```"
      },
      {
        "id": "ex-26-2-3",
        "title": "Gesty touch",
        "description": "Zaimplementuj testy gestów: swipe na liście, long press na elemencie, tap na współrzędnych. Użyj touchPerform API.",
        "solution": "## Testy gestów dotykowych — Appium touchPerform API\n\n### 1. Podstawowy Swipe (przewijanie listy)\n\n```typescript\n// tests/gestures.spec.ts\n\ntest('swipe up — przewiń listę produktów w górę', async () => {\n  const driver = await remote({ /* config */ });\n  await driver.launchApp();\n  \n  // Znajdź kontener listy\n  const productList = await driver.$('~product-list');\n  const listBox = await productList.getLocation();\n  \n  // Oblicz współrzędne swipe\n  const startX = listBox.x + listBox.width / 2; // Środek szerokości\n  const startY = listBox.y + listBox.height - 50; // 50px od dołu\n  const endY = listBox.y + 50; // 50px od góry\n  \n  // Wykonaj swipe up ( пальцем od dołu do góry)\n  await driver.touchPerform([\n    { action: 'press', options: { x: startX, y: startY } },\n    { action: 'moveTo', options: { x: startX, y: endY } },\n    { action: 'release', options: {} },\n  ]);\n  \n  // Poczekaj na nowe elementy\n  await driver.pause(500); // Animacja może trwać\n  \n  // Sprawdź, czy pojawiły się nowe produkty\n  const newProductCount = await driver.$$('~product-card-*');\n  // (weryfikacja zależy od implementacji — scroll może być infinite)\n  \n  await driver.deleteSession();\n});\n\ntest('swipe down — przewiń listę w dół', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const productList = await driver.$('~product-list');\n  const listBox = await productList.getLocation();\n  \n  const startX = listBox.x + listBox.width / 2;\n  const startY = listBox.y + 50; // Od góry\n  const endY = listBox.y + listBox.height - 50; // Na dół\n  \n  await driver.touchPerform([\n    { action: 'press', options: { x: startX, y: startY } },\n    { action: 'moveTo', options: { x: startX, y: endY } },\n    { action: 'release', options: {} },\n  ]);\n  \n  await driver.deleteSession();\n});\n\ntest('swipe left — odsłoń akcję na elemencie listy', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const listItem = await driver.$('~wishlist-item-0');\n  const itemBox = await listItem.getLocation();\n  \n  // Swipe left (od prawej do lewej)\n  await driver.touchPerform([\n    { action: 'press', options: { x: itemBox.x + itemBox.width - 50, y: itemBox.y + itemBox.height / 2 } },\n    { action: 'moveTo', options: { x: itemBox.x - 200, y: itemBox.y + itemBox.height / 2 } },\n    { action: 'release', options: {} },\n  ]);\n  \n  // Przycisk \"Usuń\" powinien się pojawić\n  const deleteButton = await driver.$('~delete-wishlist-item-0');\n  await expect(deleteButton).toBeVisible();\n  \n  // Tap na przycisk Delete\n  await deleteButton.click();\n  \n  // Potwierdzenie (undo)\n  const undoButton = await driver.$('~undo-delete');\n  await expect(undoButton).toBeVisible({ timeout: 3_000 });\n  \n  await driver.deleteSession();\n});\n\ntest('swipe right — przywróć ukryty element', async () => {\n  const driver = await remote({ /* config */ });\n  \n  // Najpierw ukryj element (swipe left)\n  const listItem = await driver.$('~notification-item-0');\n  const itemBox = await listItem.getLocation();\n  \n  await driver.touchPerform([\n    { action: 'press', options: { x: itemBox.x + itemBox.width - 50, y: itemBox.y + itemBox.height / 2 } },\n    { action: 'moveTo', options: { x: itemBox.x - 200, y: itemBox.y + itemBox.height / 2 } },\n    { action: 'release', options: {} },\n  ]);\n  \n  // Teraz swipe right, żeby przywrócić\n  await driver.touchPerform([\n    { action: 'press', options: { x: itemBox.x - 200, y: itemBox.y + itemBox.height / 2 } },\n    { action: 'moveTo', options: { x: itemBox.x + itemBox.width - 50, y: itemBox.y + itemBox.height / 2 } },\n    { action: 'release', options: {} },\n  ]);\n  \n  // Element znów widoczny\n  await expect(listItem).toBeVisible();\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 2. Long Press (przytrzymanie)\n\n```typescript\n// Long press — menu kontekstowe\ntest('long press na produkcie pokazuje menu kontekstowe', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const productCard = await driver.$('~product-card-0');\n  const cardBox = await productCard.getLocation();\n  \n  const centerX = cardBox.x + cardBox.width / 2;\n  const centerY = cardBox.y + cardBox.height / 2;\n  \n  // Long press: press + hold (1000ms) + release\n  await driver.touchPerform([\n    { action: 'press', options: { x: centerX, y: centerY } },\n  ]);\n  \n  // Przytrzymaj przez 1 sekundę\n  await driver.pause(1000);\n  \n  await driver.touchPerform([\n    { action: 'release', options: {} },\n  ]);\n  \n  // Menu kontekstowe powinno się pojawić\n  const contextMenu = await driver.$('~context-menu');\n  await contextMenu.waitFor({ state: 'visible', timeout: 3_000 });\n  \n  // Opcje menu\n  await expect(driver.$('~menu-add-to-favorites')).toBeVisible();\n  await expect(driver.$('~menu-share')).toBeVisible();\n  await expect(driver.$('~menu-remove')).toBeVisible();\n  \n  // Zamknij menu (tap poza nim)\n  await driver.touchPerform([\n    { action: 'tap', options: { x: 10, y: 10 } },\n  ]);\n  \n  await contextMenu.waitFor({ state: 'hidden', timeout: 3_000 });\n  \n  await driver.deleteSession();\n});\n\n// Long press z ruchem (drag)\ntest('long press + drag — przenieś element', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const sourceItem = await driver.$('~list-item-0');\n  const targetZone = await driver.$('~drop-zone');\n  \n  const sourceBox = await sourceItem.getLocation();\n  const targetBox = await targetZone.getLocation();\n  \n  const startX = sourceBox.x + sourceBox.width / 2;\n  const startY = sourceBox.y + sourceBox.height / 2;\n  const endX = targetBox.x + targetBox.width / 2;\n  const endY = targetBox.y + targetBox.height / 2;\n  \n  // Long press\n  await driver.touchPerform([\n    { action: 'press', options: { x: startX, y: startY } },\n  ]);\n  await driver.pause(1000); // Hold\n  \n  // Drag w krokach\n  const steps = 10;\n  for (let i = 1; i <= steps; i++) {\n    const currentX = startX + (endX - startX) * (i / steps);\n    const currentY = startY + (endY - startY) * (i / steps);\n    await driver.touchPerform([\n      { action: 'moveTo', options: { x: currentX, y: currentY } },\n    ]);\n    await driver.pause(50);\n  }\n  \n  // Release\n  await driver.touchPerform([\n    { action: 'release', options: {} },\n  ]);\n  \n  // Sprawdź drop\n  const dropFeedback = await driver.$('~item-dropped');\n  await expect(dropFeedback).toBeVisible({ timeout: 3_000 });\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 3. Tap na współrzędnych\n\n```typescript\n// Precyzyjny tap na współrzędnych\ntest('tap na środek przycisku', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const button = await driver.$('~submit-form-button');\n  const buttonBox = await button.getLocation();\n  \n  // Tap w środek przycisku\n  const centerX = buttonBox.x + buttonBox.width / 2;\n  const centerY = buttonBox.y + buttonBox.height / 2;\n  \n  await driver.touchPerform([\n    { action: 'tap', options: { x: centerX, y: centerY } },\n  ]);\n  \n  // Sprawdź akcję\n  const loadingIndicator = await driver.$('~loading-spinner');\n  await expect(loadingIndicator).toBeVisible({ timeout: 2_000 });\n  \n  await driver.deleteSession();\n});\n\n// Podwójny tap (double tap)\ntest('double tap na obrazie — zoom in', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const image = await driver.$('~product-image');\n  const imageBox = await image.getLocation();\n  \n  const centerX = imageBox.x + imageBox.width / 2;\n  const centerY = imageBox.y + imageBox.height / 2;\n  \n  // Pierwszy tap\n  await driver.touchPerform([\n    { action: 'tap', options: { x: centerX, y: centerY } },\n  ]);\n  await driver.pause(200); // Krótka przerwa\n  \n  // Drugi tap\n  await driver.touchPerform([\n    { action: 'tap', options: { x: centerX, y: centerY } },\n  ]);\n  \n  // Sprawdź zoom\n  const zoomIndicator = await driver.$('~zoom-level');\n  await expect(zoomIndicator).toContainText('200%');\n  \n  await driver.deleteSession();\n});\n\n// Tap w różne miejsca ekranu (pary przycisków, radio buttons)\ntest('tap na opcję w grupie radio button', async () => {\n  const driver = await remote({ /* config */ });\n  \n  // Pobierz wszystkie opcje\n  const options = await driver.$$('~payment-option');\n  expect(options).toHaveLength(3); // Karta, BLIK, Przelew\n  \n  // Tap na drugą opcję (BLIK)\n  const secondOption = options[1];\n  const optionBox = await secondOption.getLocation();\n  \n  await driver.touchPerform([\n    { action: 'tap', options: { x: optionBox.x + 20, y: optionBox.y + optionBox.height / 2 } },\n  ]);\n  \n  // Sprawdź, czy jest wybrana\n  const selectedIndicator = await secondOption.$('android=new UiSelector().selected(true)');\n  await expect(selectedIndicator).toBeDisplayed();\n  \n  // Sprawdź formularz BLIK\n  const blikInput = await driver.$('~blik-code-input');\n  await expect(blikInput).toBeVisible();\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 4. Pinch-to-Zoom (multi-touch)\n\n```typescript\n// Pinch gesture — dwa palce\ntest('pinch out (zoom in) na zdjęciu produktu', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const image = await driver.$('~product-image');\n  const imageBox = await image.getLocation();\n  \n  const centerX = imageBox.x + imageBox.width / 2;\n  const centerY = imageBox.y + imageBox.height / 2;\n  \n  // Pinch out: dwa palce zaczynają blisko, rozchodzą się\n  // Lewy palec\n  const leftStartX = centerX - 30;\n  const rightStartX = centerX + 30;\n  \n  // Rozchodzą się do\n  const leftEndX = centerX - 150;\n  const rightEndX = centerX + 150;\n  \n  // Multi-touch wymaga wielu wątków touch\n  // Appium touchPerform może symulować wielopunktowy gest\n  await driver.touchPerform([\n    // Lewy palec press\n    { action: 'press', options: { x: leftStartX, y: centerY, element: image } },\n    // Prawy palec press\n    { action: 'press', options: { x: rightStartX, y: centerY, element: image } },\n  ]);\n  \n  // Przesuń oba palce jednocześnie (symulacja multi-touch)\n  // UWAGA: touchPerform wykonuje akcje sekwencyjnie, nie równolegle!\n  // Dla prawdziwego pinch użyj mobile: pinch\n  \n  await driver.execute('mobile: pinch', {\n    scale: 2.0, // 2x zoom\n    velocity: 1.0,\n    element: image,\n  });\n  \n  // Sprawdź zoom\n  const zoomLevel = await driver.$('~zoom-percentage');\n  await expect(zoomLevel).toContainText('200%');\n  \n  await driver.deleteSession();\n});\n\n// Pinch in (zoom out)\ntest('pinch in (zoom out) na zdjęciu', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const image = await driver.$('~product-image');\n  \n  await driver.execute('mobile: pinch', {\n    scale: 0.5, // 0.5x zoom (pomniejszenie)\n    velocity: 1.0,\n    element: image,\n  });\n  \n  const zoomLevel = await driver.$('~zoom-percentage');\n  await expect(zoomLevel).toContainText('50%');\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 5. ScrollIntoView (automatyczny scroll)\n\n```typescript\n// Scroll do elementu\ntest('scroll do elementu poza ekranem', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const targetElement = await driver.$('~settings-advanced-option');\n  \n  // Metoda 1: scrollIntoView (Android UiScrollable)\n  await driver.execute('mobile: scroll', {\n    element: targetElement,\n    strategy: 'android.view.View',\n    selector: '@content-desc=\"settings-advanced-option\"',\n  });\n  \n  // Sprawdź, czy element jest widoczny\n  await expect(targetElement).toBeVisible();\n  \n  // Metoda 2: manual swipe w pętli\n  async function swipeUntilVisible(selector: string, maxSwipes = 10): Promise<boolean> {\n    for (let i = 0; i < maxSwipes; i++) {\n      const el = await driver.$(selector);\n      const isVisible = await el.isDisplayed().catch(() => false);\n      if (isVisible) return true;\n      \n      // Swipe up\n      const container = await driver.$('~scrollable-container');\n      const box = await container.getLocation();\n      \n      await driver.touchPerform([\n        { action: 'press', options: { x: box.x + 100, y: box.y + box.height - 50 } },\n        { action: 'moveTo', options: { x: box.x + 100, y: box.y + 50 } },\n        { action: 'release', options: {} },\n      ]);\n      \n      await driver.pause(300);\n    }\n    return false;\n  }\n  \n  const found = await swipeUntilVisible('~last-list-item');\n  expect(found).toBe(true);\n  \n  await driver.deleteSession();\n});\n\n// Scroll do końca listy\ntest('scroll do końca listy i sprawdź pagination', async () => {\n  const driver = await remote({ /* config */ });\n  \n  const listContainer = await driver.$('~product-list');\n  \n  // Scroll w pętli aż do końca (brak więcej elementów)\n  let previousCount = 0;\n  let sameCountCount = 0;\n  \n  for (let i = 0; i < 20; i++) { // Max 20 swipe'ów\n    // Policz widoczne elementy\n    const items = await driver.$$('~product-card-*');\n    const currentCount = items.length;\n    \n    if (currentCount === previousCount) {\n      sameCountCount++;\n      if (sameCountCount >= 3) {\n        // 3 razy ta sama ilość = koniec listy\n        break;\n      }\n    } else {\n      sameCountCount = 0;\n    }\n    \n    previousCount = currentCount;\n    \n    // Swipe up\n    const box = await listContainer.getLocation();\n    await driver.touchPerform([\n      { action: 'press', options: { x: box.x + 100, y: box.y + box.height - 50 } },\n      { action: 'moveTo', options: { x: box.x + 100, y: box.y + 50 } },\n      { action: 'release', options: {} },\n    ]);\n    \n    await driver.pause(500);\n  }\n  \n  // Sprawdź czy jest pagination lub \"Koniec listy\"\n  const endMarker = await driver.$('~end-of-list');\n  const loadMoreButton = await driver.$('~load-more-button');\n  \n  const hasEndMarker = await endMarker.isDisplayed().catch(() => false);\n  const hasLoadMore = await loadMoreButton.isDisplayed().catch(() => false);\n  \n  expect(hasEndMarker || hasLoadMore).toBe(true);\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### Helper functions\n\n```typescript\n// utils/touch-helpers.ts\n\nexport async function swipeUp(\n  driver: WebdriverIO.Browser,\n  element: WebdriverIO.Element,\n  distance = 300,\n  duration = 500\n) {\n  const box = await element.getLocation();\n  const centerX = box.x + box.width / 2;\n  const startY = box.y + box.height - 50;\n  const endY = startY - distance;\n  \n  await driver.touchPerform([\n    { action: 'press', options: { x: centerX, y: startY } },\n    { action: 'moveTo', options: { x: centerX, y: Math.max(endY, box.y + 50) } },\n    { action: 'release', options: {} },\n  ]);\n  await driver.pause(duration / 10);\n}\n\nexport async function swipeDown(\n  driver: WebdriverIO.Browser,\n  element: WebdriverIO.Element,\n  distance = 300\n) {\n  const box = await element.getLocation();\n  const centerX = box.x + box.width / 2;\n  const startY = box.y + 50;\n  const endY = startY + distance;\n  \n  await driver.touchPerform([\n    { action: 'press', options: { x: centerX, y: startY } },\n    { action: 'moveTo', options: { x: centerX, y: Math.min(endY, box.y + box.height - 50) } },\n    { action: 'release', options: {} },\n  ]);\n}\n\nexport async function longPress(\n  driver: WebdriverIO.Browser,\n  element: WebdriverIO.Element,\n  durationMs = 1000\n) {\n  const box = await element.getLocation();\n  const centerX = box.x + box.width / 2;\n  const centerY = box.y + box.height / 2;\n  \n  await driver.touchPerform([\n    { action: 'press', options: { x: centerX, y: centerY } },\n  ]);\n  \n  await driver.pause(durationMs);\n  \n  await driver.touchPerform([\n    { action: 'release', options: {} },\n  ]);\n}\n\nexport async function tapElement(\n  driver: WebdriverIO.Browser,\n  element: WebdriverIO.Element\n) {\n  const box = await element.getLocation();\n  const centerX = box.x + box.width / 2;\n  const centerY = box.y + box.height / 2;\n  \n  await driver.touchPerform([\n    { action: 'tap', options: { x: centerX, y: centerY } },\n  ]);\n}\n```\n\n---\n\n### Checklist gestów\n\n| Gest | Użycie | API |\n|---|---|---|\n| **swipe up** | Przewijanie listy w górę | `touchPerform` z press/moveTo/release |\n| **swipe down** | Przewijanie listy w dół | `touchPerform` |\n| **swipe left** | Odsłonięcie akcji (delete) | `touchPerform` |\n| **swipe right** | Przywrócenie ukrytego elementu | `touchPerform` |\n| **long press** | Menu kontekstowe | `press` + `pause(1000)` + `release` |\n| **tap** | Kliknięcie | `touchPerform` z `tap` lub `click()` |\n| **double tap** | Zoom na obrazie | Dwa `tap` z przerwą |\n| **pinch** | Zoom in/out | `mobile: pinch` |\n| **drag** | Przenoszenie elementów | `press` + moveTo w pętli + `release` |\n| **scrollIntoView** | Dojście do elementu poza ekranem | `mobile: scroll` lub manual swipe loop |"
      },
      {
        "id": "ex-26-2-4",
        "title": "Konteksty hybrydowe",
        "description": "Przetestuj aplikację hybrydową: przełącz kontekst NATIVE_APP ↔ WEBVIEW, sprawdź elementy w obu kontekstach i napisz test end-to-end.",
        "solution": "## Testowanie kontekstów w aplikacji hybrydowej\n\n### Architektura testu hybrydowego\n\n```\n┌─────────────────────────────────────┐\n│  NATIVE_APP (Login screen)          │\n│  - accessibilty IDs                 │\n│  - UiAutomator2 selectors           │\n└─────────────────────────────────────┘\n               ↓ (login)\n┌─────────────────────────────────────┐\n│  WEBVIEW (Customer dashboard)       │\n│  - CSS selectors                    │\n│  - XPath webowy                     │\n│  - JavaScript execution             │\n└─────────────────────────────────────┘\n```\n\n---\n\n### 1. Przełączanie kontekstów\n\n```typescript\n// tests/hybrid-context.spec.ts\n\ntest('przełączanie kontekstów — NATIVE → WEBVIEW → NATIVE', async () => {\n  const driver = await remote({ /* config */ });\n  await driver.launchApp();\n  \n  // === KROK 1: Weryfikuj natywny kontekst ===\n  const initialContext = await driver.getContext();\n  console.log('Initial context:', initialContext);\n  // Output: 'NATIVE_APP'\n  \n  // === KROK 2: Praca w kontekście natywnym ===\n  const emailInput = await driver.$('~login-email');\n  await emailInput.waitFor({ state: 'visible', timeout: 5_000 });\n  \n  await emailInput.setValue('jan@example.test');\n  await driver.$('~login-password').setValue('SecurePass123!');\n  \n  // === KROK 3: Kliknij logowanie → czekaj na WebView ===\n  await driver.$('~login-submit').click();\n  \n  // Poczekaj, aż pojawi się WebView\n  console.log('Waiting for WebView...');\n  \n  await driver.waitUntil(async () => {\n    const contexts = await driver.getContexts();\n    return contexts.some(ctx => ctx.id.includes('WEBVIEW'));\n  }, {\n    timeout: 20_000,\n    timeoutMsg: 'WebView context not found after 20s',\n  });\n  \n  // === KROK 4: Przełącz na WebView ===\n  const allContexts = await driver.getContexts();\n  console.log('All contexts:', allContexts.map(c => c.id));\n  // Output: ['NATIVE_APP', 'WEBVIEW_com.example.shop', 'WEBVIEW_chrome']\n  \n  const webviewContext = allContexts.find(ctx => ctx.id.includes('WEBVIEW_com.example.shop'));\n  \n  if (!webviewContext) {\n    throw new Error('WebView context not found');\n  }\n  \n  await driver.switchContext(webviewContext.id);\n  \n  const newContext = await driver.getContext();\n  console.log('Switched to context:', newContext);\n  // Output: 'WEBVIEW_com.example.shop'\n  \n  // === KROK 5: Praca w WebView ===\n  // Teraz używamy selektorów webowych\n  const welcomeHeading = await driver.$('h1.welcome-heading');\n  await welcomeHeading.waitFor({ state: 'visible', timeout: 10_000 });\n  await expect(welcomeHeading).toContainText('Witaj');\n  \n  // Sprawdź listę zamówień\n  const ordersList = await driver.$('.orders-list');\n  await expect(ordersList).toBeVisible();\n  \n  // === KROK 6: Wróć do NATYWNEGO ===\n  await driver.switchContext('NATIVE_APP');\n  \n  const backButton = await driver.$('~back-button');\n  await expect(backButton).toBeVisible();\n  \n  // === KROK 7: Znów w natywnym ===\n  await backButton.click();\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 2. Test end-to-end hybrydowej aplikacji\n\n```typescript\n// tests/hybrid-e2e.spec.ts\n\ntest('pełny przepływ: login (native) → dashboard (webview) → order details (webview) → back (native)', async () => {\n  const driver = await remote({ /* config */ });\n  await driver.launchApp();\n  \n  // ========== FAZA 1: NATIVE LOGIN SCREEN ==========\n  \n  const loginEmail = await driver.$('~login-email');\n  const loginPassword = await driver.$('~login-password');\n  const loginSubmit = await driver.$('~login-submit');\n  \n  await loginEmail.setValue('jan@example.test');\n  await loginPassword.setValue('SecurePass123!');\n  \n  // Sprawdź, że hasło nie jest widoczne (type=password)\n  const passwordType = await loginPassword.getAttribute('password');\n  expect(passwordType).toBe('true');\n  \n  await loginSubmit.click();\n  \n  // ========== FAZA 2: SWITCH TO WEBVIEW ==========\n  \n  await driver.waitUntil(async () => {\n    const ctxs = await driver.getContexts();\n    return ctxs.some(c => c.id.includes('WEBVIEW'));\n  }, { timeout: 20_000 });\n  \n  const webview = (await driver.getContexts()).find(c => c.id.includes('WEBVIEW'));\n  await driver.switchContext(webview.id);\n  \n  // ========== FAZA 3: WEBVIEW DASHBOARD ==========\n  \n  // Poczekaj na załadowanie dashboard\n  await driver.waitUntil(async () => {\n    const heading = await driver.$('h1');\n    const text = await heading.getText().catch(() => '');\n    return text.includes('Panel') || text.includes('Dashboard');\n  }, { timeout: 10_000 });\n  \n  // Zweryfikuj dane użytkownika\n  const userName = await driver.$('.user-name');\n  await expect(userName).toContainText('Jan');\n  \n  // Policz zamówienia\n  const orderItems = await driver.$$('.order-item');\n  expect(orderItems.length).toBeGreaterThan(0);\n  \n  // ========== FAZA 4: WEBVIEW — KLIKNIJ ZAMÓWIENIE ==========\n  \n  // Kliknij na pierwsze zamówienie\n  const firstOrder = orderItems[0];\n  const orderLink = await firstOrder.$('a');\n  await orderLink.click();\n  \n  // Sprawdź szczegóły zamówienia\n  await driver.waitFor('.order-detail', { state: 'visible', timeout: 5_000 });\n  \n  const orderNumber = await driver.$('.order-number');\n  const orderStatus = await driver.$('.order-status');\n  const orderTotal = await driver.$('.order-total');\n  \n  await expect(orderNumber).toBeVisible();\n  await expect(orderStatus).toContainText('W trakcie');\n  \n  // ========== FAZA 5: WYKONAJ AKCJĘ W WEBVIEW ==========\n  \n  // Dodaj notatkę do zamówienia (formularz w WebView)\n  const notesTextarea = await driver.$('.order-notes textarea');\n  await notesTextarea.fill('Dostawa po 18:00');\n  \n  const saveNoteButton = await driver.$('.order-notes button[type=\"submit\"]');\n  await saveNoteButton.click();\n  \n  // Sprawdź feedback\n  const successMessage = await driver.$('.success-message');\n  await expect(successMessage).toBeVisible();\n  \n  // ========== FAZA 6: WYKONAJ AKCJĘ W NATYWNYM ==========\n  \n  // Wróć do native (np. natywny przycisk nawigacji lub hardware back)\n  await driver.switchContext('NATIVE_APP');\n  \n  // Hardware back button (Android)\n  await driver.pressKeyCode(4); // KEYCODE_BACK\n  \n  // Lub natywny przycisk back\n  // const backBtn = await driver.$('~nav-back');\n  // await backBtn.click();\n  \n  // Poczekaj na powrót do dashboard (WebView)\n  await driver.waitUntil(async () => {\n    const ctxs = await driver.getContexts();\n    return ctxs.some(c => c.id.includes('WEBVIEW'));\n  }, { timeout: 5_000 });\n  \n  const currentCtx = await driver.getContext();\n  if (currentCtx.includes('WEBVIEW')) {\n    // Potwierdź, że jesteśmy na liście zamówień\n    const ordersList = await driver.$('.orders-list');\n    await expect(ordersList).toBeVisible();\n  }\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 3. Debugowanie WebView\n\n```typescript\n// Debug WebView content — JavaScript injection\n\ntest('debug WebView — sprawdź strukturę strony', async () => {\n  const driver = await remote({ /* config */ });\n  await driver.launchApp();\n  \n  // Przełącz na WebView\n  await driver.execute('mobile: deepLink', {\n    url: 'myapp://dashboard',\n    package: 'com.example.shop',\n  });\n  \n  await driver.waitUntil(async () => {\n    const ctxs = await driver.getContexts();\n    return ctxs.some(c => c.id.includes('WEBVIEW'));\n  }, { timeout: 20_000 });\n  \n  const webview = (await driver.getContexts()).find(c => c.id.includes('WEBVIEW'));\n  await driver.switchContext(webview.id);\n  \n  // === WEB DEBUG: Pobierz title strony ===\n  const pageTitle = await driver.execute(() => document.title);\n  console.log('Page title:', pageTitle);\n  \n  // === WEB DEBUG: Pobierz wszystkie linki ===\n  const allLinks = await driver.execute(() => {\n    return Array.from(document.querySelectorAll('a')).map(a => ({\n      href: a.href,\n      text: a.textContent?.trim(),\n    }));\n  });\n  console.log('All links:', allLinks);\n  \n  // === WEB DEBUG: Sprawdź stan logowania ===\n  const isLoggedIn = await driver.execute(() => {\n    const userElement = document.querySelector('.user-name');\n    return {\n      isLoggedIn: !!userElement,\n      userName: userElement?.textContent,\n      sessionStorage: window.sessionStorage.getItem('auth_token'),\n    };\n  });\n  console.log('Auth state:', isLoggedIn);\n  \n  // === WEB DEBUG: Pobierz wszystkie elementy formularza ===\n  const formElements = await driver.execute(() => {\n    const forms = document.querySelectorAll('form');\n    return Array.from(forms).map(form => ({\n      id: form.id,\n      action: form.action,\n      inputs: Array.from(form.elements).map(el => ({\n        name: (el as HTMLInputElement).name,\n        type: (el as HTMLInputElement).type,\n        required: (el as HTMLInputElement).required,\n      })),\n    }));\n  });\n  console.log('Forms:', formElements);\n  \n  // === WEB DEBUG: Sprawdź cookies ===\n  const cookies = await driver.execute(() => {\n    return document.cookie.split(';').map(c => c.trim());\n  });\n  console.log('Cookies:', cookies);\n  \n  // === WEB DEBUG: Network interceptor ===\n  // (Wymaga CDP session w Appium)\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 4. Kontekst mieszany — elementy natywne w WebView\n\n```typescript\n// Hybrydowa strona z natywnymi overlay'ami\n\ntest('WebView z natywnym overlayem (loading spinner)', async () => {\n  const driver = await remote({ /* config */ });\n  \n  // Przełącz na WebView\n  await driver.execute('mobile: deepLink', {\n    url: 'myapp://orders',\n    package: 'com.example.shop',\n  });\n  \n  await driver.waitUntil(async () => {\n    const ctxs = await driver.getContexts();\n    return ctxs.some(c => c.id.includes('WEBVIEW'));\n  }, { timeout: 20_000 });\n  \n  const webview = (await driver.getContexts()).find(c => c.id.includes('WEBVIEW'));\n  await driver.switchContext(webview.id);\n  \n  // === WebView: Kliknij filtr ===\n  await driver.$('.filter-dropdown').click();\n  \n  // === Natywny: Overlay się pojawił (loading) ===\n  // WAŻNE: Przełącz się z powrotem na NATIVE_APP żeby sprawdzić overlay!\n  await driver.switchContext('NATIVE_APP');\n  \n  // Sprawdź natywny loading spinner\n  const loadingOverlay = await driver.$('~loading-overlay');\n  await expect(loadingOverlay).toBeVisible();\n  \n  // Sprawdź natywny progress bar\n  const progressBar = await driver.$('~loading-progress-bar');\n  await expect(progressBar).toBeVisible();\n  \n  // Poczekaj aż overlay zniknie\n  await loadingOverlay.waitFor({ state: 'hidden', timeout: 30_000 });\n  \n  // === Wróć do WebView żeby sprawdzić wyniki ===\n  await driver.switchContext(webview.id);\n  \n  // Sprawdź przefiltrowaną listę\n  const filteredItems = await driver.$$('.order-item.filtered');\n  expect(filteredItems.length).toBeGreaterThan(0);\n  \n  await driver.deleteSession();\n});\n\n// Natywny dialog (alert) na stronie WebView\ntest('natywny alert w WebView', async () => {\n  const driver = await remote({ /* config */ });\n  \n  await driver.switchToWebView();\n  \n  // Kliknij przycisk, który wywołuje natywny alert\n  await driver.$('.dangerous-action-button').click();\n  \n  // Przełącz na NATIVE_APP — alert jest natywny!\n  await driver.switchContext('NATIVE_APP');\n  \n  // Sprawdź natywny alert\n  const alertTitle = await driver.$('~alert-title');\n  await expect(alertTitle).toContainText('Potwierdzenie');\n  \n  // Kliknij \"Anuluj\" lub \"OK\"\n  const cancelButton = await driver.$('~alert-cancel');\n  await cancelButton.click();\n  \n  // Wróć do WebView\n  await driver.switchContext(webview.id);\n  \n  // Sprawdź, że akcja została anulowana\n  const actionNotExecuted = await driver.$('.action-pending');\n  await expect(actionNotExecuted).toBeVisible();\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 5. Helper klasa dla kontekstów\n\n```typescript\n// pages/HybridPage.ts — helper do zarządzania kontekstami\n\nimport { WebdriverIO } from 'webdriverio';\n\nexport class HybridPage {\n  private driver: WebdriverIO.Browser;\n  private nativeContextId = 'NATIVE_APP';\n  private webviewContextId: string | null = null;\n\n  constructor(driver: WebdriverIO.Browser) {\n    this.driver = driver;\n  }\n\n  async switchToNative(): Promise<void> {\n    await this.driver.switchContext(this.nativeContextId);\n  }\n\n  async switchToWebView(): Promise<string> {\n    const contexts = await this.driver.getContexts();\n    const webview = contexts.find(c => \n      c.id.includes('WEBVIEW') && c.id.includes('shop') // filtruj tylko app WebViews\n    );\n    \n    if (!webview) {\n      throw new Error('No WebView context found');\n    }\n    \n    this.webviewContextId = webview.id;\n    await this.driver.switchContext(webview.id);\n    return webview.id;\n  }\n\n  async getCurrentContext(): Promise<string> {\n    return this.driver.getContext();\n  }\n\n  async isNativeContext(): Promise<boolean> {\n    const ctx = await this.getCurrentContext();\n    return ctx === this.nativeContextId;\n  }\n\n  async isWebViewContext(): Promise<boolean> {\n    const ctx = await this.getCurrentContext();\n    return ctx.includes('WEBVIEW');\n  }\n\n  async executeInWebView<T>(fn: () => T): Promise<T> {\n    const wasWebView = await this.isWebViewContext();\n    \n    if (!wasWebView) {\n      await this.switchToWebView();\n    }\n    \n    try {\n      return await this.driver.execute(fn as any);\n    } finally {\n      if (!wasWebView) {\n        await this.switchToNative();\n      }\n    }\n  }\n\n  async waitForWebView(timeout = 20_000): Promise<void> {\n    await this.driver.waitUntil(async () => {\n      const ctxs = await this.driver.getContexts();\n      return ctxs.some(c => c.id.includes('WEBVIEW'));\n    }, { timeout, timeoutMsg: 'WebView context not found' });\n  }\n}\n```\n\n---\n\n### Checklist kontekstów\n\n| Problem | Rozwiązanie |\n|---|---|\n| **Element nie znaleziony w WebView** | Upewnij się, że `switchContext` został wywołany |\n| **Stary kontekst cached** | Po `switchContext` sprawdź `getContext()` |\n| **Brak WebView context** | Sprawdź czy WebView jest włączony w ustawieniach app |\n| **Element natywny w WebView** | Przełącz z powrotem na `NATIVE_APP` |\n| **Wiele WebViews** | Filtruj po id: `WEBVIEW_com.example.shop` |\n| **Timeout na switch** | Zwiększ timeout do 30s dla wolnych urządzeń |\n\n---\n\n### Typowy błąd i jak go naprawić\n\n```typescript\n// ❌ BŁĄD: Próba znalezienia elementu bez przełączenia kontekstu\nawait driver.$('.my-class'); // Szuka w NATIVE_APP!\nawait driver.switchContext(webview.id);\n\n// ✅ POPRAWNIE: Najpierw switch, potem find\nawait driver.switchContext(webview.id);\nconst element = await driver.$('.my-class'); // Teraz szuka w WebView\n\n// ❌ BŁĄD: Zapomnienie powrotu do natywnego\nawait driver.switchContext(webview.id);\n// ... web actions ...\nawait driver.$('~native-button').click(); // FAIL! Szuka w WebView!\n\n// ✅ POPRAWNIE: Jawny powrót\nawait driver.switchContext(webview.id);\n// ... web actions ...\nawait driver.switchContext('NATIVE_APP');\nawait driver.$('~native-button').click(); // Teraz działa!\n```"
      },
      {
        "id": "ex-26-2-5",
        "title": "Oczekiwania i waits",
        "description": "Zaimplementuj waits w Appium: implicit wait, explicit wait z WebdriverIO, waitFor state i custom waitUntil.",
        "solution": "## Oczekiwania (Waits) w Appium — kompletny przewodnik\n\n### Architektura waitów\n\n```\nImplicit Wait (sesja)\n  ↓ globalny timeout na każdy findElement\nExplicit Wait (funkcja)\n  ↓ czekaj na konkretny warunek, z resetowalnym timeoutem\nwaitFor (helper)\n  ↓ wrappery na najczęstsze warunki\nCustom waitUntil\n  ↓ własna logika warunku\n```\n\n---\n\n### 1. Implicit Wait (ustawiany raz na sesję)\n\n```typescript\n// Ustaw na początku sesji — wpływa na WSZYSTKIE findElement\n\nconst driver = await remote({\n  hostname: 'localhost',\n  port: 4723,\n  capabilities: { /* ... */ },\n});\n\n// Ustaw implicit wait na 10 sekund\n// Oznacza: jeśli element nie jest znaleziony od razu, czekaj do 10s przed error\nawait driver.setImplicitTimeout(10_000); // 10 sekund\n\n// Teraz każdy `driver.$()` będzie czekać max 10s\nconst element = await driver.$('~login-email');\n// Jeśli element pojawia się po 5s — find powiedzie się\n// Jeśli element nigdy się nie pojawi — error po 10s\n\n// Zmiana implicit timeout\nawait driver.setImplicitTimeout(5_000); // Zmień na 5s\n\n// Wyłączenie implicit wait\nawait driver.setImplicitTimeout(0); // 0 = brak czekania\n```\n\n**Kiedy używać:**\n- Globalny fallback dla wszystkich locators\n- Dobrze działa z stabilnymi locators (accessibility ID)\n- Ustaw na 5-10s dla większości przypadków\n\n---\n\n### 2. Explicit Wait z waitUntil\n\n```typescript\n// waitUntil — najbardziej elastyczny explicit wait\n\n// Czekaj, aż element będzie widoczny\nawait driver.waitUntil(\n  async () => {\n    const el = await driver.$('~login-email');\n    return el && await el.isDisplayed();\n  },\n  {\n    timeout: 10_000, // 10 sekund\n    timeoutMsg: 'Login email input not visible after 10s',\n    interval: 500, // Sprawdzaj co 500ms\n  }\n);\n\n// Czekaj, aż element NIE będzie widoczny (znikanie loadera)\nawait driver.waitUntil(\n  async () => {\n    const loader = await driver.$('~loading-spinner');\n    return !(loader && await loader.isDisplayed());\n  },\n  {\n    timeout: 30_000,\n    timeoutMsg: 'Loading spinner did not disappear in 30s',\n  }\n);\n\n// Czekaj na warunek z wieloma próbami\nawait driver.waitUntil(\n  async () => {\n    const response = await driver.execute(() => {\n      return (window as any).__apiResponseReceived;\n    });\n    return response === true;\n  },\n  { timeout: 15_000 }\n);\n\n// Czekaj na URL\nawait driver.waitUntil(\n  async () => {\n    const url = await driver.getUrl();\n    return url.includes('/dashboard');\n  },\n  { timeout: 10_000 }\n);\n\n// Czekaj na tekst w elemencie\nawait driver.waitUntil(\n  async () => {\n    const el = await driver.$('~status-text');\n    const text = await el.getText();\n    return text?.includes('Sukces');\n  },\n  { timeout: 5_000 }\n);\n```\n\n---\n\n### 3. waitFor — helper methods (najczęstsze warunki)\n\n```typescript\n// waitFor — wygodne wrappery na typowe warunki\n\n// Czekaj, aż element będzie widoczny\nawait driver.waitFor('~login-email', { state: 'visible', timeout: 10_000 });\n\n// Czekaj, aż element zniknie\nawait driver.waitFor('~loading-spinner', { state: 'hidden', timeout: 30_000 });\n\n// Czekaj, aż element będzie clickable (visible + enabled)\nawait driver.waitFor('~submit-button', { state: 'attached', timeout: 5_000 });\n\n// Przykład z Page Object\ntest('waitFor usage w akcji', async () => {\n  const driver = await remote({ /* config */ });\n  \n  await driver.launchApp();\n  \n  // === Czekanie na widoczność ===\n  const emailField = await driver.$('~login-email');\n  await emailField.waitFor({ state: 'visible', timeout: 10_000 });\n  \n  // === Fill + czekanie na zniknięcie loadera ===\n  await emailField.setValue('jan@example.test');\n  \n  const loader = await driver.$('~keyboard-loader');\n  await loader.waitFor({ state: 'hidden', timeout: 5_000 });\n  \n  // === Kolejne waitFor ===\n  const passwordField = await driver.$('~login-password');\n  await passwordField.waitFor({ state: 'visible', timeout: 5_000 });\n  await passwordField.setValue('SecurePass123!');\n  \n  const submitBtn = await driver.$('~login-submit');\n  await submitBtn.waitFor({ state: 'attached', timeout: 5_000 });\n  await submitBtn.click();\n  \n  // === Czekanie na redirect ===\n  await driver.waitUntil(\n    async () => (await driver.getUrl()).includes('/dashboard'),\n    { timeout: 15_000 }\n  );\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 4. Polling strategies\n\n```typescript\n// Różne strategie pollowania — wybór zależy od use case\n\n// Strategia A: Stały interval (500ms)\nawait driver.waitUntil(\n  async () => {\n    const el = await driver.$('~dynamic-element');\n    return el && await el.isDisplayed();\n  },\n  { timeout: 10_000, interval: 500 } // Sprawdza co 500ms\n);\n\n// Strategia B: Rosnący interval (exponential backoff)\n// Dobre dla zasobów, które ładują się powoli\nasync function waitWithExponentialBackoff(\n  driver: WebdriverIO.Browser,\n  selector: string,\n  maxTimeout = 30_000\n): Promise<void> {\n  const startTime = Date.now();\n  let interval = 100;\n  \n  while (Date.now() - startTime < maxTimeout) {\n    const el = await driver.$(selector);\n    if (el && await el.isDisplayed()) return;\n    \n    await driver.pause(interval);\n    interval = Math.min(interval * 1.5, 2000); // Max 2s interval\n  }\n  \n  throw new Error(`Element ${selector} not found after ${maxTimeout}ms`);\n}\n\n// Strategia C: Retry z resetem (dla flaky elements)\n// Dobre dla animacji, transition\nasync function waitForStable(\n  driver: WebdriverIO.Browser,\n  selector: string,\n  stableTimeMs = 1000,\n  maxAttempts = 10\n): Promise<void> {\n  let lastValue: string | null = null;\n  let stableSince: number | null = null;\n  \n  for (let i = 0; i < maxAttempts; i++) {\n    const el = await driver.$(selector);\n    if (!el) {\n      lastValue = null;\n      stableSince = null;\n      await driver.pause(500);\n      continue;\n    }\n    \n    const currentValue = await el.getText().catch(() => null);\n    \n    if (currentValue === lastValue) {\n      if (!stableSince) stableSince = Date.now();\n      if (Date.now() - stableSince >= stableTimeMs) return; // Stable!\n    } else {\n      lastValue = currentValue;\n      stableSince = null;\n    }\n    \n    await driver.pause(200);\n  }\n  \n  throw new Error(`Element ${selector} not stable after ${maxAttempts} attempts`);\n}\n```\n\n---\n\n### 5. Custom waits dla specyficznych scenariuszy\n\n```typescript\n// Wait dla Network Idle (brak requestów przez X ms)\nasync function waitForNetworkIdle(\n  driver: WebdriverIO.Browser,\n  timeoutMs = 10_000,\n  idleThresholdMs = 1000\n): Promise<void> {\n  // Wymaga CDP session\n  const cdpSession = await driver.createCDPSession('app');\n  \n  let lastRequestTime = Date.now();\n  \n  cdpSession.on('Network.requestWillBeSent', () => {\n    lastRequestTime = Date.now();\n  });\n  \n  const startTime = Date.now();\n  \n  while (Date.now() - startTime < timeoutMs) {\n    const idleTime = Date.now() - lastRequestTime;\n    if (idleTime > idleThresholdMs) return; // Idle!\n    await driver.pause(100);\n  }\n  \n  throw new Error(`Network not idle after ${timeoutMs}ms`);\n}\n\n// Wait dla konkretnego tekstu z retry\nasync function waitForTextContains(\n  driver: WebdriverIO.Browser,\n  selector: string,\n  expectedText: string,\n  timeoutMs = 10_000\n): Promise<void> {\n  const startTime = Date.now();\n  \n  while (Date.now() - startTime < timeoutMs) {\n    const el = await driver.$(selector);\n    if (!el) {\n      await driver.pause(200);\n      continue;\n    }\n    \n    const text = await el.getText().catch(() => '');\n    if (text.includes(expectedText)) return; // Found!\n    \n    await driver.pause(200);\n  }\n  \n  const finalText = await driver.$(selector)?.getText() ?? 'N/A';\n  throw new Error(\n    `Text \"${expectedText}\" not found in element ${selector} after ${timeoutMs}ms. ` +\n    `Current text: \"${finalText}\"`\n  );\n}\n\n// Wait dla zmiany URL z retry\nasync function waitForUrlContains(\n  driver: WebdriverIO.Browser,\n  substring: string,\n  timeoutMs = 10_000\n): Promise<void> {\n  const startTime = Date.now();\n  \n  while (Date.now() - startTime < timeoutMs) {\n    const url = await driver.getUrl();\n    if (url.includes(substring)) return;\n    await driver.pause(200);\n  }\n  \n  throw new Error(\n    `URL containing \"${substring}\" not reached after ${timeoutMs}ms. ` +\n    `Current URL: ${await driver.getUrl()}`\n  );\n}\n\n// Wait dla zmiany stanu elementu (np. enabled → disabled)\nasync function waitForAttribute(\n  driver: WebdriverIO.Browser,\n  selector: string,\n  attribute: string,\n  expectedValue: string,\n  timeoutMs = 10_000\n): Promise<void> {\n  const startTime = Date.now();\n  \n  while (Date.now() - startTime < timeoutMs) {\n    const el = await driver.$(selector);\n    if (!el) {\n      await driver.pause(200);\n      continue;\n    }\n    \n    const value = await el.getAttribute(attribute);\n    if (value === expectedValue) return;\n    \n    await driver.pause(200);\n  }\n  \n  const currentValue = await driver.$(selector)?.getAttribute(attribute) ?? 'N/A';\n  throw new Error(\n    `Attribute ${attribute} = \"${expectedValue}\" not set in ${selector} after ${timeoutMs}ms. ` +\n    `Current value: \"${currentValue}\"`\n  );\n}\n```\n\n---\n\n### 6. Page Object z wait helperami\n\n```typescript\n// pages/WaitHelpers.ts — reużywalne waity\n\nexport class WaitHelpers {\n  constructor(private driver: WebdriverIO.Browser) {}\n\n  async forVisible(selector: string, timeout = 10_000): Promise<void> {\n    const el = await this.driver.$(selector);\n    await el.waitFor({ state: 'visible', timeout });\n  }\n\n  async forHidden(selector: string, timeout = 10_000): Promise<void> {\n    const el = await this.driver.$(selector);\n    await el.waitFor({ state: 'hidden', timeout });\n  }\n\n  async forClickable(selector: string, timeout = 10_000): Promise<void> {\n    await this.driver.waitUntil(\n      async () => {\n        const el = await this.driver.$(selector);\n        if (!el) return false;\n        return await el.isDisplayed() && await el.isEnabled();\n      },\n      { timeout, timeoutMsg: `${selector} not clickable` }\n    );\n  }\n\n  async forText(selector: string, text: string, timeout = 10_000): Promise<void> {\n    await this.driver.waitUntil(\n      async () => {\n        const el = await this.driver.$(selector);\n        if (!el) return false;\n        const elText = await el.getText().catch(() => '');\n        return elText.includes(text);\n      },\n      { timeout, timeoutMsg: `Text \"${text}\" not found in ${selector}` }\n    );\n  }\n\n  async forUrl(urlPart: string, timeout = 10_000): Promise<void> {\n    await this.driver.waitUntil(\n      async () => {\n        const url = await this.driver.getUrl();\n        return url.includes(urlPart);\n      },\n      { timeout, timeoutMsg: `URL containing \"${urlPart}\" not reached` }\n    );\n  }\n\n  async forAnimationComplete(selector: string, timeout = 10_000): Promise<void> {\n    // Czekaj, aż element przestanie się animować\n    await this.driver.waitUntil(\n      async () => {\n        const el = await this.driver.$(selector);\n        if (!el) return false;\n        const opacity = await el.getAttribute('opacity');\n        const visibility = await el.getAttribute('visibility');\n        return opacity === '1' && visibility !== 'hidden';\n      },\n      { timeout }\n    );\n  }\n\n  async forElementCount(\n    selector: string,\n    minCount: number,\n    timeout = 10_000\n  ): Promise<number> {\n    const startTime = Date.now();\n    \n    while (Date.now() - startTime < timeout) {\n      const elements = await this.driver.$$(selector);\n      if (elements.length >= minCount) return elements.length;\n      await this.driver.pause(200);\n    }\n    \n    const finalCount = (await this.driver.$$(selector)).length;\n    throw new Error(\n      `Expected at least ${minCount} elements matching \"${selector}\", ` +\n      `found ${finalCount} after ${timeout}ms`\n    );\n  }\n}\n\n// Usage\ntest('with WaitHelpers', async () => {\n  const driver = await remote({ /* config */ });\n  const waits = new WaitHelpers(driver);\n  \n  await driver.launchApp();\n  \n  // Zamiast inline waits\n  await waits.forVisible('~login-email');\n  await driver.$('~login-email').setValue('jan@example.test');\n  \n  await waits.forClickable('~login-submit');\n  await driver.$('~login-submit').click();\n  \n  await waits.forUrl('/dashboard', 20_000);\n  \n  await waits.forHidden('~loading-overlay', 30_000);\n  \n  await waits.forText('.user-name', 'Jan');\n  \n  await driver.deleteSession();\n});\n```\n\n---\n\n### 7. Błędy i debugowanie waitów\n\n```typescript\n// Problemy z waitami i rozwiązania\n\n// Problem: Timeout zawsze się kończy\n// Rozwiązanie: Sprawdź, czy locator jest poprawny\ntest('debug wait timeout', async () => {\n  try {\n    await driver.waitFor('~does-not-exist', { state: 'visible', timeout: 5_000 });\n  } catch (e) {\n    // Pobierz page source żeby zobaczyć strukturę\n    const pageSource = await driver.getPageSource();\n    console.log('Page structure:', pageSource);\n    \n    // Sprawdź co jest na ekranie\n    const allIds = await driver.execute(() => {\n      const elements = document.querySelectorAll('[accessibilitylabel]');\n      return Array.from(elements).map(el => el.getAttribute('accessibilitylabel'));\n    });\n    console.log('Available accessibility labels:', allIds);\n    \n    throw e;\n  }\n});\n\n// Problem: Wait kończy się przedwcześnie (element jest, ale niewidoczny)\n// Rozwiązanie: Użyj `attached` zamiast `visible`\nawait driver.waitFor('~element', { state: 'attached', timeout: 10_000 });\n// attached = element istnieje w DOM (nawet hidden)\n// visible = element istnieje i jest widoczny (nie hidden, not opacity:0)\n\n// Problem: Stale element reference (element znika i wraca)\n// Rozwiązanie: Re-find element przed każdym sprawdzeniem\nawait driver.waitUntil(\n  async () => {\n    try {\n      const el = await driver.$('~dynamic-element');\n      return el && await el.isDisplayed();\n    } catch {\n      return false; // Element został usunięty, retry\n    }\n  },\n  { timeout: 10_000 }\n);\n\n// Problem: Flaky wait (czasem działa, czasem nie)\n// Rozwiązanie: Dodaj margin do timeout\nawait driver.waitFor('~element', { state: 'visible', timeout: 15_000 }); // Było 10s\n\n// Problem: Animacja = element jest widoczny, ale nie clickable\n// Rozwiązanie: Poczekaj na stable state (patrz wyżej waitForStable)\n```\n\n---\n\n### Checklist waitów\n\n| Scenariusz | Wait type | Przykład |\n|---|---|---|\n| **Element pojawia się po akcji** | `waitFor` state=visible | `btn.waitFor({ state: 'visible' })` |\n| **Loader znika** | `waitFor` state=hidden | `loader.waitFor({ state: 'hidden' })` |\n| **URL zmienia się** | `waitUntil` z getUrl | `waitUntil(() => url.includes('/dashboard'))` |\n| **Tekst się zmienia** | `waitUntil` z getText | `waitUntil(async () => (await el.getText()).includes('OK'))` |\n| **Element clickable** | `waitFor` state=attached | `btn.waitFor({ state: 'attached' })` |\n| **Wiele elementów ładuje się** | `waitForElementCount` | `waitForElementCount('.item', 10)` |\n| **Network idle** | Custom wait | `waitForNetworkIdle()` |\n| **Animacja kończy się** | `waitForStable` | `waitForStable('.animated-element')` |\n| **Upłynął timeout** | Fail z message | Throw z aktualnym stanem |\n| **Implicit** | `setImplicitTimeout` | Globalny fallback 10s |"
      },
      {
        "id": "ex-26-2-6",
        "title": "Stabilność testów",
        "description": "Napraw flaky test: analyze why it fails, implement retry, add logging, and verify the fix works.",
        "solution": "## Diagnostyka i naprawa flaky tests — kompletny proces\n\n### Faza 1: Klasyfikacja problemu\n\n```typescript\n// Klasyfikacja flaky tests — funkcja pomocnicza\n\ntype FailureType = 'timing' | 'data' | 'logic' | 'infrastructure' | 'unknown';\n\nasync function classifyFlakyTest(testName: string, driver: WebdriverIO.Browser): Promise<FailureType> {\n  // Pobierz screenshot i logi\n  const screenshot = await driver.takeScreenshot();\n  const pageSource = await driver.getPageSource();\n  const logcat = await driver.execute('mobile: logCat', { filters: ['Error', 'Exception'] });\n  \n  // Analiza screenshot\n  if (screenshot) {\n    // Sprawdź czy jest loading spinner (timing issue)\n    if (pageSource.includes('loading') && !pageSource.includes('content-loaded')) {\n      return 'timing';\n    }\n  }\n  \n  // Analiza logów\n  if (logcat.includes('NullPointerException')) return 'logic';\n  if (logcat.includes('NetworkException')) return 'infrastructure';\n  if (logcat.includes('StaleObject')) return 'timing';\n  \n  // Analiza page source\n  if (pageSource.includes('undefined') || pageSource.includes('null')) {\n    return 'data';\n  }\n  \n  return 'unknown';\n}\n\n// Usage\nconst failureType = await classifyFlakyTest('test-checkout', driver);\nconsole.log(`Failure type: ${failureType}`);\n```\n\n---\n\n### Faza 2: Analiza artefaktów\n\n```typescript\n// analyze-failure.ts — narzędzie do analizy awarii\n\nimport fs from 'fs';\n\nasync function analyzeFailureArtifacts(\n  testName: string,\n  runId: string,\n  artifactsDir: string\n) {\n  console.log(`=== Analyzing failure: ${testName} (run: ${runId}) ===\\n`);\n  \n  // 1. Screenshot analysis\n  const screenshotPath = `${artifactsDir}/screenshot-${runId}.png`;\n  if (fs.existsSync(screenshotPath)) {\n    console.log('📸 Screenshot exists');\n    // W prawdziwym narzędziu: ML analysis lub manual review\n  }\n  \n  // 2. Logcat analysis\n  const logcatPath = `${artifactsDir}/logcat-${runId}.txt`;\n  if (fs.existsSync(logcatPath)) {\n    const logcat = fs.readFileSync(logcatPath, 'utf-8');\n    \n    // Szukaj wzorców\n    const patterns = [\n      { name: 'ANR', pattern: /ANR|Application Not Responding/i },\n      { name: 'Crash', pattern: /FATAL EXCEPTION|java.lang.RuntimeException/i },\n      { name: 'Network', pattern: /NetworkException|SocketTimeoutException/i },\n      { name: 'Permission', pattern: /SecurityException|Permission denied/i },\n      { name: 'StaleElement', pattern: /StaleObject|element is not attached/i },\n    ];\n    \n    for (const { name, pattern } of patterns) {\n      if (pattern.test(logcat)) {\n        console.log(`🔴 Found: ${name}`);\n      }\n    }\n  }\n  \n  // 3. Video analysis (jeśli jest)\n  const videoPath = `${artifactsDir}/video-${runId}.mp4`;\n  if (fs.existsSync(videoPath)) {\n    console.log('🎥 Video exists — analyze timeline for timing issues');\n  }\n  \n  // 4. Device info\n  const deviceInfoPath = `${artifactsDir}/device-info-${runId}.json`;\n  if (fs.existsSync(deviceInfoPath)) {\n    const deviceInfo = JSON.parse(fs.readFileSync(deviceInfoPath, 'utf-8'));\n    console.log(`📱 Device: ${deviceInfo.model} (${deviceInfo.osVersion})`);\n    console.log(`   Memory: ${deviceInfo.availMem} available / ${deviceInfo.totalMem} total`);\n  }\n  \n  // 5. Network logs\n  const harPath = `${artifactsDir}/network-${runId}.har`;\n  if (fs.existsSync(harPath)) {\n    const har = JSON.parse(fs.readFileSync(harPath, 'utf-8'));\n    const failedRequests = har.log.entries.filter(e => e.response.status >= 400);\n    if (failedRequests.length > 0) {\n      console.log(`🔴 ${failedRequests.length} failed network requests:`);\n      failedRequests.forEach(r => console.log(`   ${r.request.method} ${r.request.url} → ${r.response.status}`));\n    }\n  }\n  \n  console.log('\\n=== Recommendation ===');\n  // Generate recommendation based on findings\n}\n```\n\n---\n\n### Faza 3: Naprawa — timing issues\n\n```typescript\n// Problem: Test pada bo nie czeka na async operation\n// Stary kod:\ntest.beforeEach(async () => {\n  await driver.$('~submit-button').click();\n  // ❌ Od razu szukaj następnego elementu bez czekania\n  await driver.$('~success-message').click();\n});\n\n// Naprawiony kod:\ntest.beforeEach(async () => {\n  await driver.$('~submit-button').click();\n  \n  // ✅ Czekaj na transition / loading\n  const loader = await driver.$('~loading-overlay');\n  await loader.waitFor({ state: 'hidden', timeout: 30_000 });\n  \n  // ✅ Dodatkowy wait na DOM update\n  await driver.waitUntil(async () => {\n    const successMsg = await driver.$('~success-message');\n    return successMsg && await successMsg.isDisplayed();\n  }, { timeout: 10_000 });\n  \n  await driver.$('~success-message').click();\n});\n\n// Alternatywa: Wait na URL change\ntest.beforeEach(async () => {\n  const currentUrl = await driver.getUrl();\n  await driver.$('~submit-button').click();\n  \n  // ✅ Czekaj aż URL się zmieni (nie tylko loading spinner)\n  await driver.waitUntil(\n    async () => {\n      const newUrl = await driver.getUrl();\n      return newUrl !== currentUrl;\n    },\n    { timeout: 15_000, timeoutMsg: 'URL did not change after submit' }\n  );\n  \n  await driver.$('~success-message').click();\n});\n```\n\n---\n\n### Faza 4: Naprawa — data issues\n\n```typescript\n// Problem: Test używa losowych danych które kolidują\n// Stary kod:\ntest('checkout z losowym email', async () => {\n  const email = `test-${Math.random().toString(36).substr(2, 9)}@example.test`;\n  await driver.$('~email-input').setValue(email);\n  // Problem: Po wielu uruchomieniach — duplicate email error\n});\n\n// Naprawiony kod z deterministic data\nimport { DataFaker } from './utils/data-faker';\n\nconst faker = new DataFaker();\n\ntest('checkout z unique email (data builder)', async () => {\n  const testData = faker.createTestUser();\n  // Generuje: test-1719000000@example.test\n  // timestamp = unikalny, deterministyczny\n  \n  await driver.$('~email-input').setValue(testData.email);\n  await driver.$('~password-input').setValue(testData.password);\n  \n  // Cleanup po teście\n  test.afterEach(async () => {\n    await faker.cleanupUser(testData.id);\n  });\n});\n\n// Problem: Race condition w parallel tests (ten sam zasób)\n// Rozwiązanie: Lock na shared resource\nimport { ResourceLock } from './utils/resource-lock';\n\nconst lock = new ResourceLock('checkout-item');\n\ntest('checkout parallel-safe', async () => {\n  await lock.acquire(); // Rezerwuj zasób\n  \n  try {\n    // Test...\n    const item = await driver.$('~checkout-item-1');\n    await item.click();\n    // ...\n  } finally {\n    await lock.release(); // Zwolnij zasób\n  }\n});\n```\n\n---\n\n### Faza 5: Retry strategy\n\n```typescript\n// Retry wrapper — dla niestabilnych testów\n\nasync function retryWithBackoff<T>(\n  fn: () => Promise<T>,\n  maxRetries = 3,\n  baseDelay = 1000\n): Promise<T> {\n  let lastError: Error | null = null;\n  \n  for (let attempt = 1; attempt <= maxRetries; attempt++) {\n    try {\n      return await fn();\n    } catch (error) {\n      lastError = error as Error;\n      \n      if (attempt < maxRetries) {\n        const delay = baseDelay * Math.pow(2, attempt - 1); // Exponential backoff\n        console.log(`Attempt ${attempt} failed, retrying in ${delay}ms...`);\n        await new Promise(resolve => setTimeout(resolve, delay));\n      }\n    }\n  }\n  \n  throw lastError;\n}\n\n// Usage\ntest('stable checkout retry', async () => {\n  await retryWithBackoff(async () => {\n    await driver.$('~checkout-button').click();\n    await driver.waitFor('~order-confirmation', { state: 'visible', timeout: 20_000 });\n  }, 3, 2000);\n});\n\n// Playwright config — global retry\n// playwright.config.ts\nexport default defineConfig({\n  retries: {\n    // Retry failed tests\n    failedTests: 2,\n    // Retry flaky tests (known)\n    // Usuń z tej listy po naprawie\n  },\n  // Sharding — rozłóż testy na wielu workerów\n  // Jeśli 1 test na 1 worker pada, to flaky\n  // Jeśli wiele testów na 1 worker pada, to infrastructure\n  workers: process.env['CI'] ? 4 : undefined,\n});\n\n// Appium retry per session\ntest('checkout with session retry', async () => {\n  let attempts = 0;\n  \n  while (attempts < 3) {\n    attempts++;\n    try {\n      const driver = await createAppiumSession();\n      \n      await driver.$('~checkout-button').click();\n      await driver.waitFor('~order-confirmation', { state: 'visible', timeout: 20_000 });\n      \n      await driver.deleteSession();\n      return; // Success\n    } catch (error) {\n      console.error(`Attempt ${attempts} failed:`, error);\n      if (attempts < 3) {\n        // Nowy driver na każdą próbę\n        await new Promise(r => setTimeout(r, 2000));\n      }\n    }\n  }\n  \n  throw new Error(`All 3 attempts failed`);\n});\n```\n\n---\n\n### Faza 6: Logging i monitoring\n\n```typescript\n// utils/test-logger.ts — enhanced logging dla flaky detection\n\nexport class TestLogger {\n  private logs: Array<{ timestamp: number; level: string; message: string; data?: unknown }> = [];\n  \n  info(message: string, data?: unknown) {\n    this.log('INFO', message, data);\n  }\n  \n  error(message: string, data?: unknown) {\n    this.log('ERROR', message, data);\n  }\n  \n  warn(message: string, data?: unknown) {\n    this.log('WARN', message, data);\n  }\n  \n  private log(level: string, message: string, data?: unknown) {\n    const entry = { timestamp: Date.now(), level, message, data };\n    this.logs.push(entry);\n    console.log(`[${level}] ${message}`, data ?? '');\n  }\n  \n  async saveArtifacts(prefix: string) {\n    const fs = await import('fs');\n    const dir = 'test-results';\n    fs.mkdirSync(dir, { recursive: true });\n    \n    fs.writeFileSync(\n      `${dir}/${prefix}-${Date.now()}.json`,\n      JSON.stringify(this.logs, null, 2)\n    );\n  }\n  \n  getTimeline() {\n    return this.logs.map(l => ({\n      time: new Date(l.timestamp).toISOString(),\n      level: l.level,\n      message: l.message,\n    }));\n  }\n}\n\n// Usage in test\nconst logger = new TestLogger();\n\ntest('logged checkout', async () => {\n  logger.info('Starting checkout test');\n  \n  logger.info('Clicking checkout button');\n  const checkoutBtn = await driver.$('~checkout-button');\n  await checkoutBtn.click();\n  \n  logger.info('Waiting for redirect', { url: await driver.getUrl() });\n  \n  try {\n    await driver.waitFor('~order-confirmation', { state: 'visible', timeout: 20_000 });\n    logger.info('Order confirmation visible');\n  } catch (e) {\n    logger.error('Order confirmation not visible', {\n      currentUrl: await driver.getUrl(),\n      pageSource: await driver.getPageSource(),\n    });\n    \n    // Save screenshot + logs\n    await driver.takeScreenshot();\n    await logger.saveArtifacts('checkout-failure');\n    throw e;\n  }\n  \n  logger.info('Test completed successfully');\n});\n```\n\n---\n\n### Faza 7: Weryfikacja naprawy\n\n```typescript\n// verify-fix.ts — weryfikacja że naprawa działa\n\nasync function runReliabilityCheck(\n  testName: string,\n  runCount = 10,\n  maxFailures = 1 // Toleruj max 1 failure na 10 runs\n): Promise<{ successRate: number; failures: number }> {\n  let failures = 0;\n  \n  for (let i = 0; i < runCount; i++) {\n    console.log(`Run ${i + 1}/${runCount}...`);\n    \n    try {\n      const driver = await createAppiumSession();\n      await runTest(testName, driver);\n      await driver.deleteSession();\n      console.log(`  ✅ Run ${i + 1} passed`);\n    } catch (e) {\n      failures++;\n      console.log(`  ❌ Run ${i + 1} failed: ${e.message}`);\n    }\n  }\n  \n  const successRate = ((runCount - failures) / runCount) * 100;\n  console.log(`\\nReliability: ${successRate}% (${failures}/${runCount} failures)`);\n  \n  return { successRate, failures };\n}\n\n// Usage\ntest('reliability check for checkout', async () => {\n  const result = await runReliabilityCheck('test-checkout', 10);\n  \n  // Pass jeśli >= 90% success rate\n  expect(result.successRate).toBeGreaterThanOrEqual(90);\n});\n\n// CI integration\n// .github/workflows/reliability-test.yml\n/*\nname: Reliability Test\n\non:\n  schedule:\n    - cron: '0 6 * * 1' # Weekly (Monday 6 AM)\n\njobs:\n  reliability:\n    runs-on: ubuntu-latest\n    strategy:\n      matrix:\n        test: ['checkout', 'login', 'search']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '20' }\n      - run: npm ci\n      - name: Run reliability check (10 iterations)\n        run: npx playwright test reliability/${matrix.test}.spec.ts\n        env:\n          EXPECTED_SUCCESS_RATE: 95\n          MAX_FAILURES: 1\n      - name: Report\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: reliability-${matrix.test}\n          path: test-results/\n*/\n```\n\n---\n\n### Checklist naprawy flaky test\n\n| Krok | Działanie | Cel |\n|---|---|---|\n| **1. Klasyfikuj** | Analizuj screenshot + logcat | Timing vs. data vs. logic vs. infra |\n| **2. Izoluj** | Uruchom test samodzielnie | Czy pada też solo? |\n| **3. Reprodukuj** | Uruchom test wielokrotnie | Potwierdź flaky |\n| **4. Napraw** | Zidentyfikowana przyczyna → fix | Timing = waits, Data = cleanup |\n| **5. Weryfikuj** | 10+ uruchomień z metrykami | Potwierdź > 90% success rate |\n| **6. Monitoruj** | CI tracking pass rate | Wykryj regresję |\n| **7. Dokumentuj** | Zapisz fix w runbook | Następna osoba wie |\n\n---\n\n### Regression test dla flaky\n\n```typescript\n// Po naprawie — dodaj regression test\n// tests/regression/flaky-checkout.spec.ts\n\ntest.describe('Regression: Previously flaky checkout', () => {\n  // Ten test ma wykryć czy fix nadal działa\n  \n  test('checkout button click → confirmation (no flaky)', async () => {\n    // Reprodukcja oryginalnego problemu\n    const submitBtn = await driver.$('~checkout-button');\n    await submitBtn.click();\n    \n    // Kluczowy fix: wait for loading complete\n    const loader = await driver.$('~loading-overlay');\n    await loader.waitFor({ state: 'hidden', timeout: 30_000 });\n    \n    // Teraz szukaj confirmation\n    await driver.waitFor('~order-confirmation', { state: 'visible', timeout: 15_000 });\n    \n    // Potwierdź\n    await expect(driver.$('~order-confirmation')).toBeVisible();\n  });\n  \n  test('checkout parallel with another user (no race condition)', async () => {\n    // Test przeciw race condition\n    const user1 = await createTestUser();\n    const user2 = await createTestUser();\n    \n    // Równoległe checkouty\n    await Promise.all([\n      performCheckout(user1, driver1),\n      performCheckout(user2, driver2),\n    ]);\n    \n    // Oba powinny się udać\n    const order1 = await verifyOrder(user1.id);\n    const order2 = await verifyOrder(user2.id);\n    \n    expect(order1.status).not.toBe(order2.status); // Różne stany (nie ten sam zasób)\n  });\n});\n```"
      }
    ],
    "quiz": [
      {
        "id": "q26-2-1",
        "question": "Czym różni się mobile web od aplikacji natywne?",
        "options": [
          "Mobile web działa w przeglądarce, natywne korzysta z API systemu i jest instalowana jako aplikacja",
          "Niczym",
          "Native zawsze jest stroną HTML",
          "Mobile web nie wymaga sieci"
        ],
        "correctAnswer": 0,
        "explanation": "Typ aplikacji decyduje o narzędziach, ryzykach i sposobie automatyzacji."
      },
      {
        "id": "q26-2-2",
        "question": "Dlaczego realne urządzenia są ważne?",
        "options": [
          "Ujawniają problemy sprzętowe, systemowe, sieciowe i wydajnościowe niewidoczne w emulatorze",
          "Bo emulatory nigdy nie działają",
          "Bo zastępują wszystkie testy",
          "Tylko do screenshotów"
        ],
        "correctAnswer": 0,
        "explanation": "Emulator nie oddaje w pełni zachowania prawdziwego urządzenia."
      },
      {
        "id": "q26-2-3",
        "question": "Co jest kluczowe w testach uprawnień?",
        "options": [
          "Sprawdzenie przyznania, odmowy i zmiany uprawnień",
          "Tylko happy path",
          "Ukrycie promptów systemowych",
          "Brak resetu aplikacji"
        ],
        "correctAnswer": 0,
        "explanation": "Uprawnienia są częścią przepływu użytkownika i źródłem wielu błędów."
      },
      {
        "id": "q26-2-4",
        "question": "Po co testować deep links?",
        "options": [
          "Aby sprawdzić wejście do aplikacji z zewnętrznego kontekstu",
          "Aby zmienić CSS",
          "Aby ominąć routing",
          "Wyłącznie dla desktopu"
        ],
        "correctAnswer": 0,
        "explanation": "Deep linki są częstym sposobem wejścia z e-maila, push notification lub kampanii."
      },
      {
        "id": "q26-2-5",
        "question": "Co powinien zawierać raport awarii mobilnej?",
        "options": [
          "Model urządzenia, OS, wersję aplikacji, logi, screenshot/wideo i kroki reprodukcji",
          "Tylko nazwę testu",
          "Sam kolor ekranu",
          "Bez danych środowiska"
        ],
        "correctAnswer": 0,
        "explanation": "Bez informacji o urządzeniu i systemie awaria mobilna bywa niemożliwa do odtworzenia."
      },
      {
        "id": "q26-2-6",
        "question": "Kiedy device farm jest szczególnie przydatna?",
        "options": [
          "Gdy potrzebujemy pokryć wiele realnych urządzeń bez ich lokalnego utrzymywania",
          "Do testowania wyłącznie unit",
          "Do zastąpienia strategii",
          "Nigdy"
        ],
        "correctAnswer": 0,
        "explanation": "Device farm daje dostęp do macierzy urządzeń i systemów."
      },
      {
        "id": "q26-2-7",
        "question": "Co odróżnia dobry test tryb offline?",
        "options": [
          "Jawnie przełącza sieć, sprawdza komunikat i synchronizację po powrocie online",
          "Dodaje sleep",
          "Ignoruje dane lokalne",
          "Działa tylko na desktopie"
        ],
        "correctAnswer": 0,
        "explanation": "Offline mode wymaga kontroli sieci i stanu lokalnego."
      },
      {
        "id": "q26-2-8",
        "question": "Najważniejsza zasada lekcji „Podstawy Appium” to:",
        "options": [
          "Testy mobilne muszą kontrolować urządzenie, system i stan aplikacji",
          "Wystarczy jeden emulator",
          "Uprawnień nie testujemy",
          "Sieć zawsze jest stabilna"
        ],
        "correctAnswer": 0,
        "explanation": "Mobilność oznacza zmienne urządzenia, warunki i integracje z systemem."
      }
    ],
    "references": [
      {
        "title": "Appium Documentation",
        "url": "https://appium.io/docs/en/latest/",
        "description": "Dokumentacja Appium, standardu automatyzacji aplikacji mobilnych."
      },
      {
        "title": "Android Developers - Test apps on Android",
        "url": "https://developer.android.com/training/testing",
        "description": "Oficjalne materiały Google o testowaniu aplikacji Android."
      },
      {
        "title": "Apple Developer - Testing",
        "url": "https://developer.apple.com/testing/",
        "description": "Materiały Apple o testowaniu aplikacji iOS i ekosystemu Apple."
      },
      {
        "title": "BrowserStack App Automate",
        "url": "https://www.browserstack.com/docs/app-automate",
        "description": "Dokumentacja testowania aplikacji mobilnych na device farm."
      }
    ],
    "tipsAndTricks": [
      "Macierz urządzeń powinna wynikać z danych użytkowników i ryzyka, nie z listy wszystkich możliwych modeli.",
      "Emulator jest świetny do szybkiej informacji zwrotnej, ale realne urządzenie ujawnia problemy sprzętowe, sieciowe i systemowe.",
      "W testach mobilnych szczególnie dbaj o stan aplikacji: uprawnienia, pamięć, sieć, sesję i dane lokalne.",
      "Każdy test deep linku, tryb offline lub push notification powinien mieć jawny setup i cleanup stanu urządzenia."
    ],
    "commonMistakes": [
      {
        "mistake": "Testowanie wyłącznie na jednym emulatorze",
        "solution": "Zbuduj małą, ryzykową macierz urządzeń obejmującą realne modele, OS i rozdzielczości."
      },
      {
        "mistake": "Ignorowanie uprawnień systemowych",
        "solution": "Testuj odmowę, przyznanie i zmianę uprawnień dla kamery, lokalizacji, powiadomień i plików."
      },
      {
        "mistake": "Brak kontroli stanu aplikacji",
        "solution": "Czyść dane aplikacji, resetuj sesję i jawnie ustawiaj warunki początkowe."
      },
      {
        "mistake": "Mylenie mobile web z natywne",
        "solution": "Dobierz narzędzia i strategię do typu aplikacji: RWD/PWA, hybrydowe albo natywne."
      }
    ]
  }
};